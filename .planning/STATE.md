---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: "Phase 06 planned (7 plans × 5 waves; plan-checker iter 2 PASSED). Chain auto-advances to execute-phase. Phase 05 real-mode canary still EMPIRICAL-PENDING."
last_updated: "2026-05-15T15:41:27.648Z"
progress:
  total_phases: 14
  completed_phases: 4
  total_plans: 27
  completed_plans: 29
  percent: 100
---

# STATE: docintel

## Project Reference

See: `.planning/PROJECT.md` (updated 2026-05-11)

**Core value:** Eval harness reports real numbers (Hit@5, MRR, faithfulness, citation accuracy, latency, $/query) with Wilson + bootstrap CIs, re-run by CI on every PR.
**Current focus:** Phase 6 — generation (next; Phase 5 merged via PR #5 with EMPIRICAL-PENDING real-mode canary running).

## Where We Are

- **Phase 1 (scaffold-foundation)** — ✓ Complete, merged to `main` (`f40bca7`) on 2026-04-28.
- **Phase 2 (adapters-protocols)** — ✓ Complete, merged to `main` (`2da4a21`, PR #2) on 2026-05-13. All ADP-01..ADP-07 closed.
- **Phase 3 (corpus-ingestion)** — ✓ Complete, merged to `main` (PR #3). 6,053 chunks committed. ING-01..ING-04 closed.
- **Phase 4 (embedding-indexing)** — ✓ Complete, merged to `main` (PR #4) on 2026-05-14. IDX-01..04 closed; verifier 15/15.
- **Phase 5 (retrieval-hybrid-rerank)** — ✓ Complete, merged to `main` (`2768961`, PR #5) on 2026-05-14. 7 plans × 4 waves shipped. RET-01..04 closed (RET-03 EMPIRICAL-PENDING — see "Hard Gates To Remember" below). CI fixes (`b07d305` ruff/black + `c020075` ci.yml reorder + `9ec4d36` lazy LLM SDK init + gitleaks allowlist) applied post-merge.
- **Phases 6–13** — Pending. See `.planning/ROADMAP.md`.
- **Phase 14 (v2)** — Deferred.

## Recent Activity

- 2026-05-15 — Phase 6 planned via `/gsd-plan-phase 6 --auto`. 7 plans landed at `.planning/phases/06-generation/06-{01..07}-PLAN.md` across 5 waves: Wave 0 (test scaffolds 06-01 + package skeleton+CI gate+5 doc-path updates+uv.lock 06-02; parallel disjoint) → Wave 1 (prompts.py + parse.py + Wave-1 re-exports 06-03) → Wave 2 (Generator class + make_generator factory + GenerationResult Pydantic in core + Wave-2 re-exports 06-04) → Wave 3 (stub _STUB_REFUSAL re-alias + _CHUNK_RE re-import 06-05; judge migration with structured-output via Anthropic tools=[{strict:true}] / OpenAI response_format json_schema 06-06; parallel disjoint single-file edits) → Wave 4 (CI YAML wiring + 20-of-21 xfail removal sweep + 27/27 Decision-Coverage Audit + CHECKPOINT 06-07). RESEARCH.md (1247 lines, 19 sections, 10 pitfalls, 3 code examples) + PATTERNS.md (1038 lines, 21 files classified with line-numbered analog excerpts) + VALIDATION.md (10 test scaffolds + 2 fixture dirs, per-task verification map) captured upstream. Plan-checker iter 1 found 1 BLOCKER (Plan 06-07 cited non-existent `05-DECISIONS-AUDIT.md`; replaced with actual `05-VERIFICATION.md` at 5 occurrences) + 4 WARNINGs (capture_logs analog redirected to `tests/test_retriever_search.py:27+43+107-127+208`; 14-vs-15 field count harmonized to 15 named fields per D-16 verbatim; Plan 06-07 truths "All 21" → "20 of the 21" preserving real_hero xfail per Phase 5 precedent). All 4 fixes applied inline. Plan-checker iter 2 returned VERIFICATION PASSED (with non-blocking nits). Pitfall 9 (import cycle stub → docintel-generate) resolved: `REFUSAL_TEXT_SENTINEL` placed in `docintel_core.types` (Plan 06-02); `_CHUNK_RE` placed in `docintel_generate.parse` (Plan 06-03); stub adapter (Plan 06-05) imports both from their canonical homes — ONE allowed cross-package import per CONTEXT.md D-12. Chain mode auto-advances to execute-phase next.

- 2026-05-15 — Phase 6 context gathered via `/gsd-discuss-phase 6 --chain`. Decisions D-01..D-17 captured in `.planning/phases/06-generation/06-CONTEXT.md`: **new 8th workspace package `docintel-generate`** at `packages/docintel-generate/` (mirrors Phase 3/4/5 precedent); **CLAUDE.md / config.json / REQUIREMENTS GEN-01 / ROADMAP / PROJECT.md path-references updated** by Phase 6 plan (original `src/docintel/generation/prompts.py` literal path never existed in this clone); **three named module constants** `SYNTHESIS_PROMPT` / `REFUSAL_PROMPT` / `JUDGE_PROMPT` with per-prompt `_SYNTHESIS_HASH`/`_REFUSAL_HASH`/`_JUDGE_HASH` (sha256[:12]) + combined `PROMPT_VERSION_HASH` (EVAL-02 manifest reads combined; ablation reports + structlog read per-prompt); **judge prompt + parser migrated** from Phase 2 placeholder in `adapters/real/judge.py` (heuristic `_SCORE_PATTERN` regex → Anthropic tool-use / OpenAI `response_format` structured-output deserializing directly into `JudgeVerdict`); Phase 2 D-04 cross-family wiring untouched; **inline `[chunk_id]` citations** with locked fenced example in `SYNTHESIS_PROMPT` matching existing stub `_CHUNK_RE = re.compile(r"\[([^\]]+)\]")`; **`Generator(bundle, retriever)` class** + 4th sibling factory `make_generator(cfg)` in `docintel_core.adapters.factory`; **`GenerationResult` Pydantic model** in `docintel_core.types` with `ConfigDict(extra="forbid", frozen=True)` carrying `text`, `cited_chunk_ids`, `refused`, `retrieved_chunks`, `completion: CompletionResponse | None`, `prompt_version_hash`; **dual-layer refusal** — hard zero-chunk skip + LLM-driven sentinel `"I cannot answer this question from the retrieved 10-K excerpts."` (replaces Phase 2 stub `_STUB_REFUSAL` value); **numbered `<context>` blocks** with `[chunk_id | company | fiscal_year | section]` headers as the chunk-formatting contract; **faithfulness measured, not enforced** (hallucinated chunk_ids logged via `generator_hallucinated_chunk_id` warning + dropped from `cited_chunk_ids`; no re-prompting, no sentence-stripping; Phase 9 MET-03/MET-04 own the measurement); **single `generator_completed` structlog line** mirroring Phase 5 `retriever_search_completed` with 14 fields (Phase 9 MET-05 reads `cost_usd`+`total_ms`; Phase 12 binds `trace_id` via contextvars without retrofit); **new CI grep gate `scripts/check_prompt_locality.sh`** mirroring `check_adapter_wraps.sh`/`check_index_wraps.sh`/`check_ingest_wraps.sh` (path allowlist + per-line `# noqa: prompt-locality` escape; default-excludes `prompts.py`, `tests/`, `conftest.py`, `adapters/stub/llm.py`'s pre-existing `_STUB_REFUSAL` + `_CHUNK_RE`); NO new tenacity wraps (Phase 5 CD-04 + Phase 2 D-18 inherited verbatim); NO new Settings fields (FND-11 single-env-reader preserved); package count holds at 8 through Phase 13. Chain mode auto-advances to plan-phase next.

- 2026-05-15 — Workflow_dispatch run **25895662489** (fourth attempt after qdrant DNS fix `e26b352`): `real-mode index build` **failed with `PermissionError: [Errno 13] Permission denied: 'data/indices/bm25'`**. Root cause: the qdrant container's volume mount `./data/indices/.qdrant:/qdrant/storage` (compose D-05) creates `data/indices/` on the host as root before the python process can `mkdir data/indices/bm25/`. The job's python process runs as the `runner` user → permission denied. **Fix:** either (a) `mkdir -p data/indices/{bm25,dense,.qdrant}` BEFORE `docker compose --profile real up qdrant`, OR (b) add `sudo chown -R $(whoami):$(whoami) data/indices` after qdrant starts, OR (c) change the compose volume to a named docker volume. Recommend (a) — simplest, no privilege escalation. **Iteration paused at this point** — 4 infrastructure failures in a row (API key → timeout → DNS → permissions). Each fix surfaces the next layer; the canary code itself has never been exercised. Phase 4's `workflow_dispatch real-index-build` job was implemented but NEVER end-to-end run before Phase 5; this run is the discovery process. Picking up: apply fix (a), re-trigger, expect either canary PASS or substantive D-16 application.

- 2026-05-15 — Workflow_dispatch run **25894710677** (third attempt after 30-min timeout bump `03023b9`): all 4 stub-side jobs green again, but `real-mode index build` **failed at qdrant client connect** — `httpx.ConnectError: [Errno -3] Temporary failure in name resolution`. Root cause: the python process runs on the GitHub runner host, NOT inside the docker network; `Settings.qdrant_url=http://qdrant:6333` (compose service hostname) only resolves within the compose network. The workflow's healthcheck step uses `localhost:6333` (correct), but the `docintel-index` step inherited the Settings default. **Fix:** override `DOCINTEL_QDRANT_URL=http://localhost:6333` in the job env. Another Phase 4 D-05 oversight surfaced by the first end-to-end real-mode run. (Run 25894439192 the same day was cancelled by the concurrency group `ci-refs/heads/main`'s `cancel-in-progress: true` collision with the push run that bumped the timeout.) RET-03 EMPIRICAL-PENDING **remains open** pending the fourth attempt after this fix.

- 2026-05-14 — Workflow_dispatch run **25890137612** (second attempt after lazy-LLM-init fix `9ec4d36`): all 4 stub-side jobs green (gitleaks ✓, lint+types+tests ✓, docker build ✓, compose smoke ✓), but `real-mode index build` was **cancelled at the 15-minute job timeout**. Diagnosis: the job's `timeout-minutes: 15` was originally provisioned for Phase 4's index-build-only flow; Phase 5 added the canary test which downloads bge-reranker-base (~250 MB) + runs the reranker on 8 cases — pushing total wall-clock past 15 min. **Fix outstanding:** bump `.github/workflows/ci.yml::real-index-build::timeout-minutes` from 15 → 30, re-trigger `gh workflow run ci.yml --ref main`. The previous run **25889375809** failed earlier at `make_adapters` requiring `DOCINTEL_ANTHROPIC_API_KEY` (now fixed by lazy SDK init in `9ec4d36`). RET-03 EMPIRICAL-PENDING **remains open** pending a successful real-mode canary run.

- 2026-05-14 — Phase 5 merged to `main` as PR #5 (merge commit `2768961`). Workflow_dispatch real-mode canary triggered (run 25889375809) to close RET-03 EMPIRICAL-PENDING. Two empirical findings flagged for Phase 13 ADRs: (1) **stub reranker structurally incapable of beating stub dense-only** — both `StubReranker.rerank` and `NumpyDenseStore.query` reduce to cosine over the same `_text_to_vector` hash (0/307 brute-force wins). Resolution: Option D — all 8 canary cases tagged `mode="real"`; stub-mode test weakened to schema-only; strict D-14 bites under workflow_dispatch only. CONTEXT.md D-14/D-15 amendment committed. Deferred ticket: "Stub-reranker discriminative-power redesign" (Phase 11 / v2). (2) **A1 tokenizer-drift assumption REFUTED** — measured mean ratio 1.1404 (+14% disagreement vs assumed ~5%); 1794/6053 chunks (29.6%) exceed XLM-RoBERTa 500-token threshold. The `chunk_reranker_token_overflow` soft warning from Plan 05-05 is LOAD-BEARING. Post-merge CI fixes: cherry-picked `b07d305` (ruff RUF100/RUF002/RUF003/B007/I001/format) + `c020075` (move Build indices before pytest; add `-m "not real"` to main pytest step) onto main. Final test gate: 121 passed / 0 failed / 0 xfailed (stub mode); mypy --strict clean (47 files); 3 wrap gates green.

- 2026-05-14 — Phase 5 executed via `/gsd-execute-phase 5 --auto --no-transition` on branch `phase/5-retrieval-hybrid-rerank` (forked off `origin/main` at `b8858a0`). 7 plans across 4 waves. Wave 0 (Plans 05-01 + 05-02) parallel in isolated worktrees: 6 test scaffolds (xfail) + cases.jsonl placeholder + CLAUDE.md hard-gate paragraph extended with the third "the canary exists specifically to catch it" substring (Plan 05-01 Task 0 BLOCKER fix from plan-checker iter 2); new 7th workspace package `docintel-retrieve` at `packages/docintel-retrieve/` with `RetrievedChunk` Pydantic model added to `docintel_core.types` per CD-02 + atomic re-export from package `__init__.py`; `uv.lock` regenerated. Wave 1 (Plans 05-03 + 05-04) sequential due to `__init__.py` overlap: `_rrf_fuse` pure function + RRF_K=60 module constant + 5 RRF tests (tie-break, skip-missing, 1-based ranks, edge cases); `NullReranker` + `NullBM25Store` stateless ablation adapters (D-08 Phase 11 seam) + Protocol structural-satisfaction tests. Wave 2 (Plan 05-05): `Retriever` class (485 lines) with single-seam `.search(query, k) → list[RetrievedChunk]` pipeline (BM25 top-100 + Dense top-100 → RRF k=60 → top-M=20 + n_tokens≤500 assertion → cross-encoder rerank → top-K=5), `_truncate_query` (D-11 64-token cap), per-stage `retriever_search_completed` structlog (D-12 twelve fields), CD-07 zero-candidate path, CD-01 eager chunk_map load with MANIFEST cardinality check (Pitfall 7); `make_retriever(cfg)` third sibling factory in `docintel_core.adapters.factory` per D-04 with lazy import inside body; 5 retriever_search tests + 2 make_retriever tests passing. Wave 3 (Plan 05-06): structural acceptance gate finding — stub reranker can't beat stub dense-only (see above). User picked Option D at curation checkpoint; 8 hand-curated cases against AAPL/MSFT/NVDA/JPM/V/PG FY2024; dual-mode pytest driver (Pattern A — function-level `@pytest.mark.real`); CI step "Reranker canary (stub-mode acceptance gate) (RET-03)". Wave 4 (Plan 05-07): `scripts/measure_tokenizer_drift.py` (205 lines) measured A1 REFUTED; `test_reranker_canary_real_mode` xfail-strict marker preemptively removed; Decision-Coverage Audit 26/26 (D-14 + D-15 marked AMENDED). Verifier ruled PHASE EMPIRICAL-PENDING (13/13 must-haves verified; real-mode strict differential routed to human workflow_dispatch).

- 2026-05-14 — Phase 4 executed via `/gsd-execute-phase 4 --auto --no-transition` on branch `phase/4-embedding-indexing` (forked off `origin/main` at `843b1e7`). All 7 plans across 6 waves landed. Wave 1 (Plans 04-01 + 04-02) ran parallel in isolated worktrees — 11 xfail test scaffolds + Settings amendment (D-17: `index_dir`, `qdrant_url`, `qdrant_collection`) + `scripts/check_index_wraps.sh` (D-21 grep gate); Waves 2–6 sequential. Wave 2 (Plan 04-03): added `DenseStore` + `BM25Store` Protocols to `docintel_core.adapters.protocols`, `IndexManifest` + `IndexStoreBundle` types, new 6th workspace package `docintel-index` (pins `bm25s==0.3.9`, `PyStemmer==3.0.0`, `qdrant-client==1.18.0`), `uv.lock` regenerated. Wave 3 (Plan 04-04): `Bm25sStore` (Lucene tokenization + stopwords + Porter stemmer, k1=1.5 b=0.75) + `NumpyDenseStore` (np.save deterministic, argpartition top-K). Wave 4 (Plan 04-05): `QdrantDenseStore` with `DOCINTEL_CHUNK_NAMESPACE = uuid.UUID("576cc79e-7285-5efc-8e6e-b66d3e6f92ae")` UUID5 derivation (Pitfall 1 fix) + every qdrant_client.* call site tenacity-wrapped, `make_index_stores(cfg)` factory with lazy Qdrant import, `docintel-index` build/verify/manifest/cli with atomic MANIFEST write + corpus_identity_hash (strips `generated_at` per Pitfall 9). Wave 5 (Plan 04-06): Makefile `build-indices` target, `docker-compose.yml` qdrant service under `profiles: ['real']` (no `depends_on: [qdrant]` on api — UI-06 preserved per Pitfall 7), CI workflow with stub-mode build+verify+grep-gate steps + workflow_dispatch `real-index-build` job. Wave 6 (Plan 04-07): removed 9 per-test xfail markers + module-level xfail element from `test_index_build_real.py` (kept `pytest.mark.real`), cleared 8 ruff + 14 black baseline issues from Plans 04-04/04-05. **Final gate:** 96 passed, 5 skipped (real-mode), 0 xfailed, 0 xpassed, 0 failed. `uv run docintel-index verify` exits 0 (6,053 chunks, dense_backend=numpy, bm25_vocab_size=7520). `mypy --strict` clean across 43 source files. 3 CI grep gates green (check_index_wraps, check_ingest_wraps, check_adapter_wraps). `uv lock --check` passes. **Verifier 15/15 dimensions PASS** — IDX-01..IDX-04 all closed; written to `04-VERIFICATION.md`. Ready for PR + merge.

- 2026-05-14 — Phase 4 planned via `/gsd-plan-phase 4 --auto`. 7 plans landed at `.planning/phases/04-embedding-indexing/04-{01..07}-PLAN.md` across 6 waves: Wave 0 (test scaffolds + Settings/CI gate) → Wave 2 (Protocols + types + package) → Wave 3 (BM25 + NumPy stores) → Wave 4 (Qdrant + factory + build/verify/cli) → Wave 5 (Makefile + compose + CI) → Wave 6 (xfail removal + phase gate). RESEARCH.md (937 lines, 10 pitfalls) + PATTERNS.md (32 files mapped) + VALIDATION.md captured upstream. Plan checker flagged 3 BLOCKERs + 6 WARNINGs in iteration 1; all 9 resolved in iteration 2 (Open Questions marked RESOLVED in RESEARCH.md; test_index_build_real.py moved into Wave 0 with dual xfail+real markers; bm25s/PyStemmer pins kept in `docintel-index/pyproject.toml` only — no duplicate in core; Plan 04-04 `Bm25sStore.name` returns `"bm25s"` not version-stamped; etc.). Decision-coverage gate passes 21/21 after explicit `D-06:` + `D-11:` colon-citations added.

- 2026-05-14 — Phase 4 context gathered via `/gsd-discuss-phase 4 --chain`. Decisions D-01..D-21 captured in `.planning/phases/04-embedding-indexing/04-CONTEXT.md`: **tiered dense backend** (Qdrant for real / NumPy `.npy` for stub) behind a new `DenseStore` Protocol + `make_index_stores(cfg)` factory parallel to Phase 2's adapter factory; **BM25 = `bm25s`** (scipy-sparse, ~500× faster than rank_bm25) with default + English stopwords + Porter stemmer tokenization, defaults k1=1.5/b=0.75, ranks-only into Phase 5's RRF; **new 6th workspace package `docintel-index`** at `packages/docintel-index/` with CLI `build/verify/all` (mirrors `docintel-ingest`); `Settings.index_dir = "data/indices"` + `qdrant_url = "http://qdrant:6333"`; **skip-if-corpus-MANIFEST-unchanged** idempotency (hash `data/corpus/MANIFEST.json` → compare to prior index MANIFEST's `corpus_manifest_sha256`); **single `data/indices/MANIFEST.json`** carrying embedder + bm25 config + per-artifact sha256s + `corpus_manifest_sha256` + chunk_count + built_at + git_sha; **compose profile `real`** gates the new `qdrant` docker service (default `docker-compose up` stays stub-mode/NumPy, preserving UI-06 one-command-demo); **CI builds indices on every PR in stub-mode**, real-Qdrant path gated behind `workflow_dispatch` (parallel to EVAL-04). Discussion ran in interactive mode (4 BM25 questions single-shot, then user requested `--batch`); user revisited the initially-skipped vector-store decision after the three selected areas wrapped, ultimately picking the tiered backend. Chain mode auto-advances to plan-phase next.

- 2026-05-14 — Phase 3 executed via `/gsd-execute-phase 3 --chain` on branch `phase/3-corpus-ingestion` (forked off `origin/main` at `2da4a21`). All 7 plans across 6 waves landed. Wave 0 (test scaffolds + Settings/CI gate) parallel; Wave 1 paused for snapshot CSV approval (15 S&P 500 tickers × 3 FYs, snapshot_date 2026-05-13); Wave 2 paused for live SEC fetch (45 filings, 30 MB raw, ~3.6 min wall-clock); Waves 3–5 autonomous. Empirical finding: 6 tickers (AMZN/META/XOM/GOOGL/LLY/WMT) use styled-span Item headings the D-07 regex can't match — user opted to continue as-is with demo scoped to the 9 fully-covered tickers (AAPL, MSFT, NVDA, TSLA, JPM, V, PG, JNJ, HD). Wave 4 chunker produced 6,053 chunks total; one Rule-1 deviation fix added `_OVERLAP_HARD_CAP=100` after live data exposed a 500-token-overlap edge case. Wave 5 wrote `data/corpus/MANIFEST.json` (45 filings, 136 sha256 entries) + wired `make fetch-corpus` body + added CI step `bash scripts/check_ingest_wraps.sh` + chunk-idempotency gate. **Final gate:** 66 passed, 2 skipped, 0 xfailed, 0 failed. `uv run docintel-ingest verify` exits 0. `mypy --strict` clean across 35 source files. ING-01..ING-04 all closed.
- 2026-05-13 — Phase 2 merged to `main` as PR #2 (commit `2da4a21`). All Phase 2 deps (sentence-transformers, torch, transformers, anthropic, openai, tenacity) now in `docintel-core/pyproject.toml` and available to Phase 3 transitively.
- 2026-05-12 — Phase 3 planned via `/gsd-plan-phase 3 --chain`. 7 plans landed at `.planning/phases/03-corpus-ingestion/03-{01..07}-PLAN.md` across 6 waves: Wave 0 (test scaffolds + Settings amendment + CI grep gate) → Wave 1 (package + types + snapshot CSV, CHECKPOINT) → Wave 2 (fetcher + raw HTML, CHECKPOINT) → Wave 3 (normalizer) → Wave 4 (BGE tokenizer + chunker) → Wave 5 (MANIFEST + idempotency test + Makefile + CI wiring). RESEARCH.md + PATTERNS.md + VALIDATION.md captured upstream. Plan checker flagged 2 blockers + 5 warnings; blockers (ING-04 claim on Plan 03-04, cfg.data_dir path resolution in Plan 03-07) and the test-file accounting warnings were fixed inline. Decision-coverage gate now passes 22/22.
- 2026-05-12 — Phase 3 context gathered via `/gsd-discuss-phase 3 --chain`. Decisions D-01..D-22 captured in `.planning/phases/03-corpus-ingestion/03-CONTEXT.md`: Top-N S&P 500 snapshot (FY2022–FY2024 pinned IN the snapshot file so re-runs don't drift); `sec-edgar-downloader` fetcher (tenacity-wrapped); commit BOTH raw HTML and chunks under `data/corpus/` (un-ignore them); raw HTML trimmed to body-only at fetch time (drop XBRL wrappers + Ex-21/23/31/32/99) targeting ~90 MB raw footprint; `selectolax` parser + regex Item-boundary detection with ordering validation; tables dropped for v1 (V2-02 deferred); per-filing normalized JSON at `data/corpus/normalized/{ticker}/FY{year}.json`; real BGE tokenizer (`AutoTokenizer.from_pretrained("BAAI/bge-small-en-v1.5")`, already in core via Phase 2 D-11) with 450-token target / 50-token overlap / 500-token hard cap; never cross Item boundaries; paragraph-aware splitter with sentence-fallback then hard token slice for outliers; `chunk_id = {ticker}-FY{year}-{item_code}-{ordinal:03d}` (e.g., `AAPL-FY2024-Item-1A-007`); JSONL per-filing under `data/corpus/chunks/`; citation anchor = inline `text` + `char_span_in_section`; new 5th workspace package `docintel-ingest` with its own pyproject + `docintel-ingest` CLI entry. Chain mode auto-advances to plan-phase next.
- 2026-05-11 — Phase 2 context gathered via `/gsd-discuss-phase 2 --chain`. Decisions D-01..D-19 captured in `.planning/phases/02-adapters-protocols/02-CONTEXT.md`: BGE-small embedder + bge-reranker-base + dual Claude/GPT clients (new `DOCINTEL_LLM_REAL_PROVIDER` Settings field) + cross-family judge; `CompletionResponse` + `JudgeVerdict` return shapes; protocols/stubs/real adapters all live in `docintel_core.adapters` with lazy real imports and `AdapterBundle` factory return; hash-based deterministic stub embeddings + cosine stub reranker + templated stub generator + schema-check stub judge.
- 2026-05-11 — Reconstructed `.planning/` (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, config.json) on a fresh clone. `projectspec.md` is not recoverable here; reconstruction sourced from `CLAUDE.md` and the Phase 1 SUMMARYs (`.planning/phases/01-scaffold-foundation/01-01-SUMMARY.md`, `01-02-SUMMARY.md`). Tracked as Decision #1 below.
- 2026-04-28 — Phase 1 Plans 01-01 and 01-02 executed; CI workflow (Plan 10) merged via `1ceb792`. Final merge of `phase/1-scaffold-foundation` to `main` at `f40bca7`.

## Open Decisions

| # | Decision | Status |
|---|----------|--------|
| 1 | Some REQ-IDs in Phase 1 SUMMARYs (FND-03..FND-05, FND-07, FND-09..FND-11) are reconstructed from inference, not quoted from the original spec. If `projectspec.md` is later recovered, REQUIREMENTS.md should be reconciled against it. | Open — low urgency; affects only the names, not the work |
| 2 | mypy `ignore_missing_imports=true` is on for Phase 1 only (D-12). Tighten when Phase 2 lands typed protocols. | ✓ Closed by Phase 2 D-19 |
| 3 | `.gitignore` currently has `data/corpus/` wholesale ignored. Phase 3 D-04 un-ignores `data/corpus/raw/`, `data/corpus/normalized/`, `data/corpus/chunks/`, and `data/corpus/MANIFEST.json` — only `data/corpus/.cache/` stays ignored. To be applied during Phase 3 execution. | Open — addressed in Phase 3 |

## Hard Gates To Remember

- **Phase 2 must land before Phase 4.** ✓ Phase 2 complete; protocols/stubs gate eval-in-CI.
- **Phase 3 chunking discipline gates Phase 5's canary.** Real BGE tokenizer + 500-token hard cap (Phase 3 D-10/D-11) means Phase 5's silent-truncation canary tests real retrieval behavior, not tokenizer mismatch. Chunker fails the build if a chunk exceeds 500 tokens.
- **Phase 5 reranker silent-truncation canary** is the most subtle failure mode. If it trips, look at BGE 512-token truncation first — but Phase 3's hard cap should prevent that at the chunker level.
- **Phase 9 depends on both Phase 7 and Phase 8.** Phase 8 is parallelizable with 4–7.
- **Phase 13 depends on 11 + 12.** Protect its time — recruiter weight is disproportionate.
- **Reports under `data/eval/reports/`** are committed; `data/eval/cache/` is not. Don't mix them up in `.gitignore`. Same pattern now applies to corpus: `data/corpus/raw|normalized|chunks/` committed (Phase 3 D-04); `data/corpus/.cache/` is not.

## How To Resume

Phase 5 merged to `main` (PR #5, `2768961`). Real-mode canary EMPIRICAL-PENDING gate **still open** after three failed workflow_dispatch attempts (25889375809: missing API key — fixed by lazy SDK init `9ec4d36`; 25890137612: 15-min timeout — fixed by 30-min bump `03023b9`; 25894710677: qdrant DNS — fix pending commit). A fourth attempt is queued after the qdrant-DNS fix lands.

Next steps:

1. **Patch `.github/workflows/ci.yml::real-index-build`** — add a step BEFORE `Start qdrant via compose profile 'real'` that pre-creates the index directories as the runner user:
   ```yaml

   - name: Pre-create data/indices/ as runner-owned
     run: mkdir -p data/indices/bm25 data/indices/dense data/indices/.qdrant
   ```
   This ensures the docker volume mount doesn't claim the parent dir as root.

2. Commit + push. **WAIT for the push CI run to complete** (concurrency group will otherwise cancel any workflow_dispatch run — see run 25894439192).
3. **Re-trigger workflow_dispatch:** `gh workflow run ci.yml --ref main`. Wait for completion. Expected: `test_reranker_canary_real_mode PASSED` or a real D-16-actionable canary assertion failure.
   *(Run **25896508029** was already triggered with fix `93ec39a` and is in_progress as of 2026-05-15 02:12 UTC — check with `gh run view 25896508029` before re-triggering.)*

4. **If PASS** — append a "Closed Decisions" row in STATE.md citing the workflow run URL; mark Phase 5 fully complete.
3. **If FAIL with assertion error** — apply D-16 debug protocol from the canary's `_DEBUG_BLOCK`: check chunk `n_tokens < 500` FIRST (Phase 3 invariant; note A1 shows 1794/6053 chunks exceed XLM-RoBERTa 500-token threshold so `chunk_reranker_token_overflow` warnings are expected — not a regression), THEN bge-reranker-base SDK pin, THEN RRF/chunk-size. Iterate on `data/eval/canary/cases.jsonl` curation if the strict criterion holds for individual cases but not aggregate.
4. **If FAIL with infrastructure error** (timeout, DNS, env): keep iterating on the workflow YAML; the canary code is correct.
5. **Start Phase 6** — `/clear` then `/gsd-discuss-phase 6 --chain` (generation: `src/docintel/generation/prompts.py` versioned prompts; LLMClient adapter; refusal path; GEN-01..04). This phase opens the LLM call path so `DOCINTEL_ANTHROPIC_API_KEY` / `DOCINTEL_OPENAI_API_KEY` will be needed for real-mode eval — the lazy SDK init from `9ec4d36` defers the requirement to `.complete()` first-call.

---
*Last updated: 2026-05-15 after workflow_dispatch run 25894710677 failed at qdrant DNS resolution (RET-03 EMPIRICAL-PENDING remains open; qdrant-DNS fix queued).*
