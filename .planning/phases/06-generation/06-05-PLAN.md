---
phase: 06-generation
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - packages/docintel-core/src/docintel_core/adapters/stub/llm.py
autonomous: true
requirements: [GEN-03, GEN-04]
must_haves:
  truths:
    - "`packages/docintel-core/src/docintel_core/adapters/stub/llm.py` imports `REFUSAL_TEXT_SENTINEL` from `docintel_core.types` AND `_CHUNK_RE` from `docintel_generate.parse` — both names are re-exported via aliasing so existing tests + Phase 2 D-16 contract are preserved."
    - "`_STUB_REFUSAL` constant value byte-equals `REFUSAL_TEXT_SENTINEL` (the new D-11 canonical sentinel `\"I cannot answer this question from the retrieved 10-K excerpts.\"`) — Phase 2's placeholder value `\"[STUB REFUSAL] No evidence found in retrieved context.\"` is fully retired."
    - "`_CHUNK_RE` is no longer DEFINED in `adapters/stub/llm.py` — only re-imported (`from docintel_generate.parse import _CHUNK_RE`). The single canonical regex now lives in `docintel_generate.parse._CHUNK_RE` per D-12."
    - "`StubLLMClient.complete(...)` behavior is unchanged at the seam — same protocol, same return shape, same template logic — but the refusal-path text now emits the new canonical sentinel."
    - "`bash scripts/check_prompt_locality.sh` exits 0 — `adapters/stub/llm.py` is allowlisted (D-05); the import of REFUSAL_TEXT_SENTINEL + assignment to `_STUB_REFUSAL` does NOT trip the gate."
    - "`bash scripts/check_adapter_wraps.sh` exits 0 — the existing `from tenacity import` marker in `adapters/real/judge.py` is untouched by this plan (this plan only touches `adapters/stub/llm.py`)."
    - "`uv run pytest tests/test_adapters.py -x` PASSES — the existing Phase 2 stub tests still pass with the new sentinel value (since they assert against `_STUB_REFUSAL` symbolically, not against the byte-literal old value)."
  artifacts:
    - path: "packages/docintel-core/src/docintel_core/adapters/stub/llm.py"
      provides: "Updated stub LLM adapter — _STUB_REFUSAL re-aliases REFUSAL_TEXT_SENTINEL from docintel_core.types (Pitfall 9 / Pitfall 5); _CHUNK_RE re-imported from docintel_generate.parse (D-12); module docstring + constant docstrings updated to note the Phase 6 promotion"
  key_links:
    - from: "adapters/stub/llm.py._STUB_REFUSAL"
      to: "docintel_core.types.REFUSAL_TEXT_SENTINEL"
      via: "from docintel_core.types import REFUSAL_TEXT_SENTINEL; _STUB_REFUSAL: Final[str] = REFUSAL_TEXT_SENTINEL"
      pattern: "_STUB_REFUSAL.*=.*REFUSAL_TEXT_SENTINEL"
    - from: "adapters/stub/llm.py._CHUNK_RE"
      to: "docintel_generate.parse._CHUNK_RE"
      via: "from docintel_generate.parse import _CHUNK_RE"
      pattern: "from docintel_generate\\.parse import _CHUNK_RE"
---

<objective>
Land the stub-LLM adapter update: (1) update `_STUB_REFUSAL` constant VALUE from the Phase 2 placeholder `"[STUB REFUSAL] No evidence found in retrieved context."` to the new D-11 canonical sentinel `"I cannot answer this question from the retrieved 10-K excerpts."` (re-aliased from `REFUSAL_TEXT_SENTINEL` in `docintel_core.types`), (2) move `_CHUNK_RE` from in-file definition to re-import from `docintel_generate.parse._CHUNK_RE` (D-12 single-canonical-regex direction).

Three structural facts get locked:
1. The dual-mode determinism contract holds: stub-mode pytest + Phase 9 faithfulness eval will see the SAME refusal text in both modes (stub `_STUB_REFUSAL` byte-equals real `REFUSAL_PROMPT` byte-equals `REFUSAL_TEXT_SENTINEL`). Pitfall 5 mitigation: no value drift between Phase 2 placeholder and Phase 6 source-of-truth.
2. The single-canonical-regex contract holds: `_CHUNK_RE` lives in `docintel_generate.parse`; both the stub adapter (this plan) and the Generator (Plan 06-04) and Phase 7's Citation parser (future) import the SAME `re.Pattern` instance. Single source of truth across stub + real + Phase 7.
3. The import-direction-cycle Pitfall 9 is fully resolved: `_STUB_REFUSAL` imports from `docintel_core.types` (in-package upward dep is fine); `_CHUNK_RE` imports from `docintel_generate.parse` (cross-package downward dep that ONLY fires when `make_adapters` returns a stub — the cycle is one-way at runtime per the original CONTEXT.md D-12 reasoning, and the planner-confirmed alternative move of REFUSAL_TEXT_SENTINEL to core eliminates the inverse half of the cycle).

Output: 1 modified file — `adapters/stub/llm.py`. Plan 06-01's `tests/test_generator_refusal.py::test_hard_zero_chunk_refusal` test body now passes for two distinct paths: the Generator's hard-refusal path uses the new sentinel from core; the stub adapter's `_STUB_REFUSAL` returns the same string when `_CHUNK_RE.findall(prompt)` is empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adapters-protocols/02-CONTEXT.md
@.planning/phases/06-generation/06-CONTEXT.md
@.planning/phases/06-generation/06-RESEARCH.md
@.planning/phases/06-generation/06-PATTERNS.md
@.planning/phases/06-generation/06-VALIDATION.md
@.planning/phases/06-generation/06-03-SUMMARY.md
@.planning/phases/06-generation/06-04-SUMMARY.md

<interfaces>
<!-- Current state of adapters/stub/llm.py (the file to modify) -->

From `packages/docintel-core/src/docintel_core/adapters/stub/llm.py` (current 97 lines):

Lines 1-14 — module docstring describing the deterministic-template + chunk-id-citation behavior + the note that the regex is locked for Phase 6 forward compatibility.

Lines 15-21 — imports:
- `from __future__ import annotations`
- `import re` (used ONLY for `_CHUNK_RE` definition; this import is REMOVED in Plan 06-05 once `_CHUNK_RE` is re-imported from `docintel_generate.parse`)
- `from typing import Final`
- `from docintel_core.adapters.types import CompletionResponse, TokenUsage`

Lines 23-29 — the existing `_STUB_REFUSAL` constant:
- Current value: `"[STUB REFUSAL] No evidence found in retrieved context."`
- Docstring: explicitly notes "Phase 6 may replace this constant when formalising the prompt schema and refusal contract."
- **Plan 06-05 IS that replacement.**

Lines 31-37 — the existing `_CHUNK_RE` constant:
- Pattern: `re.compile(r"\[([^\]]+)\]")`
- Docstring: "Pattern is locked here for forward compatibility: Phase 6 introduces the formal prompt schema; Phase 9 faithfulness tests rely on the same regex producing the same citation extractions in stub and real modes."
- Plan 06-03 ADDED `_CHUNK_RE` to `docintel_generate.parse` (currently TWO byte-identical definitions exist between Wave 1 and Wave 3). **Plan 06-05 retires the in-file definition.**

Lines 39-49 — `StubLLMClient` class definition + `.name` property — UNCHANGED by this plan.

Lines 56-96 — `.complete(prompt, system) -> CompletionResponse` method:
- Line 77: `chunk_ids = _CHUNK_RE.findall(prompt)` — uses the in-file `_CHUNK_RE` reference. AFTER Plan 06-05, the same name resolves to the imported `_CHUNK_RE` from `docintel_generate.parse` — Python's name resolution rules make this transparent (the symbol `_CHUNK_RE` is module-scope; the import binds the same name; existing line 77 needs no change).
- Lines 78-84: refusal branch (`if not chunk_ids: text = _STUB_REFUSAL`) + template branch (`else: text = f"Based on the provided context: {prompt[:200]}... [STUB ANSWER citing {chunk_ids}]"`). UNCHANGED.

<!-- Current state of REFUSAL_TEXT_SENTINEL home (Plan 06-02 ships) -->

From `packages/docintel-core/src/docintel_core/types.py` (post-Plan 06-02):
- `REFUSAL_TEXT_SENTINEL: Final[str] = "I cannot answer this question from the retrieved 10-K excerpts."`
- Exported in `__all__`.

The byte-string is exactly 64 characters (verify with `len("I cannot answer this question from the retrieved 10-K excerpts.")` = 64). No leading/trailing whitespace, no surrounding punctuation drift.

<!-- Current state of _CHUNK_RE home (Plan 06-03 ships) -->

From `packages/docintel-generate/src/docintel_generate/parse.py` (post-Plan 06-03):
- `_CHUNK_RE: Final[re.Pattern[str]] = re.compile(r"\[([^\]]+)\]")` with the canonical docstring.
- `is_refusal(text: str) -> bool` helper exists (not used by stub adapter — the stub uses startswith inline in tests; but available if a future stub test wants the helper).

<!-- D-12 spec — verbatim from CONTEXT.md lines 106-109 -->

D-12 spec: "`packages/docintel-core/src/docintel_core/adapters/stub/llm.py` is amended:
- `_STUB_REFUSAL` constant value changes to match `REFUSAL_PROMPT_SENTINEL` from `docintel_generate.prompts` (single source of truth). The `_STUB_REFUSAL` name is retained for backward-compatibility of any tests already importing it; the value tracks `prompts.py`. **One stub import** is allowed to cross into `docintel-generate` (stub → generate is upward-stack, acceptable; the generator never imports the stub).
- The `[STUB ANSWER citing ...]` template continues to emit `[chunk_id]` markers verbatim — no change needed.
- The chunk_id regex `_CHUNK_RE` is moved to `docintel_generate.parse._CHUNK_RE` and re-imported by the stub for consistency."

**Pitfall 9 amendment:** the planner adopted the alternative — `REFUSAL_TEXT_SENTINEL` lives in `docintel_core.types` (Plan 06-02), NOT in `docintel_generate.prompts`. The stub adapter imports from CORE, not from generate — eliminates the upward-stack cycle for the sentinel. The `_CHUNK_RE` import direction (stub → generate) is the only remaining cross-package import; D-12's note "One stub import is allowed to cross into docintel-generate" applies to this single line.

<!-- Existing stub tests (`tests/test_adapters.py`) that exercise the stub LLM -->

From `tests/test_adapters.py` (relevant test names — searched via grep for `stub` + `_STUB_REFUSAL`):
- Stub LLMClient determinism tests at the file's stub-LLM section assert: same input → same output.
- Stub LLMClient refusal test asserts: empty-prompt input → returns `_STUB_REFUSAL` text.

These tests import `_STUB_REFUSAL` BY NAME (not by value) — so as long as the name still resolves and the byte-string still matches what the test expects, they pass. Plan 06-05 changes the VALUE; if any test asserts `_STUB_REFUSAL == "[STUB REFUSAL] No evidence found in retrieved context."` (byte-literal), that assertion now fails. Plan 06-05 MUST scan tests/ for any byte-literal assertions against the OLD sentinel value and amend them; the canonical pattern uses the name `_STUB_REFUSAL` symbolically, so this should be a no-op grep verification.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Update _STUB_REFUSAL value via REFUSAL_TEXT_SENTINEL import + retire in-file _CHUNK_RE</name>
  <files>packages/docintel-core/src/docintel_core/adapters/stub/llm.py</files>
  <read_first>
    - packages/docintel-core/src/docintel_core/adapters/stub/llm.py (current full file — for the line-by-line edit plan)
    - packages/docintel-core/src/docintel_core/types.py (post-Plan 06-02 — verify REFUSAL_TEXT_SENTINEL is importable; verify the byte-value)
    - packages/docintel-generate/src/docintel_generate/parse.py (post-Plan 06-03 — verify _CHUNK_RE is importable; verify the pattern is byte-identical to the in-stub definition being retired)
    - .planning/phases/06-generation/06-CONTEXT.md D-11 + D-12 (the contracts)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-core/src/docintel_core/adapters/stub/llm.py`" lines 637-682 (verbatim deviation notes; line-by-line edit map)
    - .planning/phases/06-generation/06-RESEARCH.md §Common Pitfalls §Pitfall 5 lines 778-788 (stub _STUB_REFUSAL value drift defense) + §Pitfall 9 lines 826-838 (cycle resolution — sentinel home in core)
    - tests/test_adapters.py (existing stub tests — for verifying name-based assertions don't break with the value change; search for `_STUB_REFUSAL` references in the file)
  </read_first>
  <action>
    Edit `packages/docintel-core/src/docintel_core/adapters/stub/llm.py` (single file, three small edits):

    **Edit 1 — Update module docstring (lines 1-14):**

    The current docstring contains "The chunk_id regex is locked to the bracket-pattern for forward compatibility with Phase 6 prompt schema formalisation (RESEARCH.md §Stub Determinism)." Update to reference Phase 6 NOW IN PLACE:

    Old line (line 8-9 region): `The chunk_id regex is locked to the bracket-pattern for forward compatibility with Phase 6 prompt schema formalisation (RESEARCH.md §Stub Determinism).`

    New line: `The chunk_id regex and refusal sentinel are imported from the canonical Phase 6 homes (docintel_generate.parse._CHUNK_RE and docintel_core.types.REFUSAL_TEXT_SENTINEL respectively); this module re-aliases them as `_CHUNK_RE` and `_STUB_REFUSAL` for backward-compat with existing tests + Phase 2 D-16 contract.`

    **Edit 2 — Update imports (lines 15-21):**

    Replace the import block. Old (current):
    ```
    from __future__ import annotations

    import re
    from typing import Final

    from docintel_core.adapters.types import CompletionResponse, TokenUsage
    ```

    New:
    ```
    from __future__ import annotations

    from typing import Final

    from docintel_core.adapters.types import CompletionResponse, TokenUsage
    from docintel_core.types import REFUSAL_TEXT_SENTINEL

    # D-12: single canonical _CHUNK_RE home is docintel_generate.parse. The Pitfall 9
    # cycle concern (stub-in-core importing from generate-package) is acknowledged:
    # this is the ONE stub import allowed to cross packages (CONTEXT.md D-12 line 107).
    # The cycle is one-way at runtime — make_adapters() returns the stub only when
    # llm_provider="stub", at which point docintel-generate is already loaded.
    from docintel_generate.parse import _CHUNK_RE
    ```

    The `import re` is REMOVED — no longer needed in this file (the regex definition moves to parse.py; the import is now name-only via the from-import).

    **Edit 3 — Update _STUB_REFUSAL value + retire in-file _CHUNK_RE (lines 23-37):**

    Replace the constant block. Old (current lines 23-37):
    ```
    _STUB_REFUSAL: Final[str] = "[STUB REFUSAL] No evidence found in retrieved context."
    """Canonical refusal string for the no-chunk path (GEN-04).

    Phase 6 may replace this constant when formalising the prompt schema and
    refusal contract. Phase 2 locks this text so Phase 9 stub-mode faithfulness
    tests have a stable, predictable refusal sentinel.
    """

    _CHUNK_RE: Final[re.Pattern[str]] = re.compile(r"\[([^\]]+)\]")
    """Module-level compiled regex for extracting [chunk_id] tokens.

    Pattern is locked here for forward compatibility: Phase 6 introduces the
    formal prompt schema; Phase 9 faithfulness tests rely on the same regex
    producing the same citation extractions in stub and real modes.
    """
    ```

    New (a single constant block; `_CHUNK_RE` no longer DEFINED here — it's imported above):
    ```
    _STUB_REFUSAL: Final[str] = REFUSAL_TEXT_SENTINEL
    """Backward-compat alias for the canonical refusal sentinel.

    Phase 6 D-11 + D-12 promotion: this constant now mirrors `docintel_core.types.REFUSAL_TEXT_SENTINEL`
    verbatim — the same 64-char string the Generator emits on hard refusal (Step B of D-15).
    Phase 2's placeholder value `[STUB REFUSAL] No evidence found in retrieved context.` is retired;
    stub-mode pytest + Phase 9 faithfulness tests now see the SAME byte-string in both stub and real
    modes (RESEARCH Pitfall 5 mitigation).

    The `_STUB_REFUSAL` name is retained for backward-compatibility of any existing test imports
    (tests assert against the symbol, not the byte-literal old value). New code should import
    `REFUSAL_TEXT_SENTINEL` from `docintel_core.types` directly.
    """
    ```

    The `_CHUNK_RE` constant block is GONE — replaced by the `from docintel_generate.parse import _CHUNK_RE` line in the imports block. Line 77 of the existing file (`chunk_ids = _CHUNK_RE.findall(prompt)`) still works without change — Python resolves the `_CHUNK_RE` name from the module-scope import.

    **Edit 4 — Verify `.complete()` body (lines 56-96) is UNCHANGED:**

    The `StubLLMClient.complete` method's body should NOT be modified by this plan. The template at lines 81-84 (`f"Based on the provided context: {prompt[:200]}... [STUB ANSWER citing {chunk_ids}]"`) continues to emit `[chunk_id]` markers verbatim per D-12 "no change needed". The refusal branch at line 79 (`text = _STUB_REFUSAL`) now resolves to the new byte-string.

    Anti-patterns to avoid (per PATTERNS §"### stub/llm.py"):
    - Do NOT delete the `_STUB_REFUSAL` name — existing tests in `tests/test_adapters.py` may import it. Deleting the name (rather than re-binding the value) breaks those tests.
    - Do NOT reformat the `_STUB_REFUSAL` string body during the update — Pitfall 3 (whitespace drift) applies even though `_STUB_REFUSAL` itself is not hashed; Phase 9 faithfulness tests `assert resp.text.startswith(_STUB_REFUSAL)` are byte-exact.
    - Do NOT introduce a third sentinel string (e.g., for a "soft refusal" path). Single canonical sentinel across stub + real + zero-chunk + LLM-driven paths is the D-11 invariant.
    - Do NOT add `REFUSAL_TEXT_SENTINEL` to a local re-export — the constant is consumed via `_STUB_REFUSAL` (the existing alias) or via direct import `from docintel_core.types import REFUSAL_TEXT_SENTINEL` (new callers).
    - Do NOT remove `import re` if the file still uses `re.` anywhere else — verify via `grep "^import re\|^from re " packages/docintel-core/src/docintel_core/adapters/stub/llm.py` and `grep -c "re\." packages/docintel-core/src/docintel_core/adapters/stub/llm.py` after the edit. The verify step confirms `re` is no longer needed; the only `re`-derived value in the file was `re.compile(...)` for `_CHUNK_RE`, which is now imported.
    - Do NOT change `from docintel_core.adapters.types import CompletionResponse, TokenUsage` — the protocol types are unchanged.
  </action>
  <verify>
    <automated>uv run python -c "from docintel_core.adapters.stub.llm import _STUB_REFUSAL, _CHUNK_RE; from docintel_core.types import REFUSAL_TEXT_SENTINEL; from docintel_generate.parse import _CHUNK_RE as canonical_chunk_re; assert _STUB_REFUSAL == REFUSAL_TEXT_SENTINEL, f'{_STUB_REFUSAL!r} != {REFUSAL_TEXT_SENTINEL!r}'; assert _STUB_REFUSAL == 'I cannot answer this question from the retrieved 10-K excerpts.', repr(_STUB_REFUSAL); assert _CHUNK_RE is canonical_chunk_re, 'stub _CHUNK_RE is not the docintel_generate.parse instance'" && uv run python -c "from docintel_core.adapters.stub.llm import StubLLMClient; c = StubLLMClient(); r1 = c.complete('hello world'); from docintel_core.adapters.stub.llm import _STUB_REFUSAL; assert r1.text == _STUB_REFUSAL, f'refusal-path text mismatch: {r1.text!r}'; r2 = c.complete('Apple revenue [AAPL-FY2024-Item-1A-007] grew.'); assert '[STUB ANSWER citing' in r2.text and 'AAPL-FY2024-Item-1A-007' in r2.text" && ! grep -E "^import re\$" packages/docintel-core/src/docintel_core/adapters/stub/llm.py && uv run pytest tests/test_adapters.py -ra -q -m "not real" 2>&1 | tail -3 && uv run mypy --strict packages/docintel-core/src/docintel_core/adapters/stub/llm.py 2>&1 | grep -E "error:" | head -3 ; true && bash scripts/check_prompt_locality.sh && bash scripts/check_adapter_wraps.sh && uv run pytest tests/test_generator_refusal.py tests/test_generator_stub_determinism.py -ra -q -m "not real" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^_STUB_REFUSAL: Final\[str\] = REFUSAL_TEXT_SENTINEL" packages/docintel-core/src/docintel_core/adapters/stub/llm.py` returns 1 (the new alias is in place).
    - Source assertion: `grep -c "REFUSAL_TEXT_SENTINEL" packages/docintel-core/src/docintel_core/adapters/stub/llm.py` returns at least 2 (one import + one assignment).
    - Source assertion: `grep -c "from docintel_generate.parse import _CHUNK_RE" packages/docintel-core/src/docintel_core/adapters/stub/llm.py` returns 1 (the cross-package import per D-12).
    - Source assertion: the OLD byte-literal value is fully removed — `! grep -q "\[STUB REFUSAL\] No evidence found" packages/docintel-core/src/docintel_core/adapters/stub/llm.py`.
    - Source assertion: `_CHUNK_RE` is NO LONGER defined in `stub/llm.py` — `! grep -q "^_CHUNK_RE: Final\[re\.Pattern\[str\]\]" packages/docintel-core/src/docintel_core/adapters/stub/llm.py`.
    - Source assertion: `import re` is removed (stripping happens cleanly because there are no other re-derived usages in this file post-edit) — `! grep -E "^import re\$" packages/docintel-core/src/docintel_core/adapters/stub/llm.py`.
    - Behavior assertion: `_STUB_REFUSAL == REFUSAL_TEXT_SENTINEL == "I cannot answer this question from the retrieved 10-K excerpts."` (verified by the python -c above).
    - Behavior assertion: stub `_CHUNK_RE` IS the SAME re.Pattern instance as `docintel_generate.parse._CHUNK_RE` (verified by `_CHUNK_RE is canonical_chunk_re`; this is stronger than `_CHUNK_RE == canonical_chunk_re` and proves the single-source).
    - Behavior assertion: `StubLLMClient.complete("hello world")` returns `_STUB_REFUSAL` text on the refusal path (no `[` markers in the prompt → empty findall → refusal branch).
    - Behavior assertion: `StubLLMClient.complete("Apple revenue [AAPL-FY2024-Item-1A-007] grew.")` returns the `[STUB ANSWER citing ...]` template (the chunk-marker present → template branch, behavior unchanged).
    - Behavior assertion: `uv run pytest tests/test_adapters.py -ra -q -m "not real"` exits 0 — no existing Phase 2 stub test breaks (they assert symbolically, not against the old byte-literal).
    - Behavior assertion: `uv run pytest tests/test_generator_refusal.py tests/test_generator_stub_determinism.py -ra -q -m "not real"` exits 0 — the Plan 06-01 stub-mode tests for Generator behavior now exercise the unified sentinel.
    - Behavior assertion: `uv run mypy --strict packages/docintel-core/src/docintel_core/adapters/stub/llm.py` reports 0 errors.
    - Behavior assertion: `bash scripts/check_prompt_locality.sh` exits 0 (the stub adapter is in the allowlist per D-05; the new REFUSAL_TEXT_SENTINEL import is a short string identifier, not a prompt body).
    - Behavior assertion: `bash scripts/check_adapter_wraps.sh` exits 0 (Phase 2 wrap gate stays green — no new SDK call sites added in this plan; this plan does not touch `adapters/real/`).
  </acceptance_criteria>
  <done>The stub LLM adapter now mirrors the canonical Phase 6 sentinel + regex. `_STUB_REFUSAL` byte-equals `REFUSAL_TEXT_SENTINEL`. `_CHUNK_RE` IS the same `re.Pattern` instance as `docintel_generate.parse._CHUNK_RE` — single source of truth across stub + real + Phase 7. No new tenacity wraps; no new env reads; no inline prompts. Existing Phase 2 tests pass; new Plan 06-01 tests exercise the unified behavior. Plan 06-06 owns the judge migration; Plan 06-07 owns CI wiring + xfail-removal sweep.</done>
</task>

</tasks>

<verification>
After Task A completes:

1. `uv run python -c "from docintel_core.adapters.stub.llm import _STUB_REFUSAL, _CHUNK_RE; from docintel_generate.prompts import REFUSAL_PROMPT; from docintel_generate.parse import _CHUNK_RE as canonical; assert _STUB_REFUSAL == REFUSAL_PROMPT; assert _CHUNK_RE is canonical"` exits 0 — the three sentinel-related symbols (stub `_STUB_REFUSAL`, generate `REFUSAL_PROMPT`, generator-imported `REFUSAL_TEXT_SENTINEL`) ALL byte-equal each other; the regex is the SAME instance across the stub + the new home.
2. `uv run pytest -ra -q -m "not real"` exits 0 (full stub-mode suite green; no regressions).
3. `bash scripts/check_prompt_locality.sh` exits 0; `bash scripts/check_adapter_wraps.sh` exits 0; `bash scripts/check_index_wraps.sh` exits 0; `bash scripts/check_ingest_wraps.sh` exits 0 — all four CI grep gates green.
4. `uv run mypy --strict packages/docintel-core/` exits 0 (full strict-mode clean).
</verification>

<success_criteria>
Wave 3 stub-adapter update lands:
- `_STUB_REFUSAL` re-aliases REFUSAL_TEXT_SENTINEL from `docintel_core.types`; Phase 2 placeholder value retired
- `_CHUNK_RE` re-imports from `docintel_generate.parse`; in-file definition retired; single-canonical-regex contract held across stub + real + Phase 7
- `import re` removed from `adapters/stub/llm.py` (no longer needed)
- `StubLLMClient.complete()` behavior unchanged at the seam; refusal text now matches the new canonical sentinel
- All four CI grep gates remain green; mypy --strict clean
- Plan 06-01's stub-mode test bodies for Generator refusal + determinism + stub citation extraction all exercise the unified sentinel
- Pitfall 5 mitigation fully observed (no value drift between stub and real refusal text)
- Pitfall 9 fully resolved (REFUSAL_TEXT_SENTINEL home in core; _CHUNK_RE home in generate.parse; ONE cross-package import allowed per CONTEXT.md D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/06-generation/06-05-SUMMARY.md` documenting:
- Confirmation that the OLD `[STUB REFUSAL] ...` byte-string is fully eliminated from the codebase (`git grep "\[STUB REFUSAL\]" packages/` returns no matches)
- Confirmation that `_STUB_REFUSAL is REFUSAL_TEXT_SENTINEL` is identity-equal (Python interns short strings — but the assertion uses `==` for the byte-value comparison, not `is`)
- Confirmation that `_CHUNK_RE is docintel_generate.parse._CHUNK_RE` is identity-equal (the import binds the SAME Pattern instance; the re-export tests this directly)
- The pre/post line count of `adapters/stub/llm.py` (informational; expect ~10 lines shorter due to inlined doc + removed `_CHUNK_RE` block)
- Any changes to `tests/test_adapters.py` if a byte-literal assertion was found (expected: none; the tests use symbolic references)
</output>
