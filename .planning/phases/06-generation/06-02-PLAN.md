---
phase: 06-generation
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/docintel-generate/pyproject.toml
  - packages/docintel-generate/src/docintel_generate/__init__.py
  - packages/docintel-generate/src/docintel_generate/py.typed
  - packages/docintel-core/src/docintel_core/types.py
  - scripts/check_prompt_locality.sh
  - uv.lock
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/config.json
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "A new 8th workspace package `docintel-generate` exists at `packages/docintel-generate/` with a `pyproject.toml` pinning workspace deps `docintel-core` + `docintel-retrieve`, an empty `__init__.py` skeleton (no re-exports yet — Plans 06-03/06-04 add them), a PEP 561 `py.typed` marker, and `uv.lock` is regenerated so `uv lock --check` exits 0."
    - "`REFUSAL_TEXT_SENTINEL: Final[str] = \"I cannot answer this question from the retrieved 10-K excerpts.\"` is exported from `docintel_core.types` (Pitfall 9 resolution per RESEARCH Open Question 1 — single canonical sentinel home in core; both `docintel_generate.prompts` (Plan 06-03) and `adapters/stub/llm.py` (Plan 06-05) will import from here)."
    - "`scripts/check_prompt_locality.sh` exists, is executable, and implements D-04 + D-05: scans `packages/` for `NAME_PATTERN` and `PHRASE_PATTERN` violations, supports per-line `# noqa: prompt-locality` escape, exits 1 on planted violations from `tests/fixtures/prompt_locality_violations/`, exits 0 on `tests/fixtures/prompt_locality_violations_with_noqa/`, AND exits 0 when scanning the current canonical layout (empty `prompts.py` not yet created — script tolerates this)."
    - "All five doc-path references are updated per D-02: `git grep \"src/docintel/generation/prompts.py\"` returns zero matches across CLAUDE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/config.json (excluding the changelog history in STATE.md which records the move)."
    - "`uv run pytest tests/test_prompt_locality.py::test_grep_gate_fails_on_violation tests/test_prompt_locality.py::test_grep_gate_respects_noqa -x` PASSES (no longer xfailed — Plan 06-01 Task A wrote these to xfail-strict; this plan flips them by shipping the bash script)."
  artifacts:
    - path: "packages/docintel-generate/pyproject.toml"
      provides: "Workspace package manifest pinning docintel-core + docintel-retrieve; hatchling build backend; name='docintel-generate'; version=0.1.0"
    - path: "packages/docintel-generate/src/docintel_generate/__init__.py"
      provides: "Wave 0 empty re-export surface — module docstring naming the canonical home (`prompts.py`, `parse.py`, `generator.py`) and the Wave-incremental build-up (Plans 06-03/06-04 add `__all__` entries)"
    - path: "packages/docintel-generate/src/docintel_generate/py.typed"
      provides: "PEP 561 marker (empty file) so mypy --strict treats docintel-generate as typed"
    - path: "packages/docintel-core/src/docintel_core/types.py"
      provides: "REFUSAL_TEXT_SENTINEL exported (Pitfall 9 resolution); added to `__all__` alphabetically; placed near RetrievedChunk"
    - path: "scripts/check_prompt_locality.sh"
      provides: "CI grep gate per D-04 + D-05 — scans for NAME_PATTERN + PHRASE_PATTERN violations; allowlist + noqa escape; exits 1 on offender; mirrors check_adapter_wraps.sh / check_index_wraps.sh / check_ingest_wraps.sh shape"
    - path: "uv.lock"
      provides: "Regenerated lockfile including docintel-generate workspace dep"
    - path: "CLAUDE.md"
      provides: "Operating-rules path reference updated to `packages/docintel-generate/src/docintel_generate/prompts.py`"
    - path: ".planning/PROJECT.md"
      provides: "Two path references updated (Phase 6 phase line + Constraints 'Prompts are versioned' line)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "GEN-01 success-criterion path updated"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 'Provides:' line updated"
    - path: ".planning/config.json"
      provides: "constraints.prompt_home key value updated"
  key_links:
    - from: "packages/docintel-generate/pyproject.toml"
      to: "docintel-core (workspace) + docintel-retrieve (workspace)"
      via: "dependencies = [\"docintel-core\", \"docintel-retrieve\"]"
      pattern: "dependencies.*=.*\\[.*docintel-core.*docintel-retrieve"
    - from: "scripts/check_prompt_locality.sh"
      to: "tests/fixtures/prompt_locality_violations/ + tests/fixtures/prompt_locality_violations_with_noqa/"
      via: "subprocess.run from tests/test_prompt_locality.py + ci.yml step (Plan 06-07)"
      pattern: "check_prompt_locality\\.sh"
    - from: "REFUSAL_TEXT_SENTINEL in docintel_core.types"
      to: "Plan 06-03 prompts.py (REFUSAL_PROMPT body) + Plan 06-05 stub/llm.py (_STUB_REFUSAL value) + Plan 06-04 generator.py (Step B hard refusal)"
      via: "from docintel_core.types import REFUSAL_TEXT_SENTINEL"
      pattern: "REFUSAL_TEXT_SENTINEL"
---

<objective>
Land the new 8th workspace package skeleton (`docintel-generate`), the CI grep gate script (`scripts/check_prompt_locality.sh`), the Pitfall-9-resolution constant `REFUSAL_TEXT_SENTINEL` in `docintel_core.types`, and the five D-02 doc-path updates — all in a single coordinated Wave 0 commit. This plan plus Plan 06-01 are the two parallel Wave 0 plans; their `files_modified` lists are disjoint by construction (06-01 = `tests/**`, 06-02 = `packages/docintel-generate/`, `packages/docintel-core/src/docintel_core/types.py`, `scripts/`, `uv.lock`, `CLAUDE.md`, `.planning/*`).

Purpose: This plan establishes the "package + gate + sentinel + docs" foundation Wave 1 needs. Three structural facts get locked in:
1. The package exists with a working `pyproject.toml` + `uv.lock` (Wave 1's `prompts.py` lands inside it).
2. The grep gate is installed and CI-runnable (Plan 06-07 wires the CI step; this plan ships the script + makes it pass on the canonical layout + fail on planted violations).
3. `REFUSAL_TEXT_SENTINEL` lives in core (single source of truth across stub + generate + Phase 7 — Pitfall 9 cycle-avoidance per RESEARCH Open Question 1).
4. All five doc files point at the new path (no muscle-memory drift during execution of Waves 1-4).

Output: 11 modified/new files. The package skeleton is intentionally minimal — empty `__init__.py`, no `prompts.py` yet, no `generator.py` yet. Wave 1 (Plan 06-03) lands `prompts.py` + `parse.py`. Wave 2 (Plan 06-04) lands `generator.py` + extends `__init__.py`. Wave 3 (Plan 06-05 + 06-06) lands stub-llm + judge migration. Wave 4 (Plan 06-07) wires CI + audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generation/06-CONTEXT.md
@.planning/phases/06-generation/06-RESEARCH.md
@.planning/phases/06-generation/06-PATTERNS.md
@.planning/phases/06-generation/06-VALIDATION.md

<interfaces>
<!-- Existing analog: docintel-retrieve workspace package shape -->

From `packages/docintel-retrieve/pyproject.toml` (Phase 5 analog; full file, ~17 lines):
- `[project]` block: `name = "docintel-retrieve"`, `version = "0.1.0"`, `description = "..."`, `requires-python = ">=3.11,<3.13"`, `dependencies = ["docintel-core", "numpy==2.4.4"]`.
- `[build-system]`: `requires = ["hatchling"]`, `build-backend = "hatchling.build"`.
- `[tool.hatch.build.targets.wheel]`: `packages = ["src/docintel_retrieve"]`.
- No `[project.scripts]` block (library-only, no CLI).

From `packages/docintel-retrieve/src/docintel_retrieve/__init__.py` (Wave 0 → Wave 2 incremental build-up):
- Module docstring names the package's role + phases that will add re-exports.
- `from docintel_core.types import RetrievedChunk` (Phase 5 CD-02 — schema in core, re-exported as convenience).
- `from docintel_retrieve.fuse import RRF_K, _rrf_fuse`, `from docintel_retrieve.null_adapters import NullBM25Store, NullReranker`, `from docintel_retrieve.retriever import Retriever`.
- `__all__` alphabetical list.

From `packages/docintel-retrieve/src/docintel_retrieve/py.typed` — zero-byte file (PEP 561 marker).

From `pyproject.toml` (workspace root):
- `[tool.uv.workspace] members = ["packages/*"]` — the `packages/*` glob auto-picks up `docintel-generate/` when its `pyproject.toml` lands.
- `[dependency-groups].dev` carries pytest + ruff + black + mypy pins (no change).

<!-- Existing analog: REFUSAL_TEXT_SENTINEL placement -->

From `packages/docintel-core/src/docintel_core/types.py` (the home for Pitfall 9's `REFUSAL_TEXT_SENTINEL` constant):
- `from __future__ import annotations` at top.
- `__all__` list at lines 42-52 — `REFUSAL_TEXT_SENTINEL` is inserted alphabetically (between `NormalizedFilingManifest` and `RetrievedChunk`).
- Existing similar pattern: there is no pre-existing top-level `Final[str]` constant in this file — every export is a `BaseModel` subclass. The new constant breaks the homogeneity slightly; this is intentional per Pitfall 9 alternative.

From `packages/docintel-core/src/docintel_core/adapters/stub/llm.py:23` (the current `_STUB_REFUSAL = "[STUB REFUSAL] No evidence found in retrieved context."` line — Plan 06-05 updates the value, NOT this plan):
- The CURRENT Wave 0 value (`"[STUB REFUSAL] No evidence found in retrieved context."`) remains UNCHANGED until Plan 06-05 lands. This plan ships `REFUSAL_TEXT_SENTINEL` in `docintel_core.types` but DOES NOT touch `adapters/stub/llm.py`.

<!-- Existing analogs: the three wrap-gate scripts -->

From `scripts/check_adapter_wraps.sh` (full 41 lines — the shape Plan 06-02 mirrors):
- Shebang `#!/usr/bin/env bash` + `set -euo pipefail` + `SCAN_DIR="${1:-<default>}"` + `PROBLEM=0` + grep loop + final exit code.
- Header block: usage line, SDK_PATTERNS list (one per line), exit-code semantics.
- `grep -rl "$PATTERN" "$SCAN_DIR" --include="*.py"` for the scan; inner per-file `grep -q` for the conformance check.

From `scripts/check_index_wraps.sh:45` — `SDK_PATTERNS='QdrantClient(\|qdrant_client\|\.upsert(\|...'` — POSIX `egrep`-style alternation with `\|`.

From `scripts/check_ingest_wraps.sh` — similar shape; the three scripts share the structural template.

<!-- 5-file doc updates (D-02): exact line-context for find-and-replace -->

From `CLAUDE.md:27` (the operating rule):
- Current: `- **All prompts live in \`src/docintel/generation/prompts.py\`**, versioned with a hash. Grep for inline string-literal prompts must return zero matches.`
- Target: `- **All prompts live in \`packages/docintel-generate/src/docintel_generate/prompts.py\`**, versioned with a hash. Grep for inline string-literal prompts must return zero matches.`

From `.planning/PROJECT.md` (two references):
- Line 31 (Phase 6 phase-list line): `src/docintel/generation/prompts.py` → `packages/docintel-generate/src/docintel_generate/prompts.py`.
- Line 66 (Constraints "Prompts are versioned"): same substitution.

From `.planning/REQUIREMENTS.md:56` (GEN-01 success criterion):
- Current: `**GEN-01**: All prompts live in \`src/docintel/generation/prompts.py\`; ...`
- Target: `**GEN-01**: All prompts live in \`packages/docintel-generate/src/docintel_generate/prompts.py\`; ...`

From `.planning/ROADMAP.md:210` (Phase 6 Provides:):
- Current: `**Provides:** \`src/docintel/generation/prompts.py\` with versioned prompts, generator wrapping \`LLMClient\`, refusal path`
- Target: `**Provides:** \`packages/docintel-generate/src/docintel_generate/prompts.py\` with versioned prompts, generator wrapping \`LLMClient\`, refusal path`

From `.planning/config.json:15`:
- Current: `    "prompt_home": "src/docintel/generation/prompts.py",`
- Target: `    "prompt_home": "packages/docintel-generate/src/docintel_generate/prompts.py",`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Create the docintel-generate package skeleton + add REFUSAL_TEXT_SENTINEL to docintel_core.types + regen uv.lock</name>
  <files>packages/docintel-generate/pyproject.toml, packages/docintel-generate/src/docintel_generate/__init__.py, packages/docintel-generate/src/docintel_generate/py.typed, packages/docintel-core/src/docintel_core/types.py, uv.lock</files>
  <read_first>
    - packages/docintel-retrieve/pyproject.toml (full file — the byte-exact analog for the new package's manifest; Phase 6 deviates ONLY in name, description, and dependencies — see RESEARCH §Recommended Project Structure lines 252-292)
    - packages/docintel-retrieve/src/docintel_retrieve/__init__.py (analog for the Wave 0 → Wave 2 incremental `__all__` build-up; Wave 0 Phase 6 starts with empty `__all__`)
    - packages/docintel-retrieve/src/docintel_retrieve/py.typed (zero-byte file pattern)
    - packages/docintel-core/src/docintel_core/types.py (lines 1-52 for module structure, `__all__` list, and import block; lines 148-191 for the RetrievedChunk analog — `REFUSAL_TEXT_SENTINEL` is placed near this as a sibling constant)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-generate/pyproject.toml`" lines 46-79 (verbatim deviation notes for Phase 6)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-generate/src/docintel_generate/__init__.py`" lines 82-122 (Wave-incremental docstring + Wave 0 stub `__all__ = []`)
    - .planning/phases/06-generation/06-RESEARCH.md §"Open Question 1" lines 1070-1075 (Pitfall 9 alternative — REFUSAL_TEXT_SENTINEL in core)
    - pyproject.toml (workspace root — verify `[tool.uv.workspace].members` is `packages/*`; nothing to change there)
    - uv.lock (current state — re-generated by running `uv lock` after the new package pyproject lands)
  </read_first>
  <action>
    Three sub-steps in this task (single commit when complete):

    **Sub-step 1 — Create the docintel-generate package skeleton:**

    Create `packages/docintel-generate/pyproject.toml` mirroring `packages/docintel-retrieve/pyproject.toml` verbatim with three deviations:
    - `name = "docintel-generate"`.
    - `description = "Query-time generation: prompts module + Generator orchestrator + structured-output judge parser for docintel"`.
    - `dependencies = ["docintel-core", "docintel-retrieve"]` (drop `numpy` — docintel-generate touches no vectors).

    Keep all other fields verbatim: `version = "0.1.0"`, `requires-python = ">=3.11,<3.13"`, `[build-system]` with `hatchling`, `[tool.hatch.build.targets.wheel] packages = ["src/docintel_generate"]`. No `[project.scripts]` block (library-only).

    Create `packages/docintel-generate/src/docintel_generate/__init__.py` with this Wave 0 minimal content:
    - Triple-quoted module docstring: "Phase 6 implementation — query-time generation. Composes Phase 5 `Retriever` + Phase 2 `LLMClient` into a single `Generator.generate(query, k) -> GenerationResult` seam. See `docintel_generate.generator` for the orchestrator class; `docintel_generate.prompts` for the canonical prompt home (GEN-01); `docintel_generate.parse` for the regex + sentinel helpers. Public surface is built up incrementally across Plans 06-03..06-04: Plan 06-03 adds `SYNTHESIS_PROMPT`, `REFUSAL_PROMPT`, `JUDGE_PROMPT`, `PROMPT_VERSION_HASH` re-exports; Plan 06-04 adds `Generator`, `GenerationResult` re-exports."
    - Empty `__all__: list[str] = []` (Wave 0 — no public surface yet; Plans 06-03/06-04 extend).
    - No imports (Wave 0 — adding imports here before the modules exist would fail).

    Create `packages/docintel-generate/src/docintel_generate/py.typed` as a zero-byte file (PEP 561 marker, identical to `packages/docintel-retrieve/src/docintel_retrieve/py.typed`).

    **Sub-step 2 — Add REFUSAL_TEXT_SENTINEL to docintel_core.types (Pitfall 9 resolution):**

    Edit `packages/docintel-core/src/docintel_core/types.py`:
    - Add `REFUSAL_TEXT_SENTINEL` to the module's `__all__` list (alphabetically between `NormalizedFilingManifest` and `RetrievedChunk`).
    - Add `from typing import Final` to the existing top-of-file imports (insert after `from typing import Any, Literal` at line 38 → consolidate as `from typing import Any, Final, Literal`).
    - Insert a constant block AFTER the existing `RetrievedChunk` class definition (line 192 region) and BEFORE the next class (`IndexManifestEmbedder` at line 194):

      ```
      REFUSAL_TEXT_SENTINEL: Final[str] = (
          "I cannot answer this question from the retrieved 10-K excerpts."
      )
      """Canonical refusal sentinel for Phase 6 (D-11) — single source of truth across stub LLM, generator (docintel-generate), and Phase 7 Citation parser.

      Pitfall 9 (RESEARCH Open Question 1) resolution: this constant lives HERE in docintel_core.types — not in docintel_generate.prompts — so the upward-stack import direction is preserved. docintel-generate imports from docintel-core; never the reverse. The stub adapter (packages/docintel-core/src/docintel_core/adapters/stub/llm.py) imports this name in Plan 06-05; docintel_generate.prompts.REFUSAL_PROMPT also equals this string body (Plan 06-03 imports it as the canonical body for hash computation).

      The string is exactly 64 characters; no trailing whitespace; no surrounding punctuation. Phase 9 MET-03 faithfulness tests assert byte-exact `text.startswith(REFUSAL_TEXT_SENTINEL)` — any drift in this constant breaks Phase 9 / 10 / 13.
      """
      ```

      Place it after the `RetrievedChunk` class because (a) `RetrievedChunk` is the closest semantic relative — both are Phase 5/6 boundary constants, (b) the existing file groups Pydantic models clustering early-to-late by phase. The constant is the first non-`BaseModel` export in the module; that is intentional per Pitfall 9 alternative.

    **Sub-step 3 — Regenerate uv.lock:**

    Run `uv lock` from the repository root. The new `docintel-generate/pyproject.toml` is picked up by the `packages/*` workspace glob (verified in workspace pyproject). `uv lock` adds workspace-dep edges to `uv.lock` for `docintel-generate` (pinning `docintel-core` + `docintel-retrieve` as workspace edges, no new external PyPI resolutions — both deps are workspace-local). Confirm `uv lock --check` exits 0 afterwards.

    Do NOT regenerate via `uv sync --all-packages` here (that would also install the new package and may pull non-committed installation artifacts). `uv lock` alone is sufficient for the lockfile gate.

    Anti-patterns to avoid:
    - Do NOT add a `prompts.py` skeleton (empty file) inside the new package — Wave 1 (Plan 06-03) creates `prompts.py` with the real content. Creating an empty `prompts.py` here would (a) make `__init__.py`'s eventual `from docintel_generate.prompts import ...` fail with "module exists but symbol missing" instead of the cleaner ImportError, (b) defeat the W0 → W1 → W2 incremental build-up the analog uses.
    - Do NOT add `anthropic`, `openai`, or any non-workspace external dep to `docintel-generate/pyproject.toml`. Those live in `docintel-core/pyproject.toml` (single-pin discipline per PATTERNS.md anti-pattern flag).
    - Do NOT create a `[project.scripts]` block — `docintel-generate` is library-only in v1 per CONTEXT.md `<code_context>` "Per-package Dockerfile target / no CLI".
    - Do NOT touch `packages/docintel-core/src/docintel_core/adapters/stub/llm.py` — Plan 06-05 owns the sentinel value update. This plan only adds the NEW constant in `types.py`; the stub still uses its current Phase 2 placeholder value until Wave 3.
  </action>
  <verify>
    <automated>test -f packages/docintel-generate/pyproject.toml && test -f packages/docintel-generate/src/docintel_generate/__init__.py && test -f packages/docintel-generate/src/docintel_generate/py.typed && grep -q "name = \"docintel-generate\"" packages/docintel-generate/pyproject.toml && grep -q "docintel-retrieve" packages/docintel-generate/pyproject.toml && grep -q "REFUSAL_TEXT_SENTINEL" packages/docintel-core/src/docintel_core/types.py && grep -q "I cannot answer this question from the retrieved 10-K excerpts" packages/docintel-core/src/docintel_core/types.py && uv lock --check && python -c "from docintel_core.types import REFUSAL_TEXT_SENTINEL; assert REFUSAL_TEXT_SENTINEL == 'I cannot answer this question from the retrieved 10-K excerpts.', repr(REFUSAL_TEXT_SENTINEL)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/docintel-generate/pyproject.toml` exists; `grep -c 'name = "docintel-generate"' packages/docintel-generate/pyproject.toml` returns 1; `grep -c '"docintel-core"' packages/docintel-generate/pyproject.toml` returns 1; `grep -c '"docintel-retrieve"' packages/docintel-generate/pyproject.toml` returns 1; `grep -c 'numpy' packages/docintel-generate/pyproject.toml` returns 0 (no numpy dep).
    - Source assertion: `packages/docintel-generate/src/docintel_generate/__init__.py` exists; contains `__all__: list[str] = []` (Wave 0 stub); contains the module docstring referencing Plans 06-03 and 06-04 (the W1/W2 build-up plans).
    - Source assertion: `packages/docintel-generate/src/docintel_generate/py.typed` exists and is zero bytes (`test ! -s packages/docintel-generate/src/docintel_generate/py.typed` exits 0).
    - Source assertion: `grep -c "REFUSAL_TEXT_SENTINEL" packages/docintel-core/src/docintel_core/types.py` returns at least 2 (one in `__all__`, one for the constant definition); `grep -c '"I cannot answer this question from the retrieved 10-K excerpts."' packages/docintel-core/src/docintel_core/types.py` returns 1.
    - Behavior assertion: `python -c "from docintel_core.types import REFUSAL_TEXT_SENTINEL; assert REFUSAL_TEXT_SENTINEL == 'I cannot answer this question from the retrieved 10-K excerpts.', repr(REFUSAL_TEXT_SENTINEL)"` exits 0 (constant importable; value byte-exact; 64 chars including the trailing period).
    - Behavior assertion: `uv lock --check` exits 0 (FND-09 gate stays green after lockfile regen).
    - Behavior assertion: `uv run python -c "import docintel_generate; assert docintel_generate.__all__ == []"` exits 0 (package importable; no public surface yet).
    - Source assertion: no existing tests fail — `uv run pytest -ra -q -m "not real" --co -q | tail -1` shows test-count grew by at most the Plan 06-01 scaffold count (no test silently broken by the type-file edit).
  </acceptance_criteria>
  <done>The 8th workspace package `docintel-generate` exists with a working manifest + empty `__init__.py` + `py.typed` marker. `REFUSAL_TEXT_SENTINEL` is exported from `docintel_core.types` (the Pitfall 9 cycle-safe home). `uv.lock` is regenerated and `uv lock --check` passes. Plan 06-03 will land `prompts.py` in this package, importing `REFUSAL_TEXT_SENTINEL` from core. Plan 06-05 will update the stub adapter's `_STUB_REFUSAL` to import the same constant.</done>
</task>

<task type="auto">
  <name>Task B: Update the five doc-path references (D-02) in a single coordinated edit</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/config.json</files>
  <read_first>
    - .planning/phases/06-generation/06-PATTERNS.md lines 874-895 (Docs reconciliation table — every line + every old/new substitution verbatim)
    - .planning/phases/06-generation/06-CONTEXT.md D-02 (the decision authorizing this sweep)
    - .planning/phases/06-generation/06-RESEARCH.md §"Open Question 2" lines 1077-1080 (recommends landing the doc sweep in Wave 0 alongside the package skeleton — that recommendation is honored here)
    - CLAUDE.md line 27 (current state)
    - .planning/PROJECT.md lines 31 + 66 (two references)
    - .planning/REQUIREMENTS.md line 56
    - .planning/ROADMAP.md line 210
    - .planning/config.json line 15
  </read_first>
  <action>
    Execute six byte-exact find-and-replace edits across five files. Every replacement substitutes `src/docintel/generation/prompts.py` → `packages/docintel-generate/src/docintel_generate/prompts.py` and changes NOTHING ELSE on the line.

    Use the Edit tool with exact-match `old_string` / `new_string`. Each edit targets ONE specific line in ONE specific file.

    **Edit 1 — CLAUDE.md (1 reference):**
    File: `CLAUDE.md`. Find the exact line at line 27:
    - `old_string`: `- **All prompts live in \`src/docintel/generation/prompts.py\`**, versioned with a hash. Grep for inline string-literal prompts must return zero matches.`
    - `new_string`: `- **All prompts live in \`packages/docintel-generate/src/docintel_generate/prompts.py\`**, versioned with a hash. Grep for inline string-literal prompts must return zero matches.`

    **Edit 2 — .planning/PROJECT.md (Phase 6 phase-list line at ~line 31):**
    File: `.planning/PROJECT.md`. Read the current state first to confirm the exact bytes (the Phase 6 phase line; it includes `src/docintel/generation/prompts.py` in a parenthetical). Substitute the substring only.

    **Edit 3 — .planning/PROJECT.md (Constraints "Prompts are versioned" line at ~line 66):**
    File: `.planning/PROJECT.md`. Same file, second occurrence. The constraint reads: `**Prompts are versioned**: all prompts live in \`src/docintel/generation/prompts.py\` with a hash. Grep for inline string-literal prompts must return zero matches.` Substitute the path.

    **Edit 4 — .planning/REQUIREMENTS.md (GEN-01 line at line 56):**
    File: `.planning/REQUIREMENTS.md`. The GEN-01 line: `- [ ] **GEN-01**: All prompts live in \`src/docintel/generation/prompts.py\`; grep for inline string-literal prompts outside this file returns zero matches (CI gate)`. Substitute the path.

    **Edit 5 — .planning/ROADMAP.md (Phase 6 Provides: line at line 210):**
    File: `.planning/ROADMAP.md`. The Provides: line: `**Provides:** \`src/docintel/generation/prompts.py\` with versioned prompts, generator wrapping \`LLMClient\`, refusal path`. Substitute the path.

    **Edit 6 — .planning/config.json (constraints.prompt_home key at line 15):**
    File: `.planning/config.json`. The line: `    "prompt_home": "src/docintel/generation/prompts.py",`. Substitute to: `    "prompt_home": "packages/docintel-generate/src/docintel_generate/prompts.py",`. PRESERVE the surrounding JSON indentation (4 spaces), the trailing comma, the quote-around-value format. JSON syntax MUST remain valid (`python -c "import json; json.load(open('.planning/config.json'))"` must succeed after the edit).

    Anti-patterns to avoid:
    - Do NOT rewrite surrounding sentences for "improved clarity" while doing the path update. The rest of every line is locked verbatim.
    - Do NOT add the new path to documents that don't already contain the OLD path (no edits to `STATE.md`, individual phase CONTEXT.md files, or any other `.planning/phases/**` file). D-02 lists exactly these five files; PATTERNS.md confirms the count via the explicit table.
    - Do NOT touch `STATE.md` to "record the path move" — `gsd-execute-phase` updates STATE.md during the post-execute summary commit; if a manual STATE.md edit is needed it goes in Plan 06-07's audit task, not here.
    - Do NOT delete or alter the `# STATE: docintel` Recent Activity bullet that records `Phase 6 context gathered ... CLAUDE.md / config.json / REQUIREMENTS GEN-01 / ROADMAP / PROJECT.md path-references updated` — STATE.md is intentionally a log of decisions, not a current-state snapshot.
  </action>
  <verify>
    <automated>! git grep "src/docintel/generation/prompts.py" CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/config.json && grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" CLAUDE.md | grep -q "^1$" && grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/PROJECT.md | grep -q "^2$" && grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/ROADMAP.md | grep -q "^1$" && grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/config.json | grep -q "^1$" && python -c "import json; json.load(open('.planning/config.json'))"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `git grep "src/docintel/generation/prompts.py" CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/config.json` returns NO matches (the OLD path is fully eliminated across these five files).
    - Source assertion: `grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" CLAUDE.md` returns exactly 1.
    - Source assertion: `grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/PROJECT.md` returns exactly 2 (Phase 6 phase line + Constraints line).
    - Source assertion: `grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/REQUIREMENTS.md` returns exactly 1.
    - Source assertion: `grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/ROADMAP.md` returns exactly 1.
    - Source assertion: `grep -c "packages/docintel-generate/src/docintel_generate/prompts.py" .planning/config.json` returns exactly 1.
    - Behavior assertion: `.planning/config.json` is still valid JSON (`python -c "import json; json.load(open('.planning/config.json'))"` exits 0).
    - Behavior assertion: `git diff --stat CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/config.json` shows exactly 5 files changed, with the line-change counts being symmetric (insertions = deletions per file — single-line substitutions).
  </acceptance_criteria>
  <done>All five canonical source-of-truth documents reference the new path `packages/docintel-generate/src/docintel_generate/prompts.py`. The old path `src/docintel/generation/prompts.py` is fully eliminated from CLAUDE.md, PROJECT.md, REQUIREMENTS.md, ROADMAP.md, and config.json (it survives only in `.planning/STATE.md` as a historical record of the path move; STATE.md remains untouched). `.planning/config.json` is still valid JSON. Plan 06-07's audit verifies this remains the case post-merge.</done>
</task>

<task type="auto">
  <name>Task C: Create the prompt-locality CI grep gate script</name>
  <files>scripts/check_prompt_locality.sh</files>
  <read_first>
    - scripts/check_adapter_wraps.sh (full 41-line analog — shape + comment style + `set -euo pipefail` + `SCAN_DIR` default + exit codes)
    - scripts/check_index_wraps.sh (variant with `\|`-alternation `SDK_PATTERNS`; line 45 for the alternation pattern shape)
    - scripts/check_ingest_wraps.sh (third variant; same structural template)
    - .planning/phases/06-generation/06-RESEARCH.md §Code Examples §Pattern 4 lines 586-668 (the canonical full-script reference body — `NAME_PATTERN`, `PHRASE_PATTERN`, allowlist + noqa pattern)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `scripts/check_prompt_locality.sh`" lines 685-755 (verbatim deviation notes for Phase 6)
    - .planning/phases/06-generation/06-CONTEXT.md D-04 (gate spec) + D-05 (allowlist + noqa policy) + D-06 (CI wiring — Plan 06-07 wires the YAML step; this plan only ships the script)
    - tests/fixtures/prompt_locality_violations/offender.py (Plan 06-01 Task 0 output — the negative fixture this script's positive-fail-case test will scan)
    - tests/fixtures/prompt_locality_violations_with_noqa/allowed.py (Plan 06-01 Task 0 output — the escape-hatch test target)
    - tests/test_prompt_locality.py (Plan 06-01 Task A output — the three xfail-strict tests this script will flip to passing)
  </read_first>
  <action>
    Create `scripts/check_prompt_locality.sh` mirroring the three existing wrap-gate scripts' shape verbatim, customized per D-04 and D-05.

    The script MUST be executable (chmod 0755) and exit 0 on the canonical layout, exit 1 when scanning the negative fixture, and exit 0 when scanning the noqa-escape fixture.

    Script body structure (translate to actual bash; do NOT inline the full body in this action — refer to RESEARCH §Pattern 4 lines 586-668 for the literal source-of-truth body):

    1. **Shebang + set strict mode:**
       - `#!/usr/bin/env bash`
       - `set -euo pipefail`

    2. **Header comment block (lines 2-21):**
       - Purpose: "CI grep gate for GEN-01 (Phase 6 D-04). Fails if a Python file outside the allowlist contains an inline prompt-like string literal."
       - Usage: `scripts/check_prompt_locality.sh [SCAN_DIR]` (SCAN_DIR defaults to `packages/`).
       - Allowlist enumeration: `packages/docintel-generate/src/docintel_generate/prompts.py`, `packages/docintel-generate/src/docintel_generate/parse.py`, `tests/**`, `**/conftest.py`, `**/test_*.py`, `packages/docintel-core/src/docintel_core/adapters/stub/llm.py`.
       - Escape hatch: per-line `# noqa: prompt-locality`.
       - Exit codes: 0 = pass, 1 = at least one offending line found.

    3. **Default SCAN_DIR + counters:**
       - `SCAN_DIR="${1:-packages/}"`.
       - `PROBLEM=0`.

    4. **Pattern definitions:**
       - `NAME_PATTERN='_[A-Z_]*PROMPT[A-Z_]*\b|_[A-Z_]*INSTRUCTION[A-Z_]*\b|_[A-Z_]*SYSTEM_PROMPT\b'` (identifier-based — matches `_SOMETHING_PROMPT`, `_SYSTEM_INSTRUCTION`, `_X_SYSTEM_PROMPT_Y` constant names).
       - `PHRASE_PATTERN='\b(You are|Based on the|<context>|<chunks>|cite|grounded|chunk_id)\b'` (phrase-based — applied with a length filter `[[ ${#match} -lt 80 ]] && continue` to reduce false positives per Pitfall 2).

    5. **Allowlist excludes (build grep `--exclude` / `--exclude-dir` args):**
       - `--exclude-dir=tests`, `--exclude=conftest.py`, `--exclude=test_*.py`.
       - `--exclude=prompts.py` (matches `packages/docintel-generate/src/docintel_generate/prompts.py`).
       - `--exclude=parse.py` (matches `packages/docintel-generate/src/docintel_generate/parse.py`).
       - `--exclude=llm.py` for the stub-LLM path — but NOTE: `llm.py` as a bare basename would also exclude any other `llm.py` in the tree. ALTERNATIVE: use `--exclude-dir=adapters/stub` to scope the exclude precisely. PATTERNS.md recommends using basenames per the analog scripts' convention; for Phase 6, the stub-llm path is the only `llm.py` in the tree (verified: no other `llm.py` exists outside `adapters/stub/`), so `--exclude=llm.py` is safe. If a future plan adds another `llm.py` the gate needs `--exclude-dir` refinement at that time.

    6. **Two-loop scan structure:**
       - **Loop 1 — NAME_PATTERN check:**
         ```
         while IFS=: read -r file lineno match; do
             [[ -z "$file" ]] && continue
             if grep -q '# noqa: prompt-locality' <<<"$(sed -n "${lineno}p" "$file")"; then continue; fi
             echo "FAIL: $file:$lineno matches identifier pattern: $match"
             PROBLEM=1
         done < <(grep -rnE "${EXCLUDES[@]}" --include='*.py' "$NAME_PATTERN" "$SCAN_DIR" 2>/dev/null || true)
         ```
       - **Loop 2 — PHRASE_PATTERN check with length filter:**
         ```
         while IFS=: read -r file lineno match; do
             [[ -z "$file" ]] && continue
             [[ ${#match} -lt 80 ]] && continue
             if grep -q '# noqa: prompt-locality' <<<"$(sed -n "${lineno}p" "$file")"; then continue; fi
             echo "FAIL: $file:$lineno contains prompt-like phrase (>80 chars): $match"
             PROBLEM=1
         done < <(grep -rnE "${EXCLUDES[@]}" --include='*.py' "$PHRASE_PATTERN" "$SCAN_DIR" 2>/dev/null || true)
         ```

    7. **Final exit:**
       - `if [ "$PROBLEM" -eq 0 ]; then echo "OK: no inline prompts outside allowlist"; fi`.
       - `exit "$PROBLEM"`.

    Set the executable bit: `chmod +x scripts/check_prompt_locality.sh`. Verify in git via `git update-index --chmod=+x scripts/check_prompt_locality.sh` if `git diff --raw` shows the file is committed without the executable bit (the three analogs are all committed executable — `ls -la scripts/check_adapter_wraps.sh` shows the `x` bit).

    The script handles the "empty prompts.py" Wave 0 case naturally — when `packages/docintel-generate/src/docintel_generate/prompts.py` does NOT yet exist (it lands in Plan 06-03), the script scans the rest of `packages/` and finds zero violations (the existing source tree has no inline prompts outside the allowlisted stub LLM file, which is allowlisted by `--exclude=llm.py`). So `bash scripts/check_prompt_locality.sh` exits 0 immediately after this plan lands; this is the expected Wave 0 baseline.

    Anti-patterns to avoid:
    - Do NOT use Python AST scanning (`python -c "import ast; ..."`). The four grep gates are bash for consistency (DRY policy) and to avoid the ~5s `uv run python` startup cost in CI per RESEARCH §Don't Hand-Roll line 696.
    - Do NOT scan `data/`, `.planning/`, or `*.md` paths. The gate is scoped to `--include='*.py'` only.
    - Do NOT auto-fix violations (no `--fix` flag). The gate fails CI; the developer fixes the file or adds `# noqa: prompt-locality`.
    - Do NOT skip the `# noqa: prompt-locality` line-check before reporting each hit — Pitfall 2 explicitly requires the escape hatch to work, and Plan 06-01's `test_grep_gate_respects_noqa` test will fail if the check is omitted.
    - Do NOT inline a TODO for "tighten patterns later" — D-04 says "Pattern set (planner finalises during execution)". The patterns above ARE the finalized set; any future tightening goes through a normal phase.
  </action>
  <verify>
    <automated>test -x scripts/check_prompt_locality.sh && bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations/ 2>&1 | tee /tmp/locality_violations.log; ! grep -q "OK:" /tmp/locality_violations.log && bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations_with_noqa/ 2>&1 | tee /tmp/locality_noqa.log; grep -q "OK:" /tmp/locality_noqa.log && bash scripts/check_prompt_locality.sh 2>&1 | tee /tmp/locality_canonical.log; grep -q "OK:" /tmp/locality_canonical.log && uv run pytest tests/test_prompt_locality.py::test_grep_gate_fails_on_violation tests/test_prompt_locality.py::test_grep_gate_respects_noqa -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `scripts/check_prompt_locality.sh` exists (`test -f scripts/check_prompt_locality.sh` exits 0).
    - Source assertion: script is executable (`test -x scripts/check_prompt_locality.sh` exits 0).
    - Source assertion: first line is `#!/usr/bin/env bash`; second line is `set -euo pipefail` (Plan 06-07 audit checks); `grep -q "NAME_PATTERN" scripts/check_prompt_locality.sh`; `grep -q "PHRASE_PATTERN" scripts/check_prompt_locality.sh`; `grep -q "noqa: prompt-locality" scripts/check_prompt_locality.sh`.
    - Behavior assertion: `bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations/` exits non-zero (`echo $?` is 1) AND outputs a `FAIL: ...:N matches ...` line naming `offender.py`.
    - Behavior assertion: `bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations_with_noqa/` exits 0 AND outputs `OK: no inline prompts outside allowlist`.
    - Behavior assertion: `bash scripts/check_prompt_locality.sh` (no arguments — scans `packages/` default) exits 0 (canonical layout has no violations; Wave 0 baseline).
    - Behavior assertion: `uv run pytest tests/test_prompt_locality.py::test_grep_gate_fails_on_violation tests/test_prompt_locality.py::test_grep_gate_respects_noqa -x` PASSES (the two Wave 0 xfail-strict tests for these behaviors flip to xpass — Wave 0 incremental gain).
    - Source assertion: `tests/test_prompt_locality.py::test_grep_gate_passes_on_canonical_layout` REMAINS xfailed (its xfail reason is "Wave 4 — Plan 06-07 confirms gate exit 0 after all production code lands"; Wave 0 only confirms it exits 0 on the EXISTING tree, but the comprehensive "canonical layout" assertion includes the post-Wave-3 state).
  </acceptance_criteria>
  <done>`scripts/check_prompt_locality.sh` exists, is executable, and implements D-04 + D-05 — name-based + phrase-based detection with allowlist + per-line noqa escape. Two of the three Plan 06-01 xfail tests flip to xpass immediately. The script is independent of `prompts.py` existing (handles the W0 empty-package state cleanly). Plan 06-07 adds the CI YAML step that runs this script on every PR.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `uv lock --check` exits 0 (FND-09 gate stays green; the regenerated `uv.lock` is consistent with all `pyproject.toml` files including the new `docintel-generate`).
2. `python -c "from docintel_core.types import REFUSAL_TEXT_SENTINEL; import docintel_generate; assert REFUSAL_TEXT_SENTINEL == 'I cannot answer this question from the retrieved 10-K excerpts.'"` exits 0 (both the new constant and the new package are importable).
3. `git grep "src/docintel/generation/prompts.py"` returns hits ONLY in `.planning/STATE.md` (the historical log entry that records the move). All five canonical source-of-truth docs are clean.
4. `bash scripts/check_prompt_locality.sh` exits 0 (canonical-layout baseline); `bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations/` exits 1; `bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations_with_noqa/` exits 0.
5. `uv run pytest -ra -q -m "not real"` exits 0 (no regressions). The two Plan 06-01 xfail tests `test_grep_gate_fails_on_violation` and `test_grep_gate_respects_noqa` now report xpassed (their xfail markers will be removed by Plan 06-07's xfail-removal sweep).
6. `mypy --strict packages/docintel-core/src/docintel_core/types.py` reports no new errors (the `REFUSAL_TEXT_SENTINEL: Final[str] = (...)` constant is typed correctly).
</verification>

<success_criteria>
Wave 0 foundation is in place:
- New 8th workspace package `docintel-generate` exists with `pyproject.toml` + empty `__init__.py` + `py.typed`
- `REFUSAL_TEXT_SENTINEL` lives in `docintel_core.types` (Pitfall 9 cycle-safe home; D-11 canonical value)
- `scripts/check_prompt_locality.sh` ships executable + green on the canonical layout + fails on planted violations + respects `# noqa: prompt-locality`
- All five D-02 doc-path references updated; old path eliminated from the source-of-truth docs
- `uv.lock` regenerated; `uv lock --check` passes
- Two of Plan 06-01's xfail-strict tests flip to xpass (incremental Wave 0 gain)
</success_criteria>

<output>
After completion, create `.planning/phases/06-generation/06-02-SUMMARY.md` documenting:
- Files created: 4 in the new package + 1 grep gate + 5 doc updates + uv.lock regen + the REFUSAL_TEXT_SENTINEL addition
- Confirmation that the `--exclude=llm.py` allowlist pattern only matches `adapters/stub/llm.py` (verified by `find packages -name "llm.py"`)
- Confirmation that REFUSAL_TEXT_SENTINEL is 64 bytes (length verified via `python -c`)
- Pitfall 9 resolution: explicit note that the sentinel lives in `docintel_core.types`, NOT in `docintel_generate.prompts`
- Any deviations from the analog scripts (e.g., if grep -E vs grep -rnE differed across platforms in CI)
</output>
