---
phase: 06-generation
plan: 03
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - packages/docintel-generate/src/docintel_generate/prompts.py
  - packages/docintel-generate/src/docintel_generate/parse.py
  - packages/docintel-generate/src/docintel_generate/__init__.py
autonomous: true
requirements: [GEN-01, GEN-02]
must_haves:
  truths:
    - "`packages/docintel-generate/src/docintel_generate/prompts.py` exists, contains `SYNTHESIS_PROMPT: Final[str]`, `REFUSAL_PROMPT: Final[str]`, `JUDGE_PROMPT: Final[str]`, `_SYNTHESIS_HASH: Final[str]`, `_REFUSAL_HASH: Final[str]`, `_JUDGE_HASH: Final[str]`, `PROMPT_VERSION_HASH: Final[str]`, and `build_judge_user_prompt(...)` helper — every constant computed at module-import time via `hashlib.sha256(s.encode('utf-8')).hexdigest()[:12]`."
    - "`packages/docintel-generate/src/docintel_generate/parse.py` exists, contains `_CHUNK_RE: Final[re.Pattern[str]]` (the canonical chunk-ID extraction regex moved from `adapters/stub/llm.py`) AND `is_refusal(text: str) -> bool` helper that returns `text.startswith(REFUSAL_TEXT_SENTINEL)`."
    - "`REFUSAL_PROMPT` byte-equals `REFUSAL_TEXT_SENTINEL` from `docintel_core.types` (Pitfall 9 single-source-of-truth)."
    - "`SYNTHESIS_PROMPT` body contains: (a) the locked fenced citation example with `[AAPL-FY2024-Item-1A-018]` AND `[NVDA-FY2024-Item-7-042]` substrings verbatim (D-10), (b) the refusal-when-insufficient instruction with the exact `REFUSAL_TEXT_SENTINEL` value, (c) the no-invented-chunk-ids rule, (d) the comparative-question structuring guidance per Pitfall 10."
    - "`JUDGE_PROMPT` body contains scoring rubric instructions for the 4-field `JudgeVerdict` schema (`score`, `passed`, `reasoning`, `unsupported_claims`)."
    - "`packages/docintel-generate/src/docintel_generate/__init__.py` re-exports the three `*_PROMPT` constants + `PROMPT_VERSION_HASH` — Plan 06-04 extends with `Generator` + `GenerationResult` re-exports."
    - "`uv run pytest tests/test_prompt_version_hash.py -x` passes (all three Plan 06-01 xfail-strict tests for GEN-02 now xpass; Plan 06-07 removes the xfail markers)."
    - "`bash scripts/check_prompt_locality.sh` continues to exit 0 — `prompts.py` is in the allowlist; `parse.py` is in the allowlist."
  artifacts:
    - path: "packages/docintel-generate/src/docintel_generate/prompts.py"
      provides: "Three named module-level Final[str] prompts (SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT), three per-prompt + one combined PROMPT_VERSION_HASH via hashlib at import time, _h() helper, build_judge_user_prompt() helper (D-09 migration scaffold consumed by Plan 06-06 judge migration)"
    - path: "packages/docintel-generate/src/docintel_generate/parse.py"
      provides: "_CHUNK_RE compiled re.Pattern for [chunk_id] extraction (single canonical regex across stub + real + Phase 7); is_refusal() boolean helper; imports REFUSAL_TEXT_SENTINEL from docintel_core.types"
    - path: "packages/docintel-generate/src/docintel_generate/__init__.py"
      provides: "Wave 1 re-export expansion — adds SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT, PROMPT_VERSION_HASH to __all__ (Plan 06-04 adds Generator + GenerationResult)"
  key_links:
    - from: "packages/docintel-generate/src/docintel_generate/prompts.py"
      to: "docintel_core.types.REFUSAL_TEXT_SENTINEL"
      via: "from docintel_core.types import REFUSAL_TEXT_SENTINEL; REFUSAL_PROMPT: Final[str] = REFUSAL_TEXT_SENTINEL"
      pattern: "REFUSAL_PROMPT.*=.*REFUSAL_TEXT_SENTINEL"
    - from: "packages/docintel-generate/src/docintel_generate/parse.py"
      to: "docintel_core.types.REFUSAL_TEXT_SENTINEL"
      via: "from docintel_core.types import REFUSAL_TEXT_SENTINEL; def is_refusal(text): return text.startswith(REFUSAL_TEXT_SENTINEL)"
      pattern: "is_refusal.*REFUSAL_TEXT_SENTINEL"
    - from: "PROMPT_VERSION_HASH"
      to: "Plan 06-04 Generator.__init__ + Plan 06-07 EVAL-02 manifest readers (Phase 10) + generator_completed structlog (Plan 06-04 telemetry)"
      via: "from docintel_generate.prompts import PROMPT_VERSION_HASH"
      pattern: "PROMPT_VERSION_HASH"
---

<objective>
Land the canonical prompts module + the chunk-id regex helper in the new `docintel-generate` package. This is the GEN-01 + GEN-02 fulcrum: every prompt in the system now lives in `prompts.py` (the locked-down canonical home), every prompt has a deterministic `Final[str]` SHA256[:12] hash computed at module import, and the combined `PROMPT_VERSION_HASH` is the single value Phase 10's eval manifest will surface for bisection.

Three structural facts get locked in this wave:
1. The three prompt bodies are written ONCE — `SYNTHESIS_PROMPT` per CD-01 (planner refines RESEARCH §Code Example 1 body, MUST contain the D-10 locked fenced example + D-11 refusal sentinel + Pitfall 10 comparative-structuring guidance), `REFUSAL_PROMPT = REFUSAL_TEXT_SENTINEL` (Pitfall 9 cycle-safe import from core), `JUDGE_PROMPT` per CD-02 (RESEARCH §Code Example 1 body specifying 4-field `JudgeVerdict` schema).
2. The per-prompt + combined hashes are computed at module import time (Pattern 2 of RESEARCH lines 439-499; mypy-strict `Final[str]` annotations enforce no-runtime-mutation).
3. The `_CHUNK_RE` regex moves from `adapters/stub/llm.py:31` to `parse.py` (single canonical regex — Plan 06-05 will re-import in the stub; Plan 06-04 imports for the Generator's Step D citation parsing).

Output: 3 modified/new files. The `__init__.py` extends to re-export the four public prompt names. Plan 06-01 Task A's `tests/test_prompt_version_hash.py` xfail tests flip to xpass on this commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generation/06-CONTEXT.md
@.planning/phases/06-generation/06-RESEARCH.md
@.planning/phases/06-generation/06-PATTERNS.md
@.planning/phases/06-generation/06-VALIDATION.md
@.planning/phases/06-generation/06-01-SUMMARY.md
@.planning/phases/06-generation/06-02-SUMMARY.md

<interfaces>
<!-- Existing analog: Phase 5's `Final[str]` constant block in retriever.py -->

From `packages/docintel-retrieve/src/docintel_retrieve/retriever.py:71-119` (the `Final[int]` + `Final[str]` constant pattern Phase 6's prompts.py mirrors):
- `from typing import Final` at top of file.
- Each constant: `NAME: Final[type] = value` then triple-quoted docstring explaining the rationale (multi-paragraph).
- Constants are MODULE-LEVEL, not class-scoped.
- mypy --strict enforces `Final[...]` — the constants cannot be reassigned in another module.

From `packages/docintel-core/src/docintel_core/adapters/stub/llm.py:31` (the CURRENT `_CHUNK_RE` location — Plan 06-03 MOVES this; Plan 06-05 re-imports it from `parse.py`):
- `_CHUNK_RE: Final[re.Pattern[str]] = re.compile(r"\[([^\]]+)\]")` plus docstring.
- This module is still loaded by the stub adapter (`StubLLMClient.complete` uses `_CHUNK_RE.findall(prompt)` at line 77) — Plan 06-03 MUST NOT break this until Plan 06-05 lands the re-import. **Approach:** Plan 06-03 ADDS `_CHUNK_RE` to `parse.py` but does NOT remove it from `stub/llm.py`. Plan 06-05 (Wave 3) deletes the in-stub definition and adds the import. This means the regex briefly lives in TWO places between Wave 1 and Wave 3 — the byte-identical definitions are documented in both files.

From `packages/docintel-core/src/docintel_core/types.py` (post-Plan 06-02 state):
- `REFUSAL_TEXT_SENTINEL: Final[str] = "I cannot answer this question from the retrieved 10-K excerpts."` is exported.
- Added to `__all__` alphabetically.

From `.planning/phases/06-generation/06-RESEARCH.md §Code Examples §Example 1 lines 854-930` (canonical prompts.py body — planner-refined per CD-01 + CD-02 wording locked here):
- The file structure: `from __future__ import annotations` + `import hashlib` + `from typing import Final` + import REFUSAL_TEXT_SENTINEL + `_h()` helper + critical header comment about not reformatting + three `Final[str]` prompts + four hash constants + `build_judge_user_prompt()` helper.
- The SYNTHESIS_PROMPT body MUST contain: (a) rules block ending with "If the context does not contain enough information to answer the question, emit verbatim and ONLY this sentence: \"I cannot answer this question from the retrieved 10-K excerpts.\"", (b) comparative-question structuring instruction, (c) locked fenced example with the AAPL-FY2024-Item-1A-018 + NVDA-FY2024-Item-7-042 chunk_ids. RESEARCH lines 874-891 is the source-of-truth body to ship.
- The JUDGE_PROMPT body per RESEARCH lines 897-912: scoring rubric for the 4-field schema; bias-mitigation instruction.

From `.planning/phases/06-generation/06-CONTEXT.md` D-07, D-08, D-10, D-11:
- D-07: three named prompts; SYNTHESIS includes refusal instruction + comparative guidance.
- D-08: per-prompt + combined hashes via `hashlib.sha256(s.encode("utf-8")).hexdigest()[:12]`; Phase 10 manifest reads combined; ablation reports + structlog read per-prompt.
- D-10: inline `[chunk_id]` brackets; locked fenced example.
- D-11: canonical sentinel string; stored in REFUSAL_PROMPT.

<!-- Pitfall 3: byte-exact hash; do not reformat -->

The SHA256[:12] hash is byte-exact. Whitespace, encoding, or line-ending changes propagate. Document this with a CRITICAL header comment above the three prompts: `# DO NOT reformat the body of these prompts — the SHA256[:12] hash is byte-exact. Whitespace, encoding, or line-ending changes drift the hash. See Pitfall 3.` (RESEARCH lines 754-764)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Create prompts.py with three Final[str] prompts + per-prompt + combined hashes + build_judge_user_prompt helper</name>
  <files>packages/docintel-generate/src/docintel_generate/prompts.py</files>
  <read_first>
    - .planning/phases/06-generation/06-RESEARCH.md §Code Examples §Example 1 lines 854-930 (planner-refined canonical body; ship the SYNTHESIS_PROMPT and JUDGE_PROMPT bodies verbatim from this example — CD-01 + CD-02 leave exact wording to planner, and the RESEARCH already converged on the wording)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-generate/src/docintel_generate/prompts.py`" lines 136-198 (module shape with line-by-line deviation notes; the "CRITICAL header comment above the three prompts" instruction per Pitfall 3)
    - .planning/phases/06-generation/06-RESEARCH.md §Common Pitfalls §Pitfall 3 lines 754-764 (byte-exact hash + whitespace-drift defense; the rationale for the header comment)
    - .planning/phases/06-generation/06-RESEARCH.md §Common Pitfalls §Pitfall 10 lines 840-850 (single-prompt comparative handling; SYNTHESIS_PROMPT must include comparative-question structuring guidance)
    - .planning/phases/06-generation/06-CONTEXT.md D-07, D-08, D-10, D-11, CD-01, CD-02 (the contracts)
    - packages/docintel-core/src/docintel_core/types.py (post-06-02 — verify REFUSAL_TEXT_SENTINEL is importable; verify the constant value is exactly `"I cannot answer this question from the retrieved 10-K excerpts."`)
    - packages/docintel-retrieve/src/docintel_retrieve/retriever.py:71-119 (Final[str] constant-with-docstring shape — the structural pattern Phase 6 mirrors)
  </read_first>
  <action>
    Create `packages/docintel-generate/src/docintel_generate/prompts.py` with this exact structural shape (planner ships the wording from RESEARCH §Code Examples §Example 1 verbatim — CD-01 and CD-02 leave the wording to the planner and the researcher already converged on a wording the planner can adopt without further refinement):

    **Line 1-15 — Module docstring + critical header comment:**

    Triple-quoted module docstring naming the phase + the GEN-01 + GEN-02 contracts + the Pitfall 9 cycle-resolution note (sentinel imported from core). Reference RESEARCH §Code Example 1 line 856 for the byte-exact docstring text shape.

    AFTER the docstring (above the imports), insert this exact comment block:

    `# DO NOT reformat the body of these prompts — the SHA256[:12] hash is byte-exact.`
    `# Whitespace, encoding, or line-ending changes drift the hash. See Pitfall 3.`

    **Line 16-25 — Imports:**

    - `from __future__ import annotations`
    - `import hashlib`
    - `from typing import Final`
    - `from docintel_core.types import REFUSAL_TEXT_SENTINEL` (Pitfall 9 cycle-resolution: sentinel lives in core; prompts.py imports it)

    **Line 27-32 — `_h()` helper:**

    ```
    def _h(s: str) -> str:
        """SHA256 truncated to 12 hex chars — manifest-friendly."""
        return hashlib.sha256(s.encode("utf-8")).hexdigest()[:12]
    ```

    **Line 34-55 — `SYNTHESIS_PROMPT: Final[str]`:**

    Body MUST contain all four required substrings per D-07 + D-10 + D-11 + Pitfall 10:
    1. Open with "You are answering a question using ONLY the retrieved 10-K excerpts in the <context> block below." (XML-tag instruction per D-14 + Anthropic prompt-engineering best practice).
    2. Numbered rules block including: (a) every factual sentence MUST end with `[chunk_id]` citations; (b) do not invent chunk_ids; (c) cite from `<context>` only; (d) for comparative questions across companies, structure the answer to enumerate each compared entity's grounded evidence before reaching a comparison conclusion (Pitfall 10 mitigation).
    3. Refusal instruction: "If the context does not contain enough information to answer the question, emit verbatim and ONLY this sentence: 'I cannot answer this question from the retrieved 10-K excerpts.'" — the embedded quoted sentence MUST byte-match `REFUSAL_TEXT_SENTINEL` (i.e., the same 64-char string).
    4. Locked fenced example (D-10 verbatim):

       ```
       Example:
       Apple highlighted supplier concentration in China and Taiwan as a key risk in its
       FY2024 10-K [AAPL-FY2024-Item-1A-018]. NVIDIA disclosed revenue concentration with a
       single hyperscaler customer [NVDA-FY2024-Item-7-042].
       ```

    Ship the RESEARCH §Code Example 1 lines 874-891 body verbatim — that is the source-of-truth wording. Use triple-quoted `"""\` continuation form so the first newline is suppressed and the body starts at column 0. Annotate as `SYNTHESIS_PROMPT: Final[str] = """\` ...content... `"""`.

    **Line 57-59 — `REFUSAL_PROMPT: Final[str]`:**

    Single line: `REFUSAL_PROMPT: Final[str] = REFUSAL_TEXT_SENTINEL` (Pitfall 9 — re-export the sentinel from core as the canonical refusal prompt body; the hash computation that follows will hash the 64-char string).

    Add a docstring/comment explaining the import direction: "`REFUSAL_PROMPT` shadows `REFUSAL_TEXT_SENTINEL` from `docintel_core.types` per Pitfall 9 / RESEARCH Open Question 1 — single source of truth across stub LLM + generator + Phase 7 Citation parser. The 64-char string body is HASHED here for `_REFUSAL_HASH` and the combined `PROMPT_VERSION_HASH`."

    **Line 61-85 — `JUDGE_PROMPT: Final[str]`:**

    Body per CD-02 + RESEARCH §Code Example 1 lines 897-912 verbatim. MUST contain:
    1. Opening: "You are a strict citation-faithfulness judge."
    2. Evaluation instruction: claim-by-claim grounding against reference passages.
    3. Scoring rubric naming all four `JudgeVerdict` schema fields: `score` in [0.0, 1.0] = fraction of grounded claims; `passed = (score >= 0.5)`; `reasoning` = concise human-readable explanation; `unsupported_claims` = list of unsupported claims.
    4. Bias mitigation: judge ONLY prediction-vs-reference structural match; do not infer claims outside reference; do not penalize for omitted-but-grounded claims.

    Ship the RESEARCH §Code Example 1 wording verbatim — CD-02 leaves the wording to planner, and the researcher already converged on a wording.

    **Line 87-94 — Four hash constants:**

    ```
    # Per-prompt + combined hashes, computed at module import. mypy --strict verifies Final[str].
    _SYNTHESIS_HASH: Final[str] = _h(SYNTHESIS_PROMPT)
    _REFUSAL_HASH: Final[str] = _h(REFUSAL_PROMPT)
    _JUDGE_HASH: Final[str] = _h(JUDGE_PROMPT)
    PROMPT_VERSION_HASH: Final[str] = _h(_SYNTHESIS_HASH + _REFUSAL_HASH + _JUDGE_HASH)
    ```

    Order matters: the three per-prompt hashes are computed first, then concatenated and re-hashed for the combined `PROMPT_VERSION_HASH`. This means the combined hash is sensitive to which prompts changed (a SYNTHESIS edit produces different `_SYNTHESIS_HASH` AND different `PROMPT_VERSION_HASH`, but `_REFUSAL_HASH` and `_JUDGE_HASH` are unchanged — Phase 11 ablation reports use this localization signal).

    **Line 96-110 — `build_judge_user_prompt(...)` helper:**

    Per RESEARCH §Code Example 1 lines 921-929 (the D-09 migration scaffold the judge migration in Plan 06-06 consumes):

    ```
    def build_judge_user_prompt(prediction: str, reference: list[str], rubric: str = "") -> str:
        """Build the user-side prompt for the judge LLM call (D-09 migration helper).

        Plan 06-06 imports this helper inside `adapters/real/judge.py`, replacing the
        Phase 2 placeholder `_build_judge_prompt` function (which currently lives in
        the same file). The format here is locked-down: indexed reference passages
        ([0], [1], ...) followed by an optional rubric block.
        """
        ref_text = "\n".join(f"[{i}] {r}" for i, r in enumerate(reference))
        rubric_section = f"\nAdditional rubric: {rubric}" if rubric else ""
        return (
            f"Prediction to evaluate:\n{prediction}\n\n"
            f"Reference passages:\n{ref_text}"
            f"{rubric_section}"
        )
    ```

    The function is module-level (not class-scoped); type-annotated; Plan 06-06 imports it.

    Anti-patterns to avoid (per PATTERNS §"### prompts.py" + RESEARCH §Anti-Patterns):
    - Do NOT add a `DOCINTEL_REFUSAL_THRESHOLD` or any prompt-related Settings field — FND-11 single-env-reader rule binds; prompts are module constants, not env vars.
    - Do NOT define `_CHUNK_RE` here — that lives in `parse.py` (Task B). Importing into prompts.py is not necessary; the regex is the GENERATOR's concern, not the prompts' concern.
    - Do NOT add a `pytestmark` or any test code to this file — production code only.
    - Do NOT reformat the body of `SYNTHESIS_PROMPT` or `JUDGE_PROMPT` after writing it — Pitfall 3 (whitespace drift changes the hash). The CRITICAL header comment above the prompts is the documented warning to future contributors.

    Optional: add a `from typing import Final` import re-order to satisfy ruff's `I001` import-sort rule; verify with `uv run ruff check packages/docintel-generate/src/docintel_generate/prompts.py` after writing.
  </action>
  <verify>
    <automated>test -f packages/docintel-generate/src/docintel_generate/prompts.py && uv run python -c "from docintel_generate.prompts import SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT, PROMPT_VERSION_HASH, _SYNTHESIS_HASH, _REFUSAL_HASH, _JUDGE_HASH, _h, build_judge_user_prompt; assert isinstance(SYNTHESIS_PROMPT, str) and len(SYNTHESIS_PROMPT) > 100; assert REFUSAL_PROMPT == 'I cannot answer this question from the retrieved 10-K excerpts.'; assert isinstance(JUDGE_PROMPT, str) and 'unsupported_claims' in JUDGE_PROMPT; assert len(PROMPT_VERSION_HASH) == 12 and all(c in '0123456789abcdef' for c in PROMPT_VERSION_HASH); assert _h('test') == _h('test') and _h('test') != _h('test2'); assert _h(SYNTHESIS_PROMPT) == _SYNTHESIS_HASH; assert _h(REFUSAL_PROMPT) == _REFUSAL_HASH; assert _h(JUDGE_PROMPT) == _JUDGE_HASH; assert '[AAPL-FY2024-Item-1A-018]' in SYNTHESIS_PROMPT; assert '[NVDA-FY2024-Item-7-042]' in SYNTHESIS_PROMPT; assert 'I cannot answer this question from the retrieved 10-K excerpts' in SYNTHESIS_PROMPT" && uv run ruff check packages/docintel-generate/src/docintel_generate/prompts.py && uv run mypy --strict packages/docintel-generate/src/docintel_generate/prompts.py 2>&1 | grep -v "^Success" | grep -E "error" | head -5 ; true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/docintel-generate/src/docintel_generate/prompts.py` exists; first non-blank non-comment line after the module docstring + the CRITICAL header comment is `from __future__ import annotations`.
    - Source assertion: `grep -c "^SYNTHESIS_PROMPT: Final\[str\]" packages/docintel-generate/src/docintel_generate/prompts.py` returns 1; same for `REFUSAL_PROMPT`, `JUDGE_PROMPT`, `_SYNTHESIS_HASH`, `_REFUSAL_HASH`, `_JUDGE_HASH`, `PROMPT_VERSION_HASH`.
    - Source assertion: `grep -c "from docintel_core.types import REFUSAL_TEXT_SENTINEL" packages/docintel-generate/src/docintel_generate/prompts.py` returns 1 (Pitfall 9 cycle-safe import direction).
    - Source assertion: `grep -c "DO NOT reformat the body of these prompts" packages/docintel-generate/src/docintel_generate/prompts.py` returns 1 (Pitfall 3 header comment present).
    - Source assertion: `grep -c "build_judge_user_prompt" packages/docintel-generate/src/docintel_generate/prompts.py` returns at least 1 (helper function present for D-09 migration in Plan 06-06).
    - Behavior assertion: `uv run python -c "from docintel_generate.prompts import SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT, PROMPT_VERSION_HASH; assert REFUSAL_PROMPT == 'I cannot answer this question from the retrieved 10-K excerpts.'; assert '[AAPL-FY2024-Item-1A-018]' in SYNTHESIS_PROMPT; assert '[NVDA-FY2024-Item-7-042]' in SYNTHESIS_PROMPT; assert 'unsupported_claims' in JUDGE_PROMPT; assert len(PROMPT_VERSION_HASH) == 12"` exits 0.
    - Behavior assertion: hash determinism — `uv run python -c "from docintel_generate.prompts import _h, SYNTHESIS_PROMPT, _SYNTHESIS_HASH; assert _h(SYNTHESIS_PROMPT) == _SYNTHESIS_HASH"` exits 0.
    - Behavior assertion: hash sensitivity — `uv run python -c "from docintel_generate.prompts import _h, SYNTHESIS_PROMPT, _SYNTHESIS_HASH; assert _h(SYNTHESIS_PROMPT + 'x') != _SYNTHESIS_HASH"` exits 0 (Pitfall 3 byte-exact behavior).
    - Behavior assertion: `uv run ruff check packages/docintel-generate/src/docintel_generate/prompts.py` exits 0 (no lint violations).
    - Behavior assertion: `uv run mypy --strict packages/docintel-generate/src/docintel_generate/prompts.py` reports 0 errors (Final[str] annotations + hashlib usage all type-correctly).
    - Behavior assertion: `bash scripts/check_prompt_locality.sh` exits 0 — `prompts.py` is in the gate's allowlist, so the inline prompts don't fire the gate.
  </acceptance_criteria>
  <done>`prompts.py` exists with three named Final[str] prompts, four import-time hash constants, the `_h()` helper, the `build_judge_user_prompt()` helper, and the Pitfall 3 critical header comment. SYNTHESIS_PROMPT contains the locked fenced example + refusal sentinel + comparative-question guidance. JUDGE_PROMPT specifies the 4-field JudgeVerdict schema. REFUSAL_PROMPT byte-equals REFUSAL_TEXT_SENTINEL (Pitfall 9 single-source). mypy --strict clean; ruff clean; prompt-locality gate stays green.</done>
</task>

<task type="auto">
  <name>Task B: Create parse.py with _CHUNK_RE + is_refusal helper + extend __init__.py re-exports</name>
  <files>packages/docintel-generate/src/docintel_generate/parse.py, packages/docintel-generate/src/docintel_generate/__init__.py</files>
  <read_first>
    - packages/docintel-core/src/docintel_core/adapters/stub/llm.py:31 (the current `_CHUNK_RE` location; the byte-exact regex `re.compile(r"\[([^\]]+)\]")` Plan 06-03 mirrors)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-generate/src/docintel_generate/parse.py`" lines 200-228 (verbatim deviation notes; the docstring "Phase 9 faithfulness tests rely on the same regex" sentence)
    - .planning/phases/06-generation/06-CONTEXT.md D-12 + D-13 step 5 (sentinel detection + _CHUNK_RE move; the import direction `stub/llm.py → docintel_generate.parse` is the SECOND step of D-12, landed by Plan 06-05 not 06-03; Plan 06-03 only ADDS `_CHUNK_RE` to `parse.py`, leaving the in-stub definition alone)
    - .planning/phases/06-generation/06-RESEARCH.md §Common Pitfalls §Pitfall 9 lines 826-838 (cycle-resolution: sentinel in core, _CHUNK_RE in generate.parse — both options are documented; planner adopts the recommended split)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-generate/src/docintel_generate/__init__.py`" lines 82-122 (Wave-incremental `__all__` build-up; Wave 1 adds the 4 public prompt names)
    - packages/docintel-generate/src/docintel_generate/__init__.py (post-Plan 06-02 — empty `__all__` skeleton)
  </read_first>
  <action>
    Two sub-steps in this task.

    **Sub-step 1 — Create parse.py:**

    Create `packages/docintel-generate/src/docintel_generate/parse.py` with this content (mirroring the existing `_CHUNK_RE` block in `adapters/stub/llm.py:31-37` shape):

    Module docstring naming the module's role: "Pure regex + sentinel helpers for docintel-generate. Single canonical home for the chunk_id extraction regex `_CHUNK_RE` (Plan 06-05 re-imports this into the stub adapter, retiring the in-stub definition). The `is_refusal()` helper provides a single boolean check for the canonical refusal sentinel; Generator (Plan 06-04) and Phase 7 Citation parser both use this helper. No imports outside stdlib + `docintel_core.types`."

    Imports:
    - `from __future__ import annotations`
    - `import re`
    - `from typing import Final`
    - `from docintel_core.types import REFUSAL_TEXT_SENTINEL`

    Module-level constants + helper:
    - `_CHUNK_RE: Final[re.Pattern[str]] = re.compile(r"\[([^\]]+)\]")` — byte-exact mirror of `adapters/stub/llm.py:31` (do not change the pattern). Triple-quoted docstring (4-5 lines) explaining: "Module-level compiled regex for extracting [chunk_id] tokens. Pattern is locked here as the single canonical home (D-12); Plan 06-05 re-imports this into `adapters/stub/llm.py`, retiring the duplicate definition there. Phase 7 Citation parser also imports this regex to enforce single-source consistency across stub + real + parse layers."

    - `def is_refusal(text: str) -> bool:` — helper for Step D refusal detection (D-13 step 5):

      ```
      def is_refusal(text: str) -> bool:
          """Return True if `text` starts with the canonical refusal sentinel.

          D-13 step 5 (Plan 06-04 Generator Step D) consumes this for refusal-flag
          construction. Phase 7 Citation parser consumes it to skip refusal answers
          (no citations expected). Single boolean check; no NLI, no embeddings —
          structural detection of the locked 64-char sentinel string.
          """
          return text.startswith(REFUSAL_TEXT_SENTINEL)
      ```

    No `__all__` block in `parse.py` itself — it's an internal module; the public re-exports happen at `__init__.py`.

    **Sub-step 2 — Extend __init__.py re-exports (Wave 1 build-up):**

    Edit `packages/docintel-generate/src/docintel_generate/__init__.py` (currently Wave 0 empty `__all__: list[str] = []` skeleton).

    Add imports:
    - `from docintel_generate.prompts import JUDGE_PROMPT, PROMPT_VERSION_HASH, REFUSAL_PROMPT, SYNTHESIS_PROMPT`

    Update `__all__` to be alphabetically-sorted (matches the Phase 5 `docintel-retrieve/__init__.py` convention):
    - `__all__ = ["JUDGE_PROMPT", "PROMPT_VERSION_HASH", "REFUSAL_PROMPT", "SYNTHESIS_PROMPT"]`

    The module docstring (written in Plan 06-02) already names Plans 06-03 and 06-04 as the incremental-build plans. Do NOT touch the docstring text — it correctly describes "Plan 06-03 adds SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT, PROMPT_VERSION_HASH re-exports; Plan 06-04 adds Generator, GenerationResult re-exports."

    Do NOT import or re-export `_h`, `_SYNTHESIS_HASH`, `_REFUSAL_HASH`, `_JUDGE_HASH`, `build_judge_user_prompt`, or `_CHUNK_RE` — those are leading-underscore internals; the public surface per D-01 is the four module-level prompt constants only.

    Anti-patterns to avoid (per PATTERNS §"### parse.py" + RESEARCH §Anti-Patterns):
    - Do NOT define `_CHUNK_RE` in BOTH `parse.py` AND a future home. Wave 1 LEAVES the existing `adapters/stub/llm.py:31` definition in place; Plan 06-05 will delete that and add `from docintel_generate.parse import _CHUNK_RE` to the stub. Between Wave 1 and Wave 3, both files have byte-identical `_CHUNK_RE` definitions — this is acceptable because (a) Wave 3 ends Wave 1's borrowed time, (b) the byte-identity is enforceable as a Plan 06-06 verification step if needed.
    - Do NOT alter the regex pattern (`r"\[([^\]]+)\]"` is the locked Phase 2 D-16 pattern; changing it breaks the existing `StubLLMClient.complete` + future Generator citation parsing).
    - Do NOT add `REFUSAL_TEXT_SENTINEL` to `parse.py` as a re-export — that would create THREE potential homes (core, parse, prompts) for the same string. The single home is `docintel_core.types`; everything else imports from there.
    - Do NOT import `docintel_generate.generator` or `docintel_generate.parse` from `__init__.py` yet — Plan 06-04 ships `generator.py` and extends `__init__.py` accordingly. Plan 06-03 ONLY adds the four prompt re-exports.
  </action>
  <verify>
    <automated>test -f packages/docintel-generate/src/docintel_generate/parse.py && uv run python -c "from docintel_generate.parse import _CHUNK_RE, is_refusal; from docintel_core.types import REFUSAL_TEXT_SENTINEL; assert _CHUNK_RE.findall('[AAPL-FY2024-Item-1A-007] and [NVDA-FY2024-Item-7-042].') == ['AAPL-FY2024-Item-1A-007', 'NVDA-FY2024-Item-7-042']; assert is_refusal(REFUSAL_TEXT_SENTINEL + ' suffix') is True; assert is_refusal('Apple grew rev.') is False" && uv run python -c "import docintel_generate; assert set(docintel_generate.__all__) == {'JUDGE_PROMPT', 'PROMPT_VERSION_HASH', 'REFUSAL_PROMPT', 'SYNTHESIS_PROMPT'}; from docintel_generate import SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT, PROMPT_VERSION_HASH; assert len(PROMPT_VERSION_HASH) == 12" && uv run pytest tests/test_prompt_version_hash.py -x 2>&1 | tail -5 && bash scripts/check_prompt_locality.sh && uv run mypy --strict packages/docintel-generate/src/docintel_generate/parse.py packages/docintel-generate/src/docintel_generate/__init__.py 2>&1 | grep -v "Success" | grep -E "error" | head -3 ; true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/docintel-generate/src/docintel_generate/parse.py` exists; contains `_CHUNK_RE: Final[re.Pattern[str]]` and `def is_refusal(text: str) -> bool`.
    - Source assertion: `grep -c "from docintel_core.types import REFUSAL_TEXT_SENTINEL" packages/docintel-generate/src/docintel_generate/parse.py` returns 1.
    - Source assertion: the regex pattern is byte-exact `r"\[([^\]]+)\]"` — `grep -F 'r"\[([^\]]+)\]"' packages/docintel-generate/src/docintel_generate/parse.py` exits 0.
    - Source assertion: `packages/docintel-generate/src/docintel_generate/__init__.py` `__all__` contains exactly `["JUDGE_PROMPT", "PROMPT_VERSION_HASH", "REFUSAL_PROMPT", "SYNTHESIS_PROMPT"]` (alphabetical; matches the analog Phase 5 `docintel-retrieve/__init__.py`'s convention).
    - Behavior assertion: `uv run python -c "from docintel_generate.parse import _CHUNK_RE; assert _CHUNK_RE.findall('[X] and [Y].') == ['X', 'Y']"` exits 0 (chunk-id extraction).
    - Behavior assertion: `uv run python -c "from docintel_generate.parse import is_refusal; from docintel_core.types import REFUSAL_TEXT_SENTINEL; assert is_refusal(REFUSAL_TEXT_SENTINEL) is True; assert is_refusal('answered fully') is False"` exits 0 (sentinel detection).
    - Behavior assertion: `uv run python -c "from docintel_generate import SYNTHESIS_PROMPT, REFUSAL_PROMPT, JUDGE_PROMPT, PROMPT_VERSION_HASH"` exits 0 (the four public names are re-exported from the package root).
    - Behavior assertion: `uv run pytest tests/test_prompt_version_hash.py -x` PASSES — the three Plan 06-01 xfail tests `test_hash_format`, `test_per_prompt_hashes_exposed`, `test_hash_sensitivity` now report `xpassed` (xfail-strict means xpass would FAIL the test; for Wave 1 they should now actually pass without the xfail decorator firing). NOTE: the test functions still have `@pytest.mark.xfail(strict=True, ...)` markers — under xfail-strict, xpass IS a failure. The CORRECT outcome for Wave 1 is: the xfail markers stay, the underlying behavior now passes, so pytest reports XPASS as a FAILURE. The fix: Plan 06-07's xfail-removal sweep will delete the xfail markers. **Wave 1 acceptance is therefore: the test BODY passes (test_hash_format/test_per_prompt_hashes_exposed/test_hash_sensitivity all execute without AssertionError), and the test is reported as XPASS-failure under strict mode** — which is the expected Wave 1 state (Plan 06-07 sweeps).
    - Concrete CI check: `uv run pytest tests/test_prompt_version_hash.py -ra -q 2>&1 | grep -E "XPASS|3 passed|3 xpassed"` succeeds — either the markers are still present (XPASS reported) or have been swept (3 passed). Both shape Wave 1 differently; either is acceptable at this commit. Plan 06-07 ensures the marker sweep happens.
    - Behavior assertion: `bash scripts/check_prompt_locality.sh` exits 0 — `parse.py` is in the allowlist; the new module does not trip the gate.
    - Behavior assertion: `uv run mypy --strict packages/docintel-generate/src/docintel_generate/parse.py packages/docintel-generate/src/docintel_generate/__init__.py` reports 0 errors.
  </acceptance_criteria>
  <done>`parse.py` exists with the canonical `_CHUNK_RE` regex and the `is_refusal()` helper. The Wave 1 `__init__.py` re-exports the four public prompt names. Plan 06-01's three GEN-02 tests for prompt-version hash now run without AssertionError (Plan 06-07 sweeps the now-unneeded xfail markers). Plan 06-05 (Wave 3) will delete the duplicate `_CHUNK_RE` from `adapters/stub/llm.py` and add the re-import. Plan 06-04 (Wave 2) imports `_CHUNK_RE` and `is_refusal` from `parse.py` for the Generator's Step D citation parsing + refusal detection.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `uv run python -c "from docintel_generate.prompts import PROMPT_VERSION_HASH, _SYNTHESIS_HASH, _REFUSAL_HASH, _JUDGE_HASH, _h; assert _h(_SYNTHESIS_HASH + _REFUSAL_HASH + _JUDGE_HASH) == PROMPT_VERSION_HASH"` exits 0 (the combined hash is computed correctly from the per-prompt hashes).
2. `uv run python -c "from docintel_generate.parse import _CHUNK_RE; from docintel_core.adapters.stub.llm import _CHUNK_RE as _STUB_CRE; assert _CHUNK_RE.pattern == _STUB_CRE.pattern"` exits 0 (the duplicate-definition byte-identity check; Plan 06-05 retires the stub-side definition).
3. `bash scripts/check_prompt_locality.sh` exits 0 (canonical-layout gate stays green; prompts.py + parse.py are allowlisted; no new inline prompts leaked into adjacent files).
4. `bash scripts/check_adapter_wraps.sh` exits 0 (Phase 2 wrap gate stays green; no new adapter-real files added).
5. `uv run pytest -ra -q -m "not real"` exits 0 (all 3 GEN-02 tests now either xpass-fail under strict OR pass-clean after sweep; either is the expected Wave 1 state).
6. `uv run mypy --strict packages/docintel-generate/` exits 0 (the full new package is type-clean).
</verification>

<success_criteria>
Wave 1 ships the GEN-01 + GEN-02 canonical prompt module:
- `prompts.py` exists with three Final[str] prompts + per-prompt + combined hashes computed at import time
- `parse.py` exists with the canonical `_CHUNK_RE` + `is_refusal()` helper
- `__init__.py` re-exports the four public prompt names
- Plan 06-01's three GEN-02 tests execute their bodies without error (the xfail-strict markers will be swept by Plan 06-07)
- mypy --strict clean; ruff clean; both grep gates still green
- Pitfall 9 fully resolved (sentinel in core; prompts.py + parse.py both import FROM core, never reverse)
</success_criteria>

<output>
After completion, create `.planning/phases/06-generation/06-03-SUMMARY.md` documenting:
- The actual byte-length of `SYNTHESIS_PROMPT` and `JUDGE_PROMPT` (informational)
- The computed `PROMPT_VERSION_HASH` value at Wave 1 commit time (informational; will change again at Wave 3 when JUDGE_PROMPT migration completes — flag this so the Wave 3 commit log can note the hash transition)
- Confirmation that `prompts.py` AND `parse.py` are in the prompt-locality gate's allowlist (verified by `bash scripts/check_prompt_locality.sh` exit 0)
- Confirmation that `adapters/stub/llm.py:31` still contains the duplicate `_CHUNK_RE` definition (Plan 06-05 retires it) — informational
</output>
