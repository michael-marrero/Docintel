---
phase: 06-generation
plan: 06
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - packages/docintel-core/src/docintel_core/adapters/real/judge.py
autonomous: true
requirements: [GEN-02]
must_haves:
  truths:
    - "The Phase 2 placeholder `_JUDGE_SYSTEM_PROMPT` + `_build_judge_prompt` + `_SCORE_PATTERN` + `_parse_judge_response` are REMOVED from `adapters/real/judge.py`."
    - "`adapters/real/judge.py` imports `JUDGE_PROMPT` + `build_judge_user_prompt` from `docintel_generate.prompts` — the canonical D-09 prompt + helper home."
    - "`CrossFamilyJudge.judge(prediction, reference, rubric)` body dispatches on `isinstance(self._llm, AnthropicAdapter | OpenAIAdapter)` and calls `_judge_via_anthropic(...)` or `_judge_via_openai(...)` — two new module-level helpers that use provider-native structured output (Anthropic `tools=[{strict: true}]` + `tool_choice` for Claude; OpenAI `response_format={\"type\": \"json_schema\", \"strict\": true}` for GPT) to deserialize directly into `JudgeVerdict`."
    - "Both helper functions are `@retry`-wrapped with the SAME tenacity pattern as `adapters/real/llm_anthropic.py:102-108` AND `adapters/real/llm_openai.py:102-108` (mirror the wait/stop/retry-if-exception-type/before_sleep_log args verbatim) — this preserves `scripts/check_adapter_wraps.sh` exit 0 by ensuring the new SDK call sites stay within the wrap-gate contract (PATTERNS.md §`### adapters/real/judge.py` CD-04 / ADP-06 preservation note)."
    - "Deserialization failures (Pydantic `ValidationError`, missing tool_use block, malformed JSON content) emit a `judge_structured_output_invalid` structlog WARNING and return a sentinel `JudgeVerdict(score=0.0, passed=False, reasoning=\"<deserialization failed>\", unsupported_claims=[])` — NEVER raise (per Pitfall 6 + Pattern 3 anti-pattern flag)."
    - "Phase 2 D-04 cross-family wiring is UNTOUCHED — `CrossFamilyJudge.__init__`, the `Settings.llm_real_provider` complement-selection in `make_adapters` factory, the `.name` property, and the degraded-mode detection block all stay as-is."
    - "`bash scripts/check_adapter_wraps.sh` exits 0 — the new helper functions contain `.messages.create` (Anthropic) and `chat.completions.create` (OpenAI) SDK call patterns; both are matched by the existing wrap-gate; the file ALREADY has `from tenacity import retry` (Phase 2 D-18) so the gate is satisfied."
    - "PROMPT_VERSION_HASH changes on this commit (per Pitfall 8) because the JUDGE_PROMPT body changes from the Phase 2 placeholder (lines 44-49 of judge.py) to the Plan 06-03 canonical body in `docintel_generate.prompts`. This is the EXPECTED behavior — Phase 9/10 eval reports written after this commit will carry the NEW hash; any reports written between Wave 1 and Wave 3 (none expected per ROADMAP sequencing) would have carried the Wave 1 hash."
  artifacts:
    - path: "packages/docintel-core/src/docintel_core/adapters/real/judge.py"
      provides: "Migrated judge adapter — removes Phase 2 placeholder prompt+parser; imports JUDGE_PROMPT + build_judge_user_prompt from docintel_generate.prompts; replaces heuristic regex parser with provider-native structured output (Anthropic tools / OpenAI response_format); adds _judge_via_anthropic + _judge_via_openai @retry-wrapped helpers with JudgeVerdict sentinel-on-deserialization-failure; preserves Phase 2 D-04 cross-family wiring untouched"
  key_links:
    - from: "CrossFamilyJudge.judge"
      to: "docintel_generate.prompts.JUDGE_PROMPT + build_judge_user_prompt"
      via: "from docintel_generate.prompts import JUDGE_PROMPT, build_judge_user_prompt"
      pattern: "from docintel_generate\\.prompts import"
    - from: "_judge_via_anthropic"
      to: "anthropic.messages.create with tools=[{strict: true}] + tool_choice={'type': 'tool', 'name': 'submit_verdict'}"
      via: "client.messages.create(..., tools=[...], tool_choice={...})"
      pattern: "tools=\\[\\{.*submit_verdict"
    - from: "_judge_via_openai"
      to: "openai.chat.completions.create with response_format={'type': 'json_schema', 'json_schema': {'strict': true, 'schema': _JUDGE_VERDICT_SCHEMA}}"
      via: "client.chat.completions.create(..., response_format={...})"
      pattern: "response_format.*json_schema.*strict"
---

<objective>
Migrate the Phase 2 placeholder judge prompt + heuristic regex parser in `adapters/real/judge.py` to: (a) the canonical `JUDGE_PROMPT` body now living in `docintel_generate.prompts` (Plan 06-03), and (b) provider-native structured output that deserializes the LLM's response DIRECTLY into `JudgeVerdict` — eliminating the heuristic `_SCORE_PATTERN` regex failure mode that Phase 2 explicitly flagged for Phase 6 replacement.

Three structural facts get locked:
1. The Phase 2 D-04 cross-family wiring (`CrossFamilyJudge` class + `Settings.llm_real_provider` complement-selection in `make_adapters`) is the contract; Phase 6 D-09 says "prompt + parser, NOT dispatch". The factory wiring stays as-is.
2. The new helper functions `_judge_via_anthropic` + `_judge_via_openai` are `@retry`-wrapped with the SAME tenacity pattern as the existing real adapters — preserves `scripts/check_adapter_wraps.sh` exit 0; preserves the no-silent-retries discipline. PATTERNS.md explicitly recommends this approach over extending the `LLMClient.complete` protocol with structured-output kwargs.
3. Deserialization failures are MEASUREMENTS, not retry triggers — Pitfall 6 explicitly warns about nested retry-storm if a `ValidationError` bubbles out and a caller wraps the whole `judge()` flow. The sentinel `JudgeVerdict(score=0.0, passed=False, reasoning="<deserialization failed>", ...)` makes the failure visible in Phase 9 eval reports without crashing the run.

After this plan, `PROMPT_VERSION_HASH` stabilizes at its post-Wave-3 value. Phase 9/10 eval reports written after this commit carry the new hash; the manifest header propagates it.

Output: 1 modified file — `adapters/real/judge.py`. Plan 06-01's `tests/test_judge_structured_output.py` test bodies execute (the `@pytest.mark.real` test verifies real-mode structured output; the stub-mockable test verifies the sentinel-on-failure path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adapters-protocols/02-CONTEXT.md
@.planning/phases/06-generation/06-CONTEXT.md
@.planning/phases/06-generation/06-RESEARCH.md
@.planning/phases/06-generation/06-PATTERNS.md
@.planning/phases/06-generation/06-VALIDATION.md
@.planning/phases/06-generation/06-03-SUMMARY.md
@.planning/phases/06-generation/06-04-SUMMARY.md

<interfaces>
<!-- Current state of adapters/real/judge.py (190 lines) -->

From `packages/docintel-core/src/docintel_core/adapters/real/judge.py`:

Lines 1-21 — module docstring describing the cross-family delegation pattern + the tenacity-delegate-to-self._llm rationale.

Lines 22-42 — imports + module-level loggers:
- `from __future__ import annotations` + `import logging` + `import re` (used ONLY for `_SCORE_PATTERN` regex; this import is REMOVED in Plan 06-06 once the regex is retired) + `import structlog`.
- `from tenacity import retry  # noqa: F401 — imported for ADP-06 gate; retry delegated to self._llm` — Phase 2 D-18 wrap-gate marker; KEEP as-is.
- `from docintel_core.adapters.protocols import LLMClient` + `from docintel_core.adapters.types import JudgeVerdict` + `from docintel_core.config import Settings` — all KEEP as-is.
- `_retry_log = logging.getLogger(__name__)` + `log = structlog.stdlib.get_logger(__name__)` — KEEP as-is (the two-logger SP-3 pattern is retained here because Plan 06-06 ADDS @retry-wrapped helpers).

Lines 44-49 — `_JUDGE_SYSTEM_PROMPT` constant — **REMOVED** by Plan 06-06 (the canonical body is now `docintel_generate.prompts.JUDGE_PROMPT`).

Line 52 — `_SCORE_PATTERN = re.compile(r"score\s*:\s*([0-9]*\.?[0-9]+)", re.IGNORECASE)` — **REMOVED** by Plan 06-06 (replaced by structured-output deserialization).

Lines 55-73 — `_build_judge_prompt(prediction, reference, rubric)` function — **REMOVED** by Plan 06-06 (replaced by `from docintel_generate.prompts import build_judge_user_prompt`).

Lines 76-97 — `_parse_judge_response(text)` function — **REMOVED** by Plan 06-06 (replaced by inline structured-output deserialization inside `_judge_via_anthropic` / `_judge_via_openai`).

Lines 100-160 — `CrossFamilyJudge` class — PRESERVED. The class shell (`__init__`, the degraded-mode detection block, the `.name` property) is the Phase 2 D-04 contract; Plan 06-06 does NOT touch these.

Lines 162-190 — `.judge(prediction, reference, rubric) -> JudgeVerdict` method — Plan 06-06 MODIFIES the BODY of this method. The signature stays; the implementation changes from "build prompt + call self._llm.complete + parse with _SCORE_PATTERN" to "build prompt + dispatch by isinstance + return structured JudgeVerdict".

<!-- Source-of-truth canonical body (Plan 06-03 ships) -->

From `packages/docintel-generate/src/docintel_generate/prompts.py` (post-Plan 06-03):
- `JUDGE_PROMPT: Final[str]` — the canonical judge body specifying the 4-field `JudgeVerdict` schema; per CD-02 wording locked in RESEARCH §Code Example 1 lines 897-912.
- `build_judge_user_prompt(prediction: str, reference: list[str], rubric: str = "") -> str` — the user-prompt-builder helper; per RESEARCH §Code Example 1 lines 921-929. Plan 06-06 imports BOTH.

<!-- Existing analog: AnthropicAdapter.complete @retry pattern -->

From `packages/docintel-core/src/docintel_core/adapters/real/llm_anthropic.py:102-138`:

```
@retry(
    wait=wait_exponential(multiplier=1, min=1, max=20),
    stop=stop_after_attempt(5),
    retry=retry_if_exception_type((RateLimitError, APIConnectionError, APITimeoutError)),
    before_sleep=before_sleep_log(_retry_log, logging.WARNING),
    reraise=True,
)
def complete(
    self,
    prompt: str,
    system: str | None = None,
) -> CompletionResponse:
    """..."""
    client = self._get_client()
    t0 = time.perf_counter()
    response = client.messages.create(
        model=self._model,
        max_tokens=_MAX_TOKENS,
        system=system or "You are a helpful assistant.",
        messages=[{"role": "user", "content": prompt}],
    )
    ...
```

The `@retry(...)` decorator block is the structural template Plan 06-06's `_judge_via_anthropic` and `_judge_via_openai` mirror. The exact `wait/stop/retry/before_sleep/reraise` argument shape is byte-mirrored to satisfy the `check_adapter_wraps.sh` gate (which scans for `from tenacity import` + the SDK call patterns; the helpers contain BOTH).

<!-- D-09 + CD-09 spec — verbatim from CONTEXT.md -->

D-09: "Move `_JUDGE_SYSTEM_PROMPT` + the body of `_build_judge_prompt(...)` into `prompts.py` as `JUDGE_PROMPT` + a small `build_judge_user_prompt(...)` helper alongside. **Replace** the heuristic `_SCORE_PATTERN` regex parser with structured JSON-mode output: Anthropic calls use `tools=[{...}]` to bind the response to a JSON schema (`{\"score\": float, \"passed\": bool, \"reasoning\": str, \"unsupported_claims\": list[str]}`); OpenAI calls use `response_format={\"type\": \"json_schema\", ...}`. Both adapter paths deserialize directly into `JudgeVerdict`."

CD-09: "Anthropic `tools=[{\"name\": \"submit_verdict\", \"input_schema\": {...}}]` vs newer `output_config={\"format\": {...}}`. RESEARCH recommends the older + more-stable path (`tools=[{strict: true}]` + `tool_choice={\"type\": \"tool\", \"name\": \"submit_verdict\"}`). OpenAI `response_format={\"type\": \"json_schema\", \"json_schema\": {\"name\": \"submit_verdict\", \"strict\": true, \"schema\": {...}}}` — stable path. Both pins (`anthropic==0.101.0`, `openai==2.36.0`) verified to support these APIs (RESEARCH §Standard Stack Version verification)."

<!-- Pitfall 6 — deserialization failure must NOT raise -->

RESEARCH Pitfall 6 (lines 790-800): "Structured-output deserialization failure cascading into tenacity retry storm" — Pydantic `ValidationError` bubbling out + caller retry can produce 5×N calls per failure. **Mitigation:** on `pydantic.ValidationError` from the structured-output deserialization, log `judge_structured_output_invalid` structlog warning and return a sentinel `JudgeVerdict(score=0.0, passed=False, reasoning="<deserialization failed>", unsupported_claims=[])` — NEVER raise. Failures are MEASUREMENTS, not retry triggers.

The @retry decorator handles TRANSIENT failures (`RateLimitError`, `APIConnectionError`, `APITimeoutError`) — those are retry triggers. `ValidationError` is NOT a transient failure; it's a model-behavior signal, and Phase 9's eval pipeline measures it.

<!-- PATTERNS.md verbatim deviation notes -->

From PATTERNS.md §"### `packages/docintel-core/src/docintel_core/adapters/real/judge.py`" lines 520-634:
- Lines to REMOVE (verbatim line numbers from current file): line 25 (`import re`), lines 44-49 (`_JUDGE_SYSTEM_PROMPT`), line 52 (`_SCORE_PATTERN`), lines 55-73 (`_build_judge_prompt`), lines 76-97 (`_parse_judge_response`).
- Lines to PRESERVE: lines 1-43 (docstring + imports + loggers + tenacity F401 + structural CrossFamilyJudge imports); lines 100-160 (CrossFamilyJudge class shell).
- Lines to MODIFY: lines 162-190 (the `.judge()` method body) — full rewrite per RESEARCH Pattern 3.

The CD-04 / ADP-06 preservation note (PATTERNS.md lines 626-628): "the two helpers MUST be wrapped with the same `@retry(...)` decorator pattern from `llm_anthropic.py:102-108`. Without the wrap, `scripts/check_adapter_wraps.sh` will flag `judge.py` as missing tenacity coverage (it has the import for ADP-06 but the new helpers don't have `@retry`). Planner confirms during execution."

The grep gate scans for `.messages.create` / `chat.completions.create` / `.encode(` / `.predict(` patterns AND for `from tenacity import` import. Since `from tenacity import retry` is ALREADY present at line 28 of judge.py (Phase 2 D-18 marker), the file ALREADY satisfies the gate even without the helpers wrapping. **But** the planner-recommended discipline (PATTERNS.md note) is to wrap the helpers anyway — for two reasons: (a) the helpers genuinely benefit from retry on transient errors, (b) future-proofing if the gate ever scans for `@retry` proximity to SDK calls (it doesn't today, but the consistency makes it harder to regress).

**Decision (planner picks per PATTERNS.md):** Wrap BOTH helpers with the same `@retry(...)` decorator block as `llm_anthropic.py:102-108`. The decorator targets transient SDK errors only (`RateLimitError`, `APIConnectionError`, `APITimeoutError`); `ValidationError` is caught INSIDE the function body BEFORE @retry would see it (the catch happens after the SDK call returns, so retry is not triggered).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Migrate judge.py — remove Phase 2 placeholder + add structured-output dispatch helpers</name>
  <files>packages/docintel-core/src/docintel_core/adapters/real/judge.py</files>
  <read_first>
    - packages/docintel-core/src/docintel_core/adapters/real/judge.py (current full 190-line file — for line-by-line edit map: lines 22-42 imports + loggers, lines 44-49 _JUDGE_SYSTEM_PROMPT, line 52 _SCORE_PATTERN, lines 55-73 _build_judge_prompt, lines 76-97 _parse_judge_response, lines 100-160 CrossFamilyJudge class shell — preserve, lines 162-190 .judge() method body — REWRITE)
    - packages/docintel-core/src/docintel_core/adapters/real/llm_anthropic.py (lines 102-138 — the @retry pattern + `client.messages.create` call shape Plan 06-06's `_judge_via_anthropic` mirrors; lines 85-95 — `_get_client()` accessor pattern)
    - packages/docintel-core/src/docintel_core/adapters/real/llm_openai.py (lines 102-138 — the analog @retry + `client.chat.completions.create` call shape Plan 06-06's `_judge_via_openai` mirrors)
    - .planning/phases/06-generation/06-RESEARCH.md §Code Examples §Pattern 3 lines 501-583 (verbatim _judge_via_anthropic + _judge_via_openai body shape; the _JUDGE_VERDICT_SCHEMA dict at lines 519-528; the response.content tool_use block extraction; the response.choices[0].message.content JSON parsing)
    - .planning/phases/06-generation/06-PATTERNS.md §"### `packages/docintel-core/src/docintel_core/adapters/real/judge.py`" lines 520-634 (verbatim deviation notes; PRESERVE / REMOVE / MODIFY line-by-line map; the CD-04 wrap-discipline note)
    - .planning/phases/06-generation/06-RESEARCH.md §Common Pitfalls §Pitfall 6 lines 790-800 (no double-wrap + ValidationError sentinel-not-raise rule)
    - .planning/phases/06-generation/06-RESEARCH.md §Common Pitfalls §Pitfall 8 lines 814-824 (PROMPT_VERSION_HASH changes mid-Phase — documented; expected; not a bug)
    - .planning/phases/06-generation/06-CONTEXT.md D-09, CD-09 (the contracts)
    - packages/docintel-generate/src/docintel_generate/prompts.py (post-Plan 06-03 — verify JUDGE_PROMPT and build_judge_user_prompt are importable)
  </read_first>
  <action>
    Edit `packages/docintel-core/src/docintel_core/adapters/real/judge.py` (the only file in this plan). The edit set: REMOVE 5 line-blocks; ADD ~4 line-blocks; MODIFY the `.judge()` method body.

    **Edit 1 — Imports block (lines 22-42 of current file):**

    Existing import block (KEEP most lines):
    ```
    from __future__ import annotations

    import logging
    import re                                  # REMOVE THIS LINE
    import structlog
    from tenacity import retry  # noqa: F401 — imported for ADP-06 gate; retry delegated to self._llm  # MODIFY THIS COMMENT

    from docintel_core.adapters.protocols import LLMClient
    from docintel_core.adapters.types import JudgeVerdict
    from docintel_core.config import Settings
    ```

    Updates:
    - REMOVE `import re` (no longer needed; `_SCORE_PATTERN` is gone).
    - REMOVE the `# noqa: F401` part of the tenacity import — Plan 06-06 ACTUALLY USES `@retry` on the new helpers (not delegated anymore). Replace with:
      ```
      from tenacity import (
          retry,
          retry_if_exception_type,
          stop_after_attempt,
          wait_exponential,
      )
      from tenacity.before_sleep import before_sleep_log
      ```
      Verify `before_sleep_log` import path matches `llm_anthropic.py:25-31` and `llm_openai.py:25-31` exactly.
    - ADD `import json` (for parsing the OpenAI `response_format` JSON string).
    - ADD `import time` (used by `_judge_via_anthropic` / `_judge_via_openai` if timing is added; optional — RESEARCH Pattern 3 example doesn't time the call, but adding it is consistent with the analog adapters' pattern).
    - ADD `from docintel_generate.prompts import JUDGE_PROMPT, build_judge_user_prompt` — the D-09 migration imports.
    - ADD provider-error imports for the `@retry` decorator's `retry_if_exception_type`:
      ```
      from anthropic import APIConnectionError as AnthropicAPIConnectionError, APITimeoutError as AnthropicAPITimeoutError, RateLimitError as AnthropicRateLimitError
      from openai import APIConnectionError as OpenAIAPIConnectionError, APITimeoutError as OpenAIAPITimeoutError, RateLimitError as OpenAIRateLimitError
      ```
      Use the SAME alias scheme as `llm_anthropic.py:32-36` and `llm_openai.py:32-36` to avoid naming collisions (both SDKs define classes with the same name).

    The two-logger pattern (`_retry_log` + `log`) STAYS — both are now USED (the @retry decorator's `before_sleep_log` consumes `_retry_log`; the failure-path structured warning consumes `log`).

    **Edit 2 — Remove Phase 2 placeholder constants + helpers (lines 44-97 of current file):**

    DELETE:
    - Lines 44-49: `_JUDGE_SYSTEM_PROMPT = (...)` constant block.
    - Line 52: `_SCORE_PATTERN = re.compile(...)`.
    - Lines 55-73: `def _build_judge_prompt(...) -> str:` function (and its docstring).
    - Lines 76-97: `def _parse_judge_response(...) -> JudgeVerdict:` function (and its docstring).

    The CrossFamilyJudge class (lines 100-160) immediately follows the imports + module-level docstring. There is a small gap between the imports/loggers (lines 22-42) and the class (line 100); replace that gap with the new module-level helpers (next edit).

    **Edit 3 — Add module-level helpers (after the imports + loggers, BEFORE CrossFamilyJudge class):**

    Add these in order:

    1. **`_JUDGE_VERDICT_SCHEMA` dict constant** (the 4-field `JudgeVerdict` JSON schema both SDKs consume) — per RESEARCH Pattern 3 lines 519-528:

       ```
       _JUDGE_VERDICT_SCHEMA = {
           "type": "object",
           "additionalProperties": False,
           "properties": {
               "score": {"type": "number", "minimum": 0.0, "maximum": 1.0},
               "passed": {"type": "boolean"},
               "reasoning": {"type": "string"},
               "unsupported_claims": {"type": "array", "items": {"type": "string"}},
           },
           "required": ["score", "passed", "reasoning", "unsupported_claims"],
       }
       ```

       Add a one-paragraph docstring/comment: "Locked schema for `JudgeVerdict` structured-output. Both Anthropic (`tools=[{strict: true}]`) and OpenAI (`response_format={'json_schema': {'strict': true}}`) consume this. `additionalProperties: false` AND all four fields in `required` are mandatory for both providers' strict mode."

    2. **`_sentinel_judgeverdict()` helper** (Pitfall 6 — return this on deserialization failure):

       ```
       def _sentinel_judgeverdict() -> JudgeVerdict:
           """Pitfall 6: return on deserialization failure — never raise.

           Phase 9 eval pipeline counts these as zero-score judgments. The structlog
           warning `judge_structured_output_invalid` is the canary; if eval reports
           show consistent sentinel verdicts, investigate model behavior (not retries).
           """
           return JudgeVerdict(
               score=0.0,
               passed=False,
               reasoning="<deserialization failed>",
               unsupported_claims=[],
           )
       ```

    3. **`_judge_via_anthropic(client, user_prompt) -> JudgeVerdict`** — Anthropic structured-output helper per RESEARCH Pattern 3 lines 532-551:

       Decorate with the SAME @retry block as `llm_anthropic.py:102-108`:
       ```
       @retry(
           wait=wait_exponential(multiplier=1, min=1, max=20),
           stop=stop_after_attempt(5),
           retry=retry_if_exception_type((AnthropicRateLimitError, AnthropicAPIConnectionError, AnthropicAPITimeoutError)),
           before_sleep=before_sleep_log(_retry_log, logging.WARNING),
           reraise=True,
       )
       def _judge_via_anthropic(client, user_prompt: str) -> JudgeVerdict:
           """Anthropic structured-output judge call (D-09 + CD-09).

           Uses tools=[{strict: true}] + tool_choice={'type': 'tool', 'name': 'submit_verdict'}
           to bind the response to _JUDGE_VERDICT_SCHEMA. Per Pitfall 1, this is the
           stable path (vs newer output_config); per Pitfall 6, deserialization failure
           emits a structlog warning and returns _sentinel_judgeverdict() — never raises.
           """
           response = client.messages.create(
               model="claude-sonnet-4-6",  # match the AnthropicAdapter default
               max_tokens=2048,
               system=JUDGE_PROMPT,
               messages=[{"role": "user", "content": user_prompt}],
               tools=[{
                   "name": "submit_verdict",
                   "description": "Submit the faithfulness verdict for the prediction.",
                   "input_schema": _JUDGE_VERDICT_SCHEMA,
                   "strict": True,
               }],
               tool_choice={"type": "tool", "name": "submit_verdict"},
           )
           # response.content is a list of blocks; tool_use block carries .input
           for block in response.content:
               if getattr(block, "type", None) == "tool_use" and block.name == "submit_verdict":
                   try:
                       return JudgeVerdict(**block.input)
                   except Exception as exc:  # noqa: BLE001 — Pydantic ValidationError + any malformed shape
                       log.warning(
                           "judge_structured_output_invalid",
                           provider="anthropic",
                           error=str(exc),
                           input=str(block.input)[:200],
                       )
                       return _sentinel_judgeverdict()
           # No tool_use block in response — also a deserialization failure
           log.warning(
               "judge_structured_output_invalid",
               provider="anthropic",
               error="no tool_use block in response",
           )
           return _sentinel_judgeverdict()
       ```

       The model name `"claude-sonnet-4-6"` should be verified against `llm_anthropic.py`'s default model — if the existing adapter uses a different model identifier or reads the model from `Settings`, match that exactly so the judge and the generator use the SAME model family by default. Read `llm_anthropic.py` to confirm; if there's a `_DEFAULT_MODEL` constant or similar, import + reference it instead of hardcoding.

    4. **`_judge_via_openai(client, user_prompt) -> JudgeVerdict`** — OpenAI structured-output helper per RESEARCH Pattern 3 lines 553-581:

       Same @retry shape, with the OpenAI exception types:
       ```
       @retry(
           wait=wait_exponential(multiplier=1, min=1, max=20),
           stop=stop_after_attempt(5),
           retry=retry_if_exception_type((OpenAIRateLimitError, OpenAIAPIConnectionError, OpenAIAPITimeoutError)),
           before_sleep=before_sleep_log(_retry_log, logging.WARNING),
           reraise=True,
       )
       def _judge_via_openai(client, user_prompt: str) -> JudgeVerdict:
           """OpenAI structured-output judge call (D-09 + CD-09).

           Uses response_format={'type': 'json_schema', 'json_schema': {'strict': true, ...}}
           (the stable path per Pitfall 1; avoids .beta.parse() instability per RESEARCH
           Sources tertiary issue #1733/#1763). Per Pitfall 6, deserialization failure
           emits structlog warning + sentinel.
           """
           response = client.chat.completions.create(
               model="gpt-4o",  # match the OpenAIAdapter default; floating alias OK for Phase 6 per RESEARCH Open Question 3
               max_tokens=2048,
               messages=[
                   {"role": "system", "content": JUDGE_PROMPT},
                   {"role": "user", "content": user_prompt},
               ],
               response_format={
                   "type": "json_schema",
                   "json_schema": {
                       "name": "submit_verdict",
                       "strict": True,
                       "schema": _JUDGE_VERDICT_SCHEMA,
                   },
               },
           )
           content = response.choices[0].message.content
           if content is None:
               log.warning(
                   "judge_structured_output_invalid",
                   provider="openai",
                   error="response.choices[0].message.content is None",
               )
               return _sentinel_judgeverdict()
           try:
               payload = json.loads(content)
               return JudgeVerdict(**payload)
           except Exception as exc:  # noqa: BLE001 — JSON decode + Pydantic ValidationError
               log.warning(
                   "judge_structured_output_invalid",
                   provider="openai",
                   error=str(exc),
                   content=content[:200],
               )
               return _sentinel_judgeverdict()
       ```

       Same model-name caveat — verify `llm_openai.py`'s default model.

    **Edit 4 — Modify CrossFamilyJudge.judge() method body (lines 162-190 of current file):**

    Preserve the signature: `def judge(self, prediction: str, reference: list[str], rubric: str = "") -> JudgeVerdict:`.

    Replace the body. Old (current lines 188-190):
    ```
    prompt = _build_judge_prompt(prediction, reference, rubric)
    response = self._llm.complete(prompt, system=_JUDGE_SYSTEM_PROMPT)
    return _parse_judge_response(response.text)
    ```

    New (per RESEARCH Pattern 3 lines 603-621):
    ```
    user_prompt = build_judge_user_prompt(prediction, reference, rubric)

    # Phase 2 D-04 cross-family wiring is preserved at the factory level
    # (make_adapters); inside .judge() we detect the underlying adapter family
    # and dispatch to the matching structured-output helper.
    # Lazy imports to avoid potential circular core→real→core cycles at module load.
    from docintel_core.adapters.real.llm_anthropic import AnthropicAdapter
    from docintel_core.adapters.real.llm_openai import OpenAIAdapter

    if isinstance(self._llm, AnthropicAdapter):
        return _judge_via_anthropic(self._llm._get_client(), user_prompt)
    elif isinstance(self._llm, OpenAIAdapter):
        return _judge_via_openai(self._llm._get_client(), user_prompt)
    else:
        raise TypeError(
            f"unsupported judge adapter type: {type(self._llm).__name__}; "
            f"expected AnthropicAdapter or OpenAIAdapter (Phase 2 D-04 contract)"
        )
    ```

    Update the method docstring to note the D-09 migration: "Phase 6 D-09 migration: prompt body now lives in `docintel_generate.prompts.JUDGE_PROMPT`; parser is provider-native structured output (Anthropic `tools=[{strict: true}]` / OpenAI `response_format={'type': 'json_schema', 'strict': true}`). Phase 2 D-04 cross-family wiring (the factory dispatch) is preserved untouched."

    **Edit 5 — Verify class shell + degraded-mode detection block stay UNTOUCHED:**

    Lines 100-160 of the current file (`CrossFamilyJudge.__init__`, the degraded-mode detection logging block at lines 130-159, the `.name` property at the end of the class) MUST NOT be modified. These are Phase 2 D-04 contracts; Plan 06-06 changes prompt + parser ONLY.

    Verify the class shell still imports correctly: `make_adapters(Settings(llm_provider="real", llm_real_provider="anthropic"))` must still return a CrossFamilyJudge instance whose `__init__` runs cleanly. Plan 06-06 only changes the `.judge()` method body and the imports above the class.

    Anti-patterns to avoid (per PATTERNS §"### judge.py" + RESEARCH Anti-Patterns):
    - Do NOT change `CrossFamilyJudge.__init__` or `.name` (Phase 2 D-04 contract — explicit anti-pattern flag in PATTERNS.md line 631).
    - Do NOT change `make_adapters`'s cross-family dispatch in `factory.py` (lines 91-102) — D-09 is "prompt + parser, NOT dispatch" verbatim.
    - Do NOT reuse the heuristic `_SCORE_PATTERN` regex as a fallback — if structured-output fails, return the sentinel (RESEARCH Pitfall 6 — regex-fallback would re-introduce the failure mode Phase 6 is fixing).
    - Do NOT add `@retry` to `CrossFamilyJudge.judge()` itself — the helpers are wrapped; double-wrapping creates retry storms (RESEARCH Pitfall 6).
    - Do NOT extend the `LLMClient.complete` Protocol with `tools=` / `response_format=` kwargs — PATTERNS.md explicitly recommends option (b) "duplicate the @retry decorator on the two new helper functions" over option (a) "extend the protocol" because (b) is the minimal protocol change.
    - Do NOT remove the `_retry_log` + `log` two-logger pattern — both are used (log for warnings, _retry_log for tenacity before_sleep).
    - Do NOT delete the F401 comment on the tenacity import without replacing it — the import is now USED (the @retry decorator); the F401 marker is no longer needed; the new tenacity-from-import block replaces the single `from tenacity import retry` line entirely.
  </action>
  <verify>
    <automated>uv run python -c "from docintel_core.adapters.real.judge import CrossFamilyJudge, _judge_via_anthropic, _judge_via_openai, _JUDGE_VERDICT_SCHEMA, _sentinel_judgeverdict; from docintel_generate.prompts import JUDGE_PROMPT, build_judge_user_prompt; v = _sentinel_judgeverdict(); assert v.score == 0.0 and v.passed is False and 'deserialization failed' in v.reasoning; assert _JUDGE_VERDICT_SCHEMA['type'] == 'object'; assert set(_JUDGE_VERDICT_SCHEMA['required']) == {'score', 'passed', 'reasoning', 'unsupported_claims'}" && ! grep -E "^_JUDGE_SYSTEM_PROMPT|^_SCORE_PATTERN|^def _build_judge_prompt|^def _parse_judge_response" packages/docintel-core/src/docintel_core/adapters/real/judge.py && ! grep -E "^import re$" packages/docintel-core/src/docintel_core/adapters/real/judge.py && grep -q "from docintel_generate.prompts import JUDGE_PROMPT, build_judge_user_prompt" packages/docintel-core/src/docintel_core/adapters/real/judge.py && grep -c "@retry" packages/docintel-core/src/docintel_core/adapters/real/judge.py | grep -q "^2$" && uv run mypy --strict packages/docintel-core/src/docintel_core/adapters/real/judge.py 2>&1 | grep -E "error:" | head -3 ; true && bash scripts/check_adapter_wraps.sh && bash scripts/check_prompt_locality.sh && uv run pytest tests/test_adapters.py tests/test_judge_structured_output.py -ra -q -m "not real" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `_JUDGE_SYSTEM_PROMPT`, `_SCORE_PATTERN`, `_build_judge_prompt`, `_parse_judge_response` are FULLY REMOVED — `! grep -E "_JUDGE_SYSTEM_PROMPT|_SCORE_PATTERN|_build_judge_prompt|_parse_judge_response" packages/docintel-core/src/docintel_core/adapters/real/judge.py` exits 0.
    - Source assertion: `import re` is fully removed — `! grep -E "^import re\$" packages/docintel-core/src/docintel_core/adapters/real/judge.py`.
    - Source assertion: `from docintel_generate.prompts import JUDGE_PROMPT, build_judge_user_prompt` is present — `grep -c "from docintel_generate.prompts import JUDGE_PROMPT, build_judge_user_prompt" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 1.
    - Source assertion: TWO `@retry` decorators present (one on `_judge_via_anthropic`, one on `_judge_via_openai`) — `grep -c "^@retry" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 2.
    - Source assertion: helper functions exist — `grep -c "^def _judge_via_anthropic" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 1; `grep -c "^def _judge_via_openai" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 1; `grep -c "^def _sentinel_judgeverdict" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 1.
    - Source assertion: `_JUDGE_VERDICT_SCHEMA` is module-level — `grep -c "^_JUDGE_VERDICT_SCHEMA =" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 1.
    - Source assertion: `class CrossFamilyJudge:` is still present — `grep -c "^class CrossFamilyJudge:" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns 1 (Phase 2 D-04 class shell preserved).
    - Source assertion: SDK call patterns are present — `grep -c "\.messages\.create" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns at least 1; `grep -c "chat\.completions\.create" packages/docintel-core/src/docintel_core/adapters/real/judge.py` returns at least 1.
    - Behavior assertion: `_sentinel_judgeverdict()` returns a valid JudgeVerdict with `score=0.0`, `passed=False`, `reasoning` containing "deserialization failed", `unsupported_claims=[]`.
    - Behavior assertion: `_JUDGE_VERDICT_SCHEMA` is a valid JSON schema dict with all four required fields.
    - Behavior assertion: `JUDGE_PROMPT` is importable from `docintel_generate.prompts` AND from `docintel_core.adapters.real.judge` (re-import via the imports block).
    - Behavior assertion: `uv run pytest tests/test_adapters.py -ra -q -m "not real"` exits 0 — the existing Phase 2 stub tests still pass (they don't touch the real judge; this is a regression check that the import-block edits don't break the test-time module load).
    - Behavior assertion: `bash scripts/check_adapter_wraps.sh` exits 0 — `judge.py` STILL has `from tenacity import` (Phase 2 D-18 ADP-06 marker preserved + actually used now) AND contains `.messages.create` + `chat.completions.create` (new SDK call sites, but they ARE within the tenacity-wrap discipline since the helpers are `@retry`-decorated).
    - Behavior assertion: `bash scripts/check_prompt_locality.sh` exits 0 — the file's allowlist exclusion is via path-not-allowlisted-but-no-violation-detected: judge.py is NOT explicitly allowlisted in D-05 (the allowlist is prompts.py, parse.py, tests/, conftest.py, stub/llm.py). The new judge.py imports JUDGE_PROMPT from prompts.py — no inline prompt body in the file. The `_JUDGE_VERDICT_SCHEMA` dict + error-message strings are SHORT (< 80 chars per phrase) so they don't trip the PHRASE_PATTERN check; the `_judge_via_anthropic` / `_judge_via_openai` function names don't match the NAME_PATTERN (`_[A-Z_]*PROMPT[A-Z_]*\b`). Gate stays green.
    - Behavior assertion: `uv run mypy --strict packages/docintel-core/src/docintel_core/adapters/real/judge.py` reports 0 new errors. The tenacity decorator + Pydantic dispatch + isinstance + cross-package import should all type-check correctly.
    - Behavior assertion: `uv run pytest tests/test_judge_structured_output.py::test_deserialization_failure_returns_sentinel -ra -q` — the stub-mockable Pitfall 6 test from Plan 06-01 should now pass IF the test mocks the SDK call to return a malformed response (the test body checks the sentinel is returned, not raised).
  </acceptance_criteria>
  <done>The Phase 2 placeholder judge prompt + heuristic regex parser are retired. `adapters/real/judge.py` now imports `JUDGE_PROMPT` + `build_judge_user_prompt` from `docintel_generate.prompts`; the parser is provider-native structured output (Anthropic tools + OpenAI response_format). Two new `@retry`-wrapped helper functions handle dispatch by adapter family. Deserialization failures emit a structlog warning + return a sentinel `JudgeVerdict` (never raise — Pitfall 6 mitigation). Phase 2 D-04 cross-family wiring (factory + CrossFamilyJudge class shell + `.name`) is preserved untouched. All four CI grep gates remain green; mypy --strict clean. `PROMPT_VERSION_HASH` stabilizes at its post-Wave-3 value.</done>
</task>

</tasks>

<verification>
After Task A completes:

1. `uv run python -c "from docintel_core.adapters.real.judge import CrossFamilyJudge, _judge_via_anthropic, _judge_via_openai, _JUDGE_VERDICT_SCHEMA; from docintel_generate.prompts import JUDGE_PROMPT, PROMPT_VERSION_HASH; print('PROMPT_VERSION_HASH (Wave 3 stable):', PROMPT_VERSION_HASH)"` exits 0 and prints the stabilized hash (informational for Plan 06-07's audit).
2. `bash scripts/check_adapter_wraps.sh` exits 0 (Phase 2 D-18 wrap-gate preserved; new helpers stay within the wrap discipline).
3. `bash scripts/check_prompt_locality.sh` exits 0 (the file's content does not trip GEN-01 patterns).
4. `uv run pytest -ra -q -m "not real"` exits 0 (full stub-mode suite green; the judge migration only affects real-mode paths in `tests/test_judge_structured_output.py::test_judge_returns_judgeverdict` which is `@pytest.mark.real` deselected).
5. `uv run mypy --strict packages/docintel-core/` exits 0 (full strict-mode clean).
6. `git diff --stat packages/docintel-core/src/docintel_core/adapters/real/judge.py` shows net-NEGATIVE line count (the placeholder + parser ~50 lines removed; the new helpers ~80 lines added; net change depends on docstring verbosity — informational).
</verification>

<success_criteria>
Wave 3 judge migration lands:
- Phase 2 placeholder `_JUDGE_SYSTEM_PROMPT` + `_SCORE_PATTERN` + `_build_judge_prompt` + `_parse_judge_response` fully removed
- Canonical `JUDGE_PROMPT` + `build_judge_user_prompt` imported from `docintel_generate.prompts`
- Provider-native structured output (Anthropic `tools=[{strict: true}]` + OpenAI `response_format={'type': 'json_schema', 'strict': true}`) deserializes directly into `JudgeVerdict`
- Two new `@retry`-wrapped helpers (`_judge_via_anthropic`, `_judge_via_openai`) preserve the no-silent-retries discipline
- Deserialization failures emit `judge_structured_output_invalid` structlog WARNING + return sentinel `JudgeVerdict` (never raise — Pitfall 6 mitigation)
- Phase 2 D-04 cross-family wiring (factory dispatch + CrossFamilyJudge class shell + `.name`) UNTOUCHED
- All four CI grep gates remain green (`check_adapter_wraps`, `check_index_wraps`, `check_ingest_wraps`, `check_prompt_locality`)
- mypy --strict clean across the modified file
- `PROMPT_VERSION_HASH` stabilizes at the post-Wave-3 value (per Pitfall 8 — expected, not a regression)
</success_criteria>

<output>
After completion, create `.planning/phases/06-generation/06-06-SUMMARY.md` documenting:
- The post-Wave-3 `PROMPT_VERSION_HASH` value (informational; Phase 10 EVAL-02 manifest will read this)
- Confirmation that the Wave-1 hash (Plan 06-03 SUMMARY) is now superseded
- Pre/post line count of `adapters/real/judge.py` (informational; expect net change near zero — removed 50 lines + added ~80)
- Confirmation that `bash scripts/check_adapter_wraps.sh` exits 0 with the new SDK call sites in judge.py
- Notes on the model-name decision: did the helpers hardcode `claude-sonnet-4-6` and `gpt-4o`, or did they import the default models from `llm_anthropic.py`/`llm_openai.py`? (RESEARCH Open Question 3 deferred the pinning decision to planner discretion.)
- Any deviations from RESEARCH Pattern 3 (e.g., if the openai-python `.choices[0].message.content` shape differed in `openai==2.36.0` vs the RESEARCH example)
</output>
