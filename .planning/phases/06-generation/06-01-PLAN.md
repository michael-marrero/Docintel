---
phase: 06-generation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_prompt_locality.py
  - tests/test_prompt_version_hash.py
  - tests/test_generator_stub_determinism.py
  - tests/test_generator_refusal.py
  - tests/test_make_generator.py
  - tests/test_judge_structured_output.py
  - tests/test_generator_search_integration.py
  - tests/test_generator_telemetry.py
  - tests/test_generation_result_schema.py
  - tests/test_generator_real_hero.py
  - tests/fixtures/prompt_locality_violations/offender.py
  - tests/fixtures/prompt_locality_violations_with_noqa/allowed.py
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04]
must_haves:
  truths:
    - "Ten new test files exist under `tests/` covering every GEN-01..04 behavior + D-03/D-09/D-12/D-14/D-16/D-17 listed in 06-VALIDATION.md Per-Task Verification Map; every test function is xfail-strict marked and discoverable by `uv run pytest --collect-only`."
    - "Two fixture directories exist under `tests/fixtures/` — one with a planted prompt-locality violation (positive offender for the GEN-01 negative case) and one with a violation silenced by `# noqa: prompt-locality` (verifies the escape hatch)."
    - "`uv run pytest -ra -q -m \"not real\"` exits 0 with the new xfail-marked tests reported as xfailed (not failed) — the implementation slot is reserved for Wave 1+ plans."
    - "`uv run pytest --collect-only -q tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_generator_stub_determinism.py tests/test_generator_refusal.py tests/test_make_generator.py tests/test_judge_structured_output.py tests/test_generator_search_integration.py tests/test_generator_telemetry.py tests/test_generation_result_schema.py tests/test_generator_real_hero.py` succeeds (all 10 test files collectable; no `ERRORS` lines in output)."
  artifacts:
    - path: "tests/test_prompt_locality.py"
      provides: "GEN-01 + D-04 + D-05 xfail scaffolds — 3 test functions: test_grep_gate_passes_on_canonical_layout, test_grep_gate_fails_on_violation, test_grep_gate_respects_noqa"
    - path: "tests/test_prompt_version_hash.py"
      provides: "GEN-02 + D-08 xfail scaffolds — 3 test functions: test_hash_format, test_hash_sensitivity, test_per_prompt_hashes_exposed"
    - path: "tests/test_generator_stub_determinism.py"
      provides: "GEN-03 + D-13 xfail scaffolds — 3 test functions: test_determinism, test_citation_subset, test_hallucinated_ids_dropped"
    - path: "tests/test_generator_refusal.py"
      provides: "GEN-04 + D-10 + D-11 + D-15 xfail scaffolds — 3 test functions: test_hard_zero_chunk_refusal, test_llm_driven_refusal, test_zero_chunks_warning"
    - path: "tests/test_make_generator.py"
      provides: "D-03 xfail scaffolds — 2 test functions: test_make_generator_stub, test_factory_lazy_imports_generator_module"
    - path: "tests/test_judge_structured_output.py"
      provides: "D-09 xfail scaffolds — 2 test functions: test_judge_returns_judgeverdict (real-mode), test_deserialization_failure_returns_sentinel (stub-mockable)"
    - path: "tests/test_generator_search_integration.py"
      provides: "D-14 + hero-question end-to-end stub xfail scaffold — 1 test function: test_hero_comparative_stub"
    - path: "tests/test_generator_telemetry.py"
      provides: "D-16 xfail scaffold — 1 test function: test_completed_fields (14 fields enumerated)"
    - path: "tests/test_generation_result_schema.py"
      provides: "D-17 xfail scaffolds — 2 test functions: test_generation_result_frozen, test_generation_result_extra_forbid"
    - path: "tests/test_generator_real_hero.py"
      provides: "real-mode hero question xfail scaffold — 1 test function: test_generator_hero_real_mode (@pytest.mark.real + xfail)"
    - path: "tests/fixtures/prompt_locality_violations/offender.py"
      provides: "Negative fixture — Python file with a 100+ char triple-quoted string containing `cite`, `<context>`, `chunk_id` substrings + no `# noqa: prompt-locality`; the GEN-01 gate must flag it"
    - path: "tests/fixtures/prompt_locality_violations_with_noqa/allowed.py"
      provides: "Positive escape-hatch fixture — same offending pattern as the negative fixture BUT with `# noqa: prompt-locality` appended to the offending line; the gate must NOT flag it"
  key_links:
    - from: "tests/test_prompt_locality.py"
      to: "scripts/check_prompt_locality.sh (Plan 06-02) + tests/fixtures/prompt_locality_violations/ + tests/fixtures/prompt_locality_violations_with_noqa/"
      via: "subprocess.run(['bash', _GATE_SCRIPT, str(_FIXTURE_DIR)])"
      pattern: "subprocess\\.run.*check_prompt_locality\\.sh"
    - from: "all 10 test files"
      to: "docintel_generate.prompts / docintel_generate.generator / docintel_core.types.GenerationResult (Waves 1-3)"
      via: "module imports inside test bodies; ImportError under Wave 0 triggers xfail"
      pattern: "from docintel_generate"
---

<objective>
Land all ten test-file scaffolds + the two prompt-locality fixture directories so every Phase 6 behavior named in `06-VALIDATION.md`'s Per-Task Verification Map has a collectable pytest target before any production code lands. Every new test function is xfail-strict marked (`@pytest.mark.xfail(strict=True, reason="Wave N — implementation lands in Plan 06-XX")`) so Wave 0 ships green, and Wave 1-4 plans flip the xfails to xpass as implementations land. Per CLAUDE.md operating rule "Tests before code where possible, especially for the eval harness" — this plan is the literal tests-first scaffold for Phase 6.

Purpose: Tests-first discipline (RESEARCH.md §Validation Architecture rows 1099-1123) gives Wave 1+ implementers an executable target to flip from xfail to passing. Without this scaffold, the prompt-version-hash, generator, factory, and judge-migration tests would be invented during Waves 1-3 alongside the code — interleaved, harder to review, and harder to verify the test asserts the contract rather than the implementation. Mirrors Phase 5's Plan 05-01 Wave 0 scaffold pattern exactly.

Output: 10 test files under `tests/` (every test function name listed verbatim in 06-VALIDATION.md "Wave 0 Requirements" section) + 2 fixture directories under `tests/fixtures/` (negative offender + positive noqa-escape). No production code changes; no source-module changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generation/06-CONTEXT.md
@.planning/phases/06-generation/06-RESEARCH.md
@.planning/phases/06-generation/06-PATTERNS.md
@.planning/phases/06-generation/06-VALIDATION.md

<interfaces>
<!-- Existing pytest configuration (verified) — every test file authored here MUST be collectable by the existing pytest config without modification. -->

From `pyproject.toml` (workspace root, `[tool.pytest.ini_options]`):
- `testpaths = ["tests", "packages/*/tests"]`
- `addopts = "-ra --strict-markers"`
- `markers = ["real: tests that require real (non-stub) backends — gated behind workflow_dispatch"]`

From `tests/test_retrieved_chunk_schema.py:1-32` (Phase 5 Wave 0 analog — module-docstring + xfail-strict-with-reason pattern; ImportError-on-not-yet-implemented gives xfail without skip):
- Module docstring names the plan, the wave, the requirement IDs, and the analog test file path.
- `@pytest.mark.xfail(strict=True, reason="Wave N — <module> not yet implemented")` decorator above each function.
- Test body does the import + assertion; ImportError counts as xfail under strict.

From `tests/test_index_wraps_gate.py:1-83` (Phase 4 analog — subprocess invocation of bash script + positive-fixture + negative-fixture pattern):
- `_REPO_ROOT = Path(__file__).resolve().parent.parent`
- `_GATE_SCRIPT = _REPO_ROOT / "scripts" / "check_prompt_locality.sh"`
- `_FIXTURE_DIR = Path(__file__).resolve().parent / "fixtures" / "prompt_locality_violations"`
- Two test functions: `test_grep_gate_catches_unwrapped` (negative-fixture-dir → exits non-zero) + `test_grep_gate_passes_wrapped` (uses `tmp_path` for the positive case).

From `tests/test_reranker_canary.py:44-50` (Phase 5 dual-mode marker pattern — `@pytest.mark.real` (outer) + `@pytest.mark.xfail(strict=True, ...)` (inner) — do NOT use module-level `pytestmark`, which would deselect the whole file on every PR's default `-m "not real"` run):
- Decorator ordering: `@pytest.mark.real` then `@pytest.mark.xfail(strict=True, reason="Wave 3 — judge structured-output not yet wired")`.

From `tests/test_make_retriever.py` (Phase 5 D-04 factory + lazy-import-gate analog for Plan 06-01 Task `tests/test_make_generator.py`):
- `test_make_retriever_stub` — calls factory with `Settings(llm_provider="stub", ...)` and asserts return type.
- `test_factory_lazy_imports_retriever_module` — asserts `"docintel_retrieve.retriever" not in sys.modules` AFTER `import docintel_core.adapters.factory` (the factory module load must NOT trigger Retriever import).

From `data/eval/canary/cases.jsonl` (Phase 5 existing data layout — empty/optional in Wave 0):
- Phase 6 does NOT require any data fixture under `data/eval/` for the test scaffolds; the fixtures live entirely under `tests/fixtures/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create the two prompt-locality fixture directories</name>
  <files>tests/fixtures/prompt_locality_violations/offender.py, tests/fixtures/prompt_locality_violations_with_noqa/allowed.py</files>
  <read_first>
    - tests/fixtures/missing_tenacity/qdrant_fake.py (Phase 4 negative-fixture analog — minimal Python file that intentionally violates a CI gate)
    - .planning/phases/06-generation/06-RESEARCH.md §Code Examples §Pattern 4 lines 586-668 (canonical grep-gate body shape — `NAME_PATTERN` and `PHRASE_PATTERN` definitions)
    - .planning/phases/06-generation/06-PATTERNS.md §"### CI grep-gate test pattern (positive + negative fixture)" lines 941-964 (fixture-deviation note for Phase 6)
    - .planning/phases/06-generation/06-CONTEXT.md D-04 + D-05 (allowlist + per-line noqa escape)
  </read_first>
  <action>
    Create two fixture directories under `tests/fixtures/`, each containing a single Python file.

    **`tests/fixtures/prompt_locality_violations/offender.py`** — a Python file that DELIBERATELY contains an inline prompt-like string literal that the GEN-01 gate (Plan 06-02 ships) must catch. Content shape: a single module-level constant whose NAME matches `NAME_PATTERN` from RESEARCH §Pattern 4 line 628 (`_[A-Z_]*PROMPT[A-Z_]*\b`) AND whose value is a triple-quoted string longer than 80 characters containing at least three of the `PHRASE_PATTERN` substrings (`You are`, `<context>`, `cite`, `chunk_id`). Concrete content:

    ```
    """Negative fixture for tests/test_prompt_locality.py::test_grep_gate_fails_on_violation.

    This file intentionally contains an inline prompt-like string outside the
    allowlist. The GEN-01 grep gate (scripts/check_prompt_locality.sh, Plan 06-02)
    must flag this file with exit code 1.
    """

    _INLINE_SYNTHESIS_PROMPT = """You are answering a question using ONLY the
    retrieved 10-K excerpts in the <context> block below. Every factual sentence
    must end with a [chunk_id] citation. Do not invent chunk_ids; only cite from
    <context>."""
    ```

    Do NOT add `# noqa: prompt-locality` anywhere in this file. The whole point is to be caught.

    **`tests/fixtures/prompt_locality_violations_with_noqa/allowed.py`** — same offending pattern as the negative fixture BUT with `# noqa: prompt-locality` appended at the end of the line containing the identifier (or the first line of the triple-quoted block — planner picks based on the grep-gate's `sed -n "${lineno}p"` check). Concrete content:

    ```
    """Positive escape-hatch fixture for tests/test_prompt_locality.py::test_grep_gate_respects_noqa.

    This file contains an inline prompt-like string identical to the negative
    fixture, but the offending line carries `# noqa: prompt-locality`. The gate
    must NOT flag this file (exit code 0).
    """

    _INLINE_SYNTHESIS_PROMPT = """You are answering a question using ONLY the retrieved 10-K excerpts in the <context> block. Every factual sentence must end with a [chunk_id] citation."""  # noqa: prompt-locality
    ```

    Use single-line triple-quoted string (one logical line) so the `sed -n "${lineno}p" | grep noqa` check inside the gate script (Plan 06-02) fires on the same line as the offending content. If the planner of Plan 06-02 chooses a multi-line `# noqa: prompt-locality` placement, this fixture is updated then; for Wave 0, single-line is the deterministic choice.

    Both files MUST be valid Python (importable) — no syntax errors, no missing imports. They are not imported by any test directly; they are only scanned by the bash grep gate via `subprocess.run`.

    Create the two parent directories if they do not already exist (`mkdir -p tests/fixtures/prompt_locality_violations tests/fixtures/prompt_locality_violations_with_noqa`).
  </action>
  <verify>
    <automated>test -f tests/fixtures/prompt_locality_violations/offender.py && test -f tests/fixtures/prompt_locality_violations_with_noqa/allowed.py && python -c "import ast; ast.parse(open('tests/fixtures/prompt_locality_violations/offender.py').read()); ast.parse(open('tests/fixtures/prompt_locality_violations_with_noqa/allowed.py').read())" && grep -q "noqa: prompt-locality" tests/fixtures/prompt_locality_violations_with_noqa/allowed.py && ! grep -q "noqa: prompt-locality" tests/fixtures/prompt_locality_violations/offender.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: both fixture files exist at the exact paths `tests/fixtures/prompt_locality_violations/offender.py` and `tests/fixtures/prompt_locality_violations_with_noqa/allowed.py` (verified by `test -f` for both).
    - Source assertion: both files are syntactically valid Python (verified by `python -c "import ast; ast.parse(open(<path>).read())"` exits 0 for each).
    - Source assertion: `tests/fixtures/prompt_locality_violations_with_noqa/allowed.py` contains the substring `noqa: prompt-locality` exactly once (`grep -c "noqa: prompt-locality" tests/fixtures/prompt_locality_violations_with_noqa/allowed.py` returns 1).
    - Source assertion: `tests/fixtures/prompt_locality_violations/offender.py` contains NO `noqa: prompt-locality` substring (`grep -c "noqa: prompt-locality" tests/fixtures/prompt_locality_violations/offender.py` returns 0).
    - Source assertion: both files contain `_INLINE_SYNTHESIS_PROMPT` AND `<context>` AND `chunk_id` substrings (the offending patterns the gate scans for).
  </acceptance_criteria>
  <done>Two fixture directories created under `tests/fixtures/`, each containing exactly one Python file. The negative fixture (`offender.py`) is a deliberate violation; the positive fixture (`allowed.py`) is the same violation with the escape hatch. Both are syntactically valid Python. Plan 06-02's `scripts/check_prompt_locality.sh` will scan these directories in Plan 06-02's test invocations; Plan 06-01 Task A's `tests/test_prompt_locality.py` invokes the gate against them via subprocess.</done>
</task>

<task type="auto">
  <name>Task A: Create the four protocol-locality + version-hash + factory test scaffolds</name>
  <files>tests/test_prompt_locality.py, tests/test_prompt_version_hash.py, tests/test_make_generator.py, tests/test_generation_result_schema.py</files>
  <read_first>
    - tests/test_index_wraps_gate.py (analog for `tests/test_prompt_locality.py` — full file, especially `_REPO_ROOT` / `_GATE_SCRIPT` / `_FIXTURE_DIR` constants at lines 28-30 + `test_grep_gate_catches_unwrapped` and `test_grep_gate_passes_wrapped` test bodies at lines 33-83)
    - tests/test_retrieved_chunk_schema.py (analog for `tests/test_generation_result_schema.py` — Pydantic-frozen + extra=forbid test pattern; `_ok_payload()` helper)
    - tests/test_make_retriever.py (analog for `tests/test_make_generator.py` — factory call + lazy-import gate; `assert "docintel_retrieve.retriever" not in sys.modules` pattern)
    - .planning/phases/06-generation/06-PATTERNS.md lines 809-872 (Test scaffolds section — concrete analog assignments)
    - .planning/phases/06-generation/06-RESEARCH.md §Validation Architecture lines 1099-1123 (canonical test function names)
    - .planning/phases/06-generation/06-CONTEXT.md D-04, D-05, D-08, D-17 (the requirements these tests assert)
  </read_first>
  <action>
    Create four test files under `tests/`. Every test function is xfail-strict marked. Every body attempts the import that will fail at Wave 0 (the production module does not exist yet) — the resulting ImportError counts as the expected xfail.

    **`tests/test_prompt_locality.py`** — 3 test functions (GEN-01 + D-04 + D-05). Mirror `tests/test_index_wraps_gate.py:1-83` shape verbatim, but reuse the existing Wave 0 fixtures from Plan 06-01 Task 0 (`tests/fixtures/prompt_locality_violations/` and `tests/fixtures/prompt_locality_violations_with_noqa/`).

    Module docstring per Phase 5 Plan 05-01 Task 1 convention (phase + requirement IDs + Wave 0 / Plan source + analog file + flip wave). Functions:
    - `test_grep_gate_passes_on_canonical_layout()` — invokes `bash scripts/check_prompt_locality.sh` with NO SCAN_DIR argument (scans `packages/` default per D-04); asserts `result.returncode == 0` once Wave 1 lands `prompts.py` AND Wave 3 lands `stub/llm.py` updates. xfail-strict reason: "Wave 4 — Plan 06-07 confirms gate exit 0 after all production code lands".
    - `test_grep_gate_fails_on_violation()` — invokes `bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations`; asserts `result.returncode != 0` (the planted violation MUST be caught). xfail-strict reason: "Wave 0 — Plan 06-02 ships the bash script". Will pass once 06-02 lands.
    - `test_grep_gate_respects_noqa()` — invokes `bash scripts/check_prompt_locality.sh tests/fixtures/prompt_locality_violations_with_noqa`; asserts `result.returncode == 0` (the escape hatch silences the gate). xfail-strict reason: "Wave 0 — Plan 06-02 ships the bash script with noqa handling".

    Constants at module top: `_REPO_ROOT = Path(__file__).resolve().parent.parent`, `_GATE_SCRIPT = _REPO_ROOT / "scripts" / "check_prompt_locality.sh"`. Use `subprocess.run(["bash", str(_GATE_SCRIPT), <arg>], check=False, capture_output=True, text=True)` for invocations. Every assertion message includes `f"stdout={result.stdout!r} stderr={result.stderr!r}"` per the analog.

    **`tests/test_prompt_version_hash.py`** — 3 test functions (GEN-02 + D-08). Module docstring per the convention above. Functions:
    - `test_hash_format()` — imports `from docintel_generate.prompts import PROMPT_VERSION_HASH`; asserts `isinstance(PROMPT_VERSION_HASH, str)`, `len(PROMPT_VERSION_HASH) == 12`, `all(c in "0123456789abcdef" for c in PROMPT_VERSION_HASH)`. xfail reason: "Wave 1 — Plan 06-03 ships docintel_generate.prompts".
    - `test_per_prompt_hashes_exposed()` — imports `from docintel_generate.prompts import _SYNTHESIS_HASH, _REFUSAL_HASH, _JUDGE_HASH`; asserts all three are 12-char hex strings; asserts they are all distinct (collision check). xfail reason: "Wave 1 — Plan 06-03 ships docintel_generate.prompts".
    - `test_hash_sensitivity()` — imports `from docintel_generate.prompts import SYNTHESIS_PROMPT, _h`; computes `_h(SYNTHESIS_PROMPT)` and asserts it equals `_SYNTHESIS_HASH` (same input → same hash); then computes `_h(SYNTHESIS_PROMPT + "x")` and asserts it does NOT equal `_SYNTHESIS_HASH` (one-byte change → different hash; Pitfall 3 defense). xfail reason: "Wave 1 — Plan 06-03 ships docintel_generate.prompts._h helper".

    **`tests/test_make_generator.py`** — 2 test functions (D-03). Mirror `tests/test_make_retriever.py` analog. Functions:
    - `test_make_generator_stub()` — imports `from docintel_core.adapters.factory import make_generator` + `from docintel_core.config import Settings`; constructs `cfg = Settings(llm_provider="stub")` (verify with the planner of Plan 06-04 that the Settings construction needs no other fields for stub mode — check `Settings.__init__` defaults match Plan 05-05's `test_make_retriever.py` pattern); calls `g = make_generator(cfg)`; asserts `isinstance(g, type(g))` (cannot use `isinstance(g, Generator)` without importing — instead assert `g.__class__.__name__ == "Generator"` + `g.__class__.__module__ == "docintel_generate.generator"`). xfail-strict reason: "Wave 2 — Plan 06-04 ships make_generator + Generator".
    - `test_factory_lazy_imports_generator_module()` — clean `sys.modules` of `docintel_generate.*` keys at test start via `for k in list(sys.modules): if k.startswith("docintel_generate"): del sys.modules[k]`; `import docintel_core.adapters.factory`; assert `"docintel_generate.generator" not in sys.modules` (D-12 lazy-import gate). xfail-strict reason: "Wave 2 — Plan 06-04 wires make_generator lazy-import".

    **`tests/test_generation_result_schema.py`** — 2 test functions (D-17). Mirror `tests/test_retrieved_chunk_schema.py` analog. Module docstring per the convention.

    Helper `_ok_payload()` at module level returns a dict with all six `GenerationResult` fields: `text="answer text"`, `cited_chunk_ids=["AAPL-FY2024-Item-1A-007"]`, `refused=False`, `retrieved_chunks=[]`, `completion=None`, `prompt_version_hash="abcdef012345"`. Functions:
    - `test_generation_result_frozen()` — imports `from docintel_core.types import GenerationResult`; constructs `gr = GenerationResult(**_ok_payload())`; attempts `gr.refused = True`; asserts this raises `pydantic.ValidationError` (frozen=True). xfail-strict reason: "Wave 2 — Plan 06-04 ships GenerationResult in docintel_core.types".
    - `test_generation_result_extra_forbid()` — constructs `GenerationResult(**_ok_payload(), extra_field="x")`; asserts `pydantic.ValidationError` is raised (extra="forbid"). xfail-strict reason: "Wave 2 — Plan 06-04 ships GenerationResult.model_config extra='forbid'".

    Every test function: 4-line docstring (purpose + analog + plan-source + xfail-reason). All four files start with `from __future__ import annotations`. Imports: `import pytest`, `import subprocess` (locality only), `from pathlib import Path` (locality only), `import sys` (make_generator only), domain-specific imports inside test bodies.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_make_generator.py tests/test_generation_result_schema.py 2>&1 | grep -E "^[0-9]+ tests collected$" | head -1 && uv run pytest tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_make_generator.py tests/test_generation_result_schema.py -ra -q 2>&1 | grep -E "xfailed|XFAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `uv run pytest --collect-only -q tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_make_generator.py tests/test_generation_result_schema.py` exits 0 and reports exactly 10 tests collected (3 + 3 + 2 + 2 = 10).
    - Behavior assertion: `uv run pytest tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_make_generator.py tests/test_generation_result_schema.py -ra -q` exits 0 (no failed/errored tests; all 10 are reported xfailed).
    - Source assertion: every test function has `@pytest.mark.xfail(strict=True, reason="Wave N — ...")` (verified by `grep -c "@pytest.mark.xfail(strict=True" tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_make_generator.py tests/test_generation_result_schema.py` returns 3, 3, 2, 2 respectively).
    - Source assertion: every test function name listed in `<files>` artifact spec exists exactly once in its file (verified by `grep -c "^def test_" tests/test_prompt_locality.py` = 3, `tests/test_prompt_version_hash.py` = 3, `tests/test_make_generator.py` = 2, `tests/test_generation_result_schema.py` = 2).
    - Source assertion: no `pytest.skip` calls in any of the four files (`! grep -q "pytest.skip" tests/test_prompt_locality.py tests/test_prompt_version_hash.py tests/test_make_generator.py tests/test_generation_result_schema.py`).
  </acceptance_criteria>
  <done>Four test files exist with a total of 10 xfail-strict test functions covering GEN-01 (3), GEN-02 (3), D-03 (2), and D-17 (2). All collectable by pytest; all xfailed under stub-mode Wave 0; will flip to xpass as Plans 06-02 (gate script + GEN-01 gate behavior on canonical layout deferred to 06-07 sweep), 06-03 (prompts + hashes), and 06-04 (Generator + factory + GenerationResult) land.</done>
</task>

<task type="auto">
  <name>Task B: Create the six generator-behavior + judge-migration + hero test scaffolds</name>
  <files>tests/test_generator_stub_determinism.py, tests/test_generator_refusal.py, tests/test_judge_structured_output.py, tests/test_generator_search_integration.py, tests/test_generator_telemetry.py, tests/test_generator_real_hero.py</files>
  <read_first>
    - tests/test_retriever_search.py (analog for stub-determinism + telemetry tests — `_make_chunk` helper at lines 38-53, `make_retriever(Settings(...))` invocation pattern lines 46-56, `test_zero_candidates` for refusal-path inspiration)
    - tests/test_reranker_canary.py:44-50 (dual-mode marker pattern — `@pytest.mark.real` + xfail, NOT module-level pytestmark; rationale at lines 52-65)
    - tests/test_adapters.py (analog for protocol-satisfaction tests; useful for thinking about how to stub `bundle.llm` for the structured-output deserialization test)
    - tests/test_retriever_search.py:27 + 43 + 107-127 + 208 (analog for `from structlog.testing import capture_logs` + `with capture_logs() as records:` pattern — this is the actual structlog-capture pattern the codebase uses; needed for `generator_completed` telemetry test AND `generator_refused_zero_chunks` warning test AND `generator_hallucinated_chunk_id` log assertion)
    - .planning/phases/06-generation/06-PATTERNS.md lines 809-872 (Test scaffolds analog table — every test file mapped to a source analog)
    - .planning/phases/06-generation/06-RESEARCH.md §Code Examples Example 1 lines 296-435 (Generator class + helper methods; `_format_user_prompt` + `_emit_completed` + Steps A..E)
    - .planning/phases/06-generation/06-CONTEXT.md D-09, D-10, D-11, D-13, D-14, D-15, D-16, CD-07 (the contracts these tests assert)
  </read_first>
  <action>
    Create six test files under `tests/`. Same xfail-strict + module-docstring conventions as Task A. Imports inside test bodies (so Wave 0 ImportError counts as xfail).

    **`tests/test_generator_stub_determinism.py`** — 3 test functions (GEN-03 + D-13 + CD-07). Module docstring per convention.

    Helper `_stub_retrieved_chunks()` at module level returns `[RetrievedChunk(chunk_id="AAPL-FY2024-Item-1A-007", text="Apple identifies supplier concentration as a key risk.", score=0.9, ticker="AAPL", fiscal_year=2024, item_code="Item 1A", char_span_in_section=(100, 250))]` (single representative chunk) — imports happen inside the helper or inside each test body.

    Functions (use a controlled `Generator` construction — build a `bundle = make_adapters(Settings(llm_provider="stub"))` + a fake `retriever` whose `.search(query, k)` returns the helper output; `Generator(bundle=bundle, retriever=fake_retriever)`):
    - `test_determinism()` — call `g.generate("revenue growth", k=5)` three times in a row with identical input; assert `r1.text == r2.text == r3.text` AND `r1.cited_chunk_ids == r2.cited_chunk_ids == r3.cited_chunk_ids` (ADP-07 stub determinism extends through generator). xfail reason: "Wave 2 — Plan 06-04 ships Generator".
    - `test_citation_subset()` — call `g.generate("revenue growth", k=1)` with the single-chunk retrieved set; assert `set(result.cited_chunk_ids).issubset({"AAPL-FY2024-Item-1A-007"})` (D-13 step 4 validation — cited_chunk_ids ⊆ retrieved_chunk_ids set). xfail reason: "Wave 2 — Plan 06-04 ships citation-validation Step D".
    - `test_hallucinated_ids_dropped()` — build a `fake_llm` whose `.complete(...)` returns `CompletionResponse(text="Apple risk [AAPL-FY2024-Item-1A-007] and [NVDA-FY2024-Item-7-999].", usage=..., cost_usd=0.0, latency_ms=0.0, model="fake")` (the second ID is hallucinated — not in retrieved set); compose into bundle; call `g.generate(...)`; assert `"AAPL-FY2024-Item-1A-007" in result.cited_chunk_ids` AND `"NVDA-FY2024-Item-7-999" not in result.cited_chunk_ids` (dropped per CD-07) AND `result.text` is UNCHANGED (no sentence stripping per D-13 anti-pattern). Use `caplog` to assert a `generator_hallucinated_chunk_id` log line fires. xfail reason: "Wave 2 — Plan 06-04 ships Step D hallucination drop + CD-07 structlog".

    **`tests/test_generator_refusal.py`** — 3 test functions (GEN-04 + D-10 + D-11 + D-15). Module docstring per convention.

    Functions:
    - `test_hard_zero_chunk_refusal()` — construct a `fake_retriever` whose `.search(...)` returns `[]`; build `g = Generator(bundle=stub_bundle, retriever=fake_retriever)`; call `r = g.generate("nonsense query", k=5)`; assert `r.refused is True`, `r.text == REFUSAL_TEXT_SENTINEL` (imported `from docintel_core.types import REFUSAL_TEXT_SENTINEL` — Pitfall 9 resolution: sentinel lives in core), `r.completion is None`, `r.cited_chunk_ids == []`, `r.retrieved_chunks == []`. xfail reason: "Wave 2 — Plan 06-04 ships Step B hard-refusal".
    - `test_llm_driven_refusal()` — build a `fake_llm` whose `.complete(...)` returns `CompletionResponse(text=REFUSAL_TEXT_SENTINEL, ...)`; compose into bundle; build `g` with a `fake_retriever` that returns ONE chunk (NOT empty — so we exercise the LLM-driven path, not the hard-floor path); call `g.generate(...)`; assert `r.refused is True` (Step D `.startswith` check), `r.completion is not None` (LLM was actually called), `r.cited_chunk_ids == []` (no brackets in the sentinel). xfail reason: "Wave 2 — Plan 06-04 ships Step D `.startswith(REFUSAL_TEXT_SENTINEL)`".
    - `test_zero_chunks_warning()` — same setup as `test_hard_zero_chunk_refusal` BUT use `caplog` fixture or structlog `capture_logs()` context manager to assert that exactly ONE log line is emitted with `event="generator_refused_zero_chunks"` (the structlog warning per RESEARCH §Pattern 1 line 352 + D-15 Step B). xfail reason: "Wave 2 — Plan 06-04 emits generator_refused_zero_chunks structlog".

    **`tests/test_judge_structured_output.py`** — 2 test functions (D-09). Dual-mode pattern from `tests/test_reranker_canary.py:44-50`.

    Functions:
    - `test_judge_returns_judgeverdict()` — `@pytest.mark.real` (OUTER) + `@pytest.mark.xfail(strict=True, reason="Wave 3 — judge structured-output not yet wired")` (INNER). Construct `bundle = make_adapters(Settings(llm_provider="real", llm_real_provider="anthropic"))`; call `verdict = bundle.judge.judge(prediction="Apple revenue grew.", reference=["Apple revenue grew."], rubric="")`; assert `isinstance(verdict, JudgeVerdict)`, `0.0 <= verdict.score <= 1.0`, `isinstance(verdict.passed, bool)`, `isinstance(verdict.reasoning, str)`, `isinstance(verdict.unsupported_claims, list)`.
    - `test_deserialization_failure_returns_sentinel()` — STUB-MOCKABLE (no `@pytest.mark.real`). Use `unittest.mock.patch` to patch the SDK call inside `adapters/real/judge.py::_judge_via_anthropic` (or `.._via_openai`) to return a response whose `.input` field is `{"invalid": "shape"}` (won't deserialize into `JudgeVerdict`); assert the resulting verdict is a sentinel `JudgeVerdict(score=0.0, passed=False, reasoning="<deserialization failed>", unsupported_claims=[])` (per Pitfall 6 — failures are measurements, not retry triggers). xfail-strict reason: "Wave 3 — Plan 06-06 ships structured-output dispatch + sentinel fallback".

    **`tests/test_generator_search_integration.py`** — 1 test function (D-14 + hero question end-to-end stub). Module docstring per convention.

    Function:
    - `test_hero_comparative_stub()` — construct `g = make_generator(Settings(llm_provider="stub"))`; call `r = g.generate("Which of these companies grew R&D while margins shrank in 2023?", k=5)`; assert `r.refused is False` (stub LLM emits the templated `[STUB ANSWER citing ...]` response when retrieval returns chunks, NOT the refusal sentinel), `len(r.cited_chunk_ids) >= 1`, `r.completion is not None`, `r.completion.model == "stub"`, `len(r.retrieved_chunks) <= 5`. NOTE: this test exercises the FULL stack including `Retriever` — the chunk_map eager-load + BM25/dense/RRF/rerank pipeline. xfail reason: "Wave 2 — Plan 06-04 ships make_generator(cfg) integration".

    **`tests/test_generator_telemetry.py`** — 1 test function (D-16). Module docstring per convention.

    Function:
    - `test_completed_fields()` — use `from structlog.testing import capture_logs` + `with capture_logs() as records:` (see `tests/test_retriever_search.py:27 + 43 + 107-127 + 208` for the canonical analog in this codebase). Call `g.generate("revenue growth", k=1)` with a single-chunk fake retriever + stub bundle; capture log records; assert EXACTLY ONE record has `event=="generator_completed"`; assert THAT record contains all **15 named fields** enumerated in D-16 + RESEARCH §Pattern 1 lines 417-434: `query_tokens`, `n_chunks_retrieved`, `n_chunks_cited`, `refused`, `prompt_version_hash`, `synthesis_hash`, `refusal_hash`, `judge_hash`, `prompt_tokens`, `completion_tokens`, `cost_usd`, `retrieval_ms`, `generation_ms`, `total_ms`, `model`. Assertion is "each named field is present in the record dict", not the cardinal count. xfail reason: "Wave 2 — Plan 06-04 emits generator_completed".

    **`tests/test_generator_real_hero.py`** — 1 test function (hero question real-mode). Mirror `tests/test_reranker_canary.py:44-50` dual-mode pattern.

    Function:
    - `test_generator_hero_real_mode()` — `@pytest.mark.real` (OUTER) + `@pytest.mark.xfail(strict=True, reason="Wave 4 — Plan 06-07 promotes after workflow_dispatch run lands")` (INNER). Construct `g = make_generator(Settings(llm_provider="real", llm_real_provider="anthropic"))`; call `r = g.generate("Which of these companies grew R&D while margins shrank in 2023?", k=5)`; assert `r.refused is False`, `len(r.cited_chunk_ids) >= 2` (multi-hop comparative MUST cite ≥2 chunks), tickers from cited chunks span ≥2 distinct values (`len(set(c.ticker for c in r.retrieved_chunks if c.chunk_id in r.cited_chunk_ids)) >= 2`), `r.completion.cost_usd < 0.20` (CD-06 — single hero call should be cheap), `r.completion.usage.prompt_tokens < 8000` (CD-06 context-budget guard).

    Every test file: 4+ line module docstring naming phase + requirement IDs + Wave 0 / Plan source + analog file + flip wave + xfail behavior. Every function has a brief 2-4 line docstring + the xfail-strict marker. All files start with `from __future__ import annotations`.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q tests/test_generator_stub_determinism.py tests/test_generator_refusal.py tests/test_judge_structured_output.py tests/test_generator_search_integration.py tests/test_generator_telemetry.py tests/test_generator_real_hero.py 2>&1 | tail -5 && uv run pytest tests/test_generator_stub_determinism.py tests/test_generator_refusal.py tests/test_judge_structured_output.py tests/test_generator_search_integration.py tests/test_generator_telemetry.py -ra -q -m "not real" 2>&1 | grep -E "xfailed|XFAIL|failed|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `uv run pytest --collect-only -q tests/test_generator_stub_determinism.py tests/test_generator_refusal.py tests/test_judge_structured_output.py tests/test_generator_search_integration.py tests/test_generator_telemetry.py tests/test_generator_real_hero.py` exits 0 and collects exactly 11 tests (3 + 3 + 2 + 1 + 1 + 1 = 11). Real-mode tests are collected but deselected when `-m "not real"` is passed.
    - Behavior assertion: `uv run pytest tests/test_generator_stub_determinism.py tests/test_generator_refusal.py tests/test_judge_structured_output.py tests/test_generator_search_integration.py tests/test_generator_telemetry.py -ra -q -m "not real"` exits 0 (no failed tests; xfailed report). The two `@pytest.mark.real` tests are deselected by `-m "not real"`.
    - Source assertion: every test function has `@pytest.mark.xfail(strict=True, reason="Wave N — ...")` — `grep -c "@pytest.mark.xfail(strict=True" tests/test_generator_stub_determinism.py` returns 3; `tests/test_generator_refusal.py` returns 3; `tests/test_judge_structured_output.py` returns 2; `tests/test_generator_search_integration.py` returns 1; `tests/test_generator_telemetry.py` returns 1; `tests/test_generator_real_hero.py` returns 1.
    - Source assertion: `tests/test_judge_structured_output.py` and `tests/test_generator_real_hero.py` both contain `@pytest.mark.real` (real-mode markers) — `grep -c "@pytest.mark.real" tests/test_judge_structured_output.py` returns at least 1; `tests/test_generator_real_hero.py` returns 1.
    - Source assertion: NO file in this task contains `pytestmark = pytest.mark.real` at module top — `! grep -q "^pytestmark = pytest.mark.real" tests/test_generator_real_hero.py tests/test_judge_structured_output.py` (Phase 5 anti-pattern from `test_reranker_canary.py:52-65` rationale).
    - Source assertion: every test file has a module docstring (`grep -A1 "^\"\"\"" tests/test_generator_*.py tests/test_judge_*.py | head` shows non-empty content).
  </acceptance_criteria>
  <done>Six test files exist with a total of 11 xfail-strict test functions covering GEN-03 (3), GEN-04 (3), D-09 (2 — one real, one stub-mockable), D-14 + hero stub (1), D-16 (1), and real-mode hero (1). All collectable by pytest; stub-mode tests xfailed; real-mode tests collected but deselected from `-m "not real"` CI. Will flip to xpass as Plans 06-04 (Generator + telemetry), 06-06 (judge migration), and 06-07 (real-mode promotion) land.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `uv run pytest -ra -q -m "not real"` exits 0 across the full workspace (xfailed counts grow by approximately 17 from Wave 0 scaffolds; failed = 0; error = 0). Current Phase 5 baseline is 121 passed; after Wave 0, expect ~121 passed + ~17 xfailed.
2. `uv run pytest --collect-only -q tests/` exits 0; total test count grew by 21 (10 in Task A + 11 in Task B).
3. `git status --short tests/` shows 10 new test files (`tests/test_*.py`) + 2 new fixture files under `tests/fixtures/prompt_locality_violations*/`. No other directories touched.
4. `! git diff --stat src/` and `! git diff --stat packages/` — no production code changes (Wave 0 is tests + fixtures only).
5. `grep -rn "pytestmark = pytest.mark.real" tests/test_generator*.py tests/test_judge*.py` returns no matches (Phase 5 anti-pattern stays avoided).
</verification>

<success_criteria>
Wave 0 ships green stub-mode CI:
- 10 test files exist with 21 xfail-strict test functions total
- 2 fixture directories exist with deliberate violations + escape-hatch demonstration
- `uv run pytest -m "not real"` exits 0
- Every requirement (GEN-01..04) + every Phase 6 decision listed in 06-VALIDATION.md has a collectable xfail target
- Wave 1+ plans flip xfails to xpass as production code lands; Plan 06-07's xfail-removal sweep is the final acceptance gate
</success_criteria>

<output>
After completion, create `.planning/phases/06-generation/06-01-SUMMARY.md` documenting:
- Test files created (10 + 2 fixture files)
- Test function count per file and the xfail reason naming convention used
- Wave-flip mapping: which Wave/Plan promotes each xfail to xpass
- Any deviations from the analog patterns (e.g., if `capture_logs` ended up using a different API than expected)
- Confirmation that production code remains untouched
</output>
