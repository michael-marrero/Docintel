---
phase: 05-retrieval-hybrid-rerank
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/docintel-retrieve/pyproject.toml
  - packages/docintel-retrieve/src/docintel_retrieve/__init__.py
  - packages/docintel-retrieve/src/docintel_retrieve/py.typed
  - packages/docintel-core/src/docintel_core/types.py
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [RET-04]
must_haves:
  truths:
    - "New 7th workspace package `docintel-retrieve` exists at packages/docintel-retrieve/ with pyproject.toml, src/docintel_retrieve/__init__.py, and src/docintel_retrieve/py.typed (empty file)"
    - "RetrievedChunk Pydantic v2 model is added to docintel_core.types per CD-02 with the seven D-03 fields (chunk_id, text, score, ticker, fiscal_year, item_code, char_span_in_section), ConfigDict(extra='forbid', frozen=True), and is re-exported from __all__"
    - "Workspace pyproject.toml [tool.uv.sources] block lists `docintel-retrieve = { workspace = true }` alphabetically between `docintel-index` and `docintel-ingest`"
    - "uv.lock regenerates cleanly and `uv lock --check` exits 0"
    - "tests/test_retrieved_chunk_schema.py (xfail scaffold from Plan 05-01) flips to all-pass with the xfail marker removed"
    - "tests/test_rrf_fuse.py and tests/test_null_adapters.py REMAIN xfailed (docintel_retrieve.fuse and docintel_retrieve.null_adapters still don't exist — they ship in Plans 05-03 and 05-04)"
  artifacts:
    - path: "packages/docintel-retrieve/pyproject.toml"
      provides: "Workspace package manifest; pins docintel-core + numpy explicit re-pin; NO [project.scripts] (library-only per D-01)"
    - path: "packages/docintel-retrieve/src/docintel_retrieve/__init__.py"
      provides: "Public re-export surface; re-exports RetrievedChunk from docintel_core.types; placeholder __all__ for Retriever/_rrf_fuse/RRF_K/NullReranker/NullBM25Store to be filled by Plans 05-03/04/05"
    - path: "packages/docintel-retrieve/src/docintel_retrieve/py.typed"
      provides: "PEP 561 marker — empty file (1 byte)"
    - path: "packages/docintel-core/src/docintel_core/types.py"
      provides: "RetrievedChunk Pydantic model added; __all__ extended with 'RetrievedChunk'"
  key_links:
    - from: "packages/docintel-retrieve/src/docintel_retrieve/__init__.py"
      to: "docintel_core.types.RetrievedChunk"
      via: "from docintel_core.types import RetrievedChunk re-export"
      pattern: "from docintel_core.types import RetrievedChunk"
    - from: "pyproject.toml (workspace root)"
      to: "packages/docintel-retrieve/"
      via: "[tool.uv.workspace].members = ['packages/*'] glob + [tool.uv.sources] explicit listing"
      pattern: "docintel-retrieve\\s*=\\s*\\{\\s*workspace\\s*=\\s*true\\s*\\}"
---

<objective>
Land the new 7th workspace package `docintel-retrieve` skeleton (pyproject + `__init__.py` + `py.typed`) and add the `RetrievedChunk` Pydantic v2 model to `docintel_core.types` per CD-02. This plan is the structural Wave 0 piece that downstream Wave 1 implementations (Plans 05-03..05-05) build on. No retrieval logic ships here — just the package shell, the shared contract type, and the workspace wiring so `uv lock --check` passes.

Purpose: D-01 (new workspace package) + CD-02 (RetrievedChunk in core) are structural decisions that have to land before Plans 05-03..05-05 can import from `docintel_retrieve.*` or instantiate `RetrievedChunk`. Splitting them into Wave 0 means Wave 1 plans can run in parallel without coordinating on the same files.

Output: 3 new files under `packages/docintel-retrieve/`; 1 new Pydantic class in `docintel_core/types.py`; workspace root pyproject extended with the new uv source; uv.lock regenerated. After this plan, `tests/test_retrieved_chunk_schema.py` flips from 4 xfailed to 4 passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md
@.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md
@.planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md
@.planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md

<interfaces>
<!-- Analog: existing docintel-index workspace package pyproject (Phase 4) -->

From `packages/docintel-index/pyproject.toml` (verified):
```toml
[project]
name = "docintel-index"
version = "0.1.0"
description = "Dense + BM25 index build/verify CLI for docintel"
requires-python = ">=3.11,<3.13"
dependencies = [
    "docintel-core",
    "bm25s==0.3.9",
    "PyStemmer==3.0.0",
    "qdrant-client==1.18.0",
]

[project.scripts]
docintel-index = "docintel_index.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/docintel_index"]
```

For docintel-retrieve adaptations:
- name: `docintel-retrieve`
- description: `Hybrid retrieval (BM25 + dense via RRF) + cross-encoder rerank seam for docintel`
- dependencies: `["docintel-core", "numpy"]` — Phase 5 has ZERO new third-party deps; numpy is re-pinned explicitly (FND-09 pattern). Cross-check the exact `numpy` pin string against `packages/docintel-core/pyproject.toml`'s numpy line (or the workspace lockfile) and pin to the SAME version (e.g. `numpy==2.4.4` if that's what docintel-core resolves to — the planner does not invent a version).
- OMIT `[project.scripts]` per D-01 (library-only; no CLI).
- `[tool.hatch.build.targets.wheel].packages = ["src/docintel_retrieve"]`

From `packages/docintel-core/src/docintel_core/types.py` (verified current state):
```python
__all__ = [
    "Chunk",
    "CompanyEntry",
    "IndexManifest",
    "IndexManifestBM25",
    "IndexManifestDense",
    "IndexManifestEmbedder",
    "NormalizedFiling",
    "NormalizedFilingManifest",
]
```

After this plan, `__all__` becomes:
```python
__all__ = [
    "Chunk",
    "CompanyEntry",
    "IndexManifest",
    "IndexManifestBM25",
    "IndexManifestDense",
    "IndexManifestEmbedder",
    "NormalizedFiling",
    "NormalizedFilingManifest",
    "RetrievedChunk",
]
```

From `packages/docintel-core/src/docintel_core/types.py` Chunk model (analog for RetrievedChunk):
```python
class Chunk(BaseModel):
    model_config = ConfigDict(extra="forbid")

    chunk_id: str
    ticker: str
    fiscal_year: int
    accession: str
    item_code: str
    item_title: str
    text: str
    char_span_in_section: tuple[int, int]
    n_tokens: int
    prev_chunk_id: str | None
    next_chunk_id: str | None
    sha256_of_text: str
```

From `pyproject.toml` workspace root (verified current `[tool.uv.sources]`):
```toml
[tool.uv.sources]
docintel-core   = { workspace = true }
docintel-api    = { workspace = true }
docintel-ui     = { workspace = true }
docintel-eval   = { workspace = true }
docintel-index  = { workspace = true }
docintel-ingest = { workspace = true }
torch = [
    { index = "pytorch-cpu", marker = "sys_platform == 'linux'" },
]
```

Target state — insert `docintel-retrieve = { workspace = true }` alphabetically between `docintel-index` and `docintel-ingest`:
```toml
docintel-index    = { workspace = true }
docintel-retrieve = { workspace = true }
docintel-ingest   = { workspace = true }
```

(Note: `docintel-ingest` sorts AFTER `docintel-index` lexicographically, so the alphabetical pin is between `docintel-index` and `docintel-ingest`. Verify against current file before editing.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docintel-retrieve workspace package skeleton + wire into workspace root</name>
  <files>packages/docintel-retrieve/pyproject.toml, packages/docintel-retrieve/src/docintel_retrieve/__init__.py, packages/docintel-retrieve/src/docintel_retrieve/py.typed, pyproject.toml, uv.lock</files>
  <read_first>
    - packages/docintel-index/pyproject.toml (exact pyproject analog — verified at /Users/michaelmarrero/GitHub/Docintel/packages/docintel-index/pyproject.toml)
    - packages/docintel-index/src/docintel_index/__init__.py (re-export shape analog — module docstring + `__all__` list pattern)
    - packages/docintel-index/src/docintel_index/py.typed (empty file analog — `ls -la` confirms 0-1 byte)
    - packages/docintel-core/pyproject.toml (pin source of truth for numpy — copy the exact version pin)
    - pyproject.toml (workspace root — current `[tool.uv.sources]` block to be extended)
    - .planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md (sections "### `packages/docintel-retrieve/pyproject.toml`" lines 41-77 and "### `packages/docintel-retrieve/src/docintel_retrieve/__init__.py`" lines 80-117 and "### `pyproject.toml` (workspace root)" lines 650-678)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-01 module layout; CD-02 RetrievedChunk re-export shape)
  </read_first>
  <action>
    Create three new files under `packages/docintel-retrieve/` and modify the workspace root pyproject.

    **`packages/docintel-retrieve/pyproject.toml`** — exact analog of `packages/docintel-index/pyproject.toml` with three substitutions: (1) `name = "docintel-retrieve"`, (2) `description = "Hybrid retrieval (BM25 + dense via RRF) + cross-encoder rerank seam for docintel"`, (3) `dependencies = ["docintel-core", "numpy==<EXACT_VERSION>"]` where `<EXACT_VERSION>` is read from `packages/docintel-core/pyproject.toml`'s numpy pin (do not invent — copy verbatim; if docintel-core resolves numpy transitively without an explicit pin, omit the version specifier and use `numpy` alone). OMIT the `[project.scripts]` block — Phase 5 is library-only per D-01 (the CLI lives in Phase 10/13). Keep `[build-system]` (hatchling), and `[tool.hatch.build.targets.wheel].packages = ["src/docintel_retrieve"]`. Version `0.1.0`, `requires-python = ">=3.11,<3.13"`.

    **`packages/docintel-retrieve/src/docintel_retrieve/__init__.py`** — module-docstring + `__all__` + re-export pattern, mirrors `packages/docintel-index/src/docintel_index/__init__.py`. Module docstring is the narrative shape: "Phase 5 implementation — hybrid retrieval (BM25 + dense via RRF) + cross-encoder rerank seam. Composes Phase 2 `AdapterBundle` + Phase 4 `IndexStoreBundle` into a single `Retriever.search(query, k) -> list[RetrievedChunk]` seam. See `docintel_retrieve.retriever` for the orchestrator class; `docintel_retrieve.fuse` for the pure RRF helper; `docintel_retrieve.null_adapters` for the Phase 11 ablation seam (NullReranker + NullBM25Store)." Public surface — `__all__` lists `["NullBM25Store", "NullReranker", "RRF_K", "RetrievedChunk", "Retriever", "_rrf_fuse"]` (alphabetical). Re-exports:
    - `from docintel_core.types import RetrievedChunk` — eager re-export (CD-02 Option A — RetrievedChunk lives in core; this is the convenience import).
    - The other five names (`Retriever`, `_rrf_fuse`, `RRF_K`, `NullReranker`, `NullBM25Store`) are referenced in `__all__` but the actual import statements are wrapped in `try: from docintel_retrieve.retriever import Retriever; except ImportError: pass` style guards — Wave 0 ships the skeleton; Plans 05-03/04/05 land the modules. Once Wave 1 lands, the try/except guards are removed by Plan 05-05's final wave-flip task (or by the consumer Plans 05-03/04/05 each adding their own clean import).

    Actually — simpler and cleaner: ship Wave 0 `__init__.py` with ONLY the `RetrievedChunk` re-export and `__all__ = ["RetrievedChunk"]`. Plans 05-03/04/05 EACH extend `__init__.py` and `__all__` as their modules land. This avoids the try/except gymnastics and matches the pattern used by Phase 4 Plan 04-03 (which extended the `docintel_index/__init__.py` incrementally).

    Final Wave 0 `__init__.py` content: module docstring + `from docintel_core.types import RetrievedChunk` + `__all__ = ["RetrievedChunk"]`. Done.

    **`packages/docintel-retrieve/src/docintel_retrieve/py.typed`** — empty file (PEP 561 marker). Create as 0-byte or 1-byte (newline). Match `packages/docintel-index/src/docintel_index/py.typed` (verify size via `ls -la`).

    **`pyproject.toml` (workspace root)** — extend `[tool.uv.sources]` block. Insert `docintel-retrieve = { workspace = true }` between `docintel-index` and `docintel-ingest` (alphabetical position). Use the Edit tool — the existing block has aligned `=` signs; preserve the visual alignment (the new line is wider than `docintel-index` by one character, so re-align all six existing lines OR insert without re-aligning — planner picks; `uv lock` doesn't care about whitespace). The `[tool.uv.workspace].members = ["packages/*"]` glob ALREADY auto-discovers the new package — do not modify that line.

    After the file changes: run `uv lock` (NOT `uv sync` — lock-only). This regenerates `uv.lock` with the new workspace member. Commit `uv.lock` as part of this plan (FND-09 + Phase 4 pattern).
  </action>
  <verify>
    <automated>test -f packages/docintel-retrieve/pyproject.toml && test -f packages/docintel-retrieve/src/docintel_retrieve/__init__.py && test -f packages/docintel-retrieve/src/docintel_retrieve/py.typed && grep -q '^name = "docintel-retrieve"$' packages/docintel-retrieve/pyproject.toml && grep -q '"docintel-core"' packages/docintel-retrieve/pyproject.toml && ! grep -q "^\[project.scripts\]" packages/docintel-retrieve/pyproject.toml && grep -q "^docintel-retrieve\s*=\s*{ workspace = true }" pyproject.toml && uv lock --check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/docintel-retrieve/pyproject.toml` contains `name = "docintel-retrieve"`, `version = "0.1.0"`, `requires-python = ">=3.11,<3.13"`, and `[build-system]` with `requires = ["hatchling"]` and `build-backend = "hatchling.build"`.
    - Source assertion: `dependencies` list in `packages/docintel-retrieve/pyproject.toml` contains exactly `"docintel-core"` AND a numpy specifier (either `"numpy"` or `"numpy==<version>"` where `<version>` matches the docintel-core pin). No `bm25s`, `qdrant-client`, or `sentence-transformers` — these ride transitively.
    - Source assertion: NO `[project.scripts]` block in `packages/docintel-retrieve/pyproject.toml` (verified by `! grep "^\[project.scripts\]" packages/docintel-retrieve/pyproject.toml`).
    - Source assertion: `packages/docintel-retrieve/src/docintel_retrieve/__init__.py` contains the import `from docintel_core.types import RetrievedChunk` and `__all__ = ["RetrievedChunk"]`.
    - Behavior: `packages/docintel-retrieve/src/docintel_retrieve/py.typed` is an empty or 1-byte file (verified by `wc -c packages/docintel-retrieve/src/docintel_retrieve/py.typed` returns 0 or 1).
    - Source assertion: workspace root `pyproject.toml` `[tool.uv.sources]` block contains the line `docintel-retrieve = { workspace = true }` (alphabetically placed between `docintel-index` and `docintel-ingest`).
    - CLI: `uv lock --check` exits 0 (lockfile is in sync with all 7 workspace members).
    - Behavior: `uv run python -c "import docintel_retrieve; print(docintel_retrieve.__all__)"` prints `['RetrievedChunk']` (after the package is locked but BEFORE RetrievedChunk class exists yet — Task 2 adds the class; this verify step should be AFTER Task 2 completes, OR the import should fail gracefully here because RetrievedChunk doesn't exist yet; planner orders tasks accordingly).
  </acceptance_criteria>
  <done>New workspace package skeleton landed; `uv lock --check` exits 0; the workspace has 7 members (was 6); `docintel_retrieve` is importable but exports only the (yet-to-be-added) `RetrievedChunk` re-export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RetrievedChunk Pydantic v2 model to docintel_core.types and extend __all__</name>
  <files>packages/docintel-core/src/docintel_core/types.py</files>
  <read_first>
    - packages/docintel-core/src/docintel_core/types.py (full file — read once; lines 1-50 for `__all__` block, lines 104-145 for the `Chunk` analog, lines 147-160 for `IndexManifestEmbedder` short-model analog)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-03 exact seven-field shape; CD-02 placement decision; D-16 `char_span_in_section` semantics)
    - .planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md (section "### `packages/docintel-core/src/docintel_core/types.py` (MODIFY — add `RetrievedChunk`)" lines 468-531; Pattern S1 ConfigDict pattern at lines 1169-1186)
    - tests/test_retrieved_chunk_schema.py (xfail scaffold from Plan 05-01 — the contract this task makes pass)
  </read_first>
  <behavior>
    - Test 1: `RetrievedChunk(chunk_id="AAPL-FY2024-Item-1A-007", text="Some chunk text.", score=0.812, ticker="AAPL", fiscal_year=2024, item_code="Item 1A", char_span_in_section=(100, 250))` constructs successfully and round-trips the seven fields.
    - Test 2: `RetrievedChunk(...)` with an unknown extra field (e.g. `bm25_rank=3`) raises `pydantic.ValidationError` (extra="forbid").
    - Test 3: `rc.score = 0.99` after construction raises `pydantic.ValidationError` (frozen=True).
    - Test 4: `rc.char_span_in_section` is `tuple[int, int]` after round-trip (matches Phase 3 D-16 anchor type).
    - Test 5: `from docintel_core.types import RetrievedChunk` succeeds AND `"RetrievedChunk" in docintel_core.types.__all__`.
  </behavior>
  <action>
    Edit `packages/docintel-core/src/docintel_core/types.py` to add the `RetrievedChunk` Pydantic v2 model. Two specific edits:

    **Edit 1 — extend `__all__`:** Insert `"RetrievedChunk",` into the `__all__` list at the top of the file (around line 42). Maintain alphabetical order — the existing list is alphabetical, so `RetrievedChunk` slots between `NormalizedFilingManifest` and (no more entries follow currently). New `__all__`:
    ```python
    __all__ = [
        "Chunk",
        "CompanyEntry",
        "IndexManifest",
        "IndexManifestBM25",
        "IndexManifestDense",
        "IndexManifestEmbedder",
        "NormalizedFiling",
        "NormalizedFilingManifest",
        "RetrievedChunk",
    ]
    ```

    **Edit 2 — add the model class:** Append the `RetrievedChunk` class definition AFTER the existing `Chunk` class (around line 145, just before `class IndexManifestEmbedder(BaseModel):`). The class MUST contain:

    - Class docstring referencing D-03 (the seven required fields), D-16 (`char_span_in_section` is the citation anchor — Phase 7's `Citation` will render the chunk text inline and offer an "expand" affordance that highlights the span), CD-02 (lives in `docintel_core.types` so Phase 6/7/13 import without depending on the retrieve package), and "frozen=True" (downstream callers must not mutate the result list).
    - `model_config = ConfigDict(extra="forbid", frozen=True)` — per Pattern S1 + anti-pattern "Don't make RetrievedChunk mutable" (RESEARCH.md line 626).
    - Exactly seven fields, in this order matching D-03:
      - `chunk_id: str`
      - `text: str`
      - `score: float` (with comment "final reranker score (or RRF score in no-rerank ablation)")
      - `ticker: str`
      - `fiscal_year: int`
      - `item_code: str` (with comment 'e.g., "Item 1A" — matches Phase 3 Chunk.item_code')
      - `char_span_in_section: tuple[int, int]` (with comment "citation anchor — Phase 3 D-16")

    NO additional fields (D-03 explicitly forbids per-stage debug fields like `bm25_rank`, `dense_rank`, `rrf_score`, `rerank_score` on the public shape; the anti-pattern in RESEARCH.md line 622 makes this binding).

    Do NOT add any new imports — the file already has `from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator`. No `field_validator` or `model_validator` needed for RetrievedChunk (D-03 has no cross-field invariants).

    After this edit, the four-test scaffold in `tests/test_retrieved_chunk_schema.py` from Plan 05-01 (currently xfail) flips to all-pass. The xfail markers on those four functions MUST be removed in this same task — find the four `@pytest.mark.xfail(strict=True, reason="Wave N — Plan 05-02 ...")` decorators in `tests/test_retrieved_chunk_schema.py` and delete them (or the test_retrieved_chunk_schema.py tests will fail with "XPASS strict" once the model exists; strict=True means xpass is treated as failure).

    Verify the `docintel_retrieve` package's __init__.py re-export (from Task 1) now resolves — `python -c "from docintel_retrieve import RetrievedChunk; print(RetrievedChunk)"` should succeed and print the class object.
  </action>
  <verify>
    <automated>uv run python -c "from docintel_core.types import RetrievedChunk; rc = RetrievedChunk(chunk_id='X-FY2024-Item-1A-001', text='t', score=0.5, ticker='X', fiscal_year=2024, item_code='Item 1A', char_span_in_section=(0, 10)); assert rc.chunk_id == 'X-FY2024-Item-1A-001'; assert rc.char_span_in_section == (0, 10); assert isinstance(rc.char_span_in_section, tuple); print('OK')" && uv run python -c "from docintel_core.types import RetrievedChunk; from pydantic import ValidationError; raised=False
try:
    RetrievedChunk(chunk_id='X-FY2024-Item-1A-001', text='t', score=0.5, ticker='X', fiscal_year=2024, item_code='Item 1A', char_span_in_section=(0, 10), bm25_rank=3)
except ValidationError:
    raised=True
assert raised, 'extra=forbid not enforced'; print('OK')" && uv run python -c "from docintel_core.types import RetrievedChunk; from pydantic import ValidationError; rc=RetrievedChunk(chunk_id='X-FY2024-Item-1A-001', text='t', score=0.5, ticker='X', fiscal_year=2024, item_code='Item 1A', char_span_in_section=(0, 10)); raised=False
try:
    rc.score = 0.99
except ValidationError:
    raised=True
assert raised, 'frozen=True not enforced'; print('OK')" && uv run python -c "import docintel_core.types as t; assert 'RetrievedChunk' in t.__all__, 'not in __all__'; print('OK')" && uv run python -c "from docintel_retrieve import RetrievedChunk; print('OK')" && uv run pytest tests/test_retrieved_chunk_schema.py -ra -q 2>&1 | grep -qE "4 passed"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `class RetrievedChunk(BaseModel):` appears in `packages/docintel-core/src/docintel_core/types.py` (verified by `grep -c "^class RetrievedChunk(BaseModel):" packages/docintel-core/src/docintel_core/types.py` returns 1).
    - Source assertion: The class has `model_config = ConfigDict(extra="forbid", frozen=True)` (verified by `grep -A1 "^class RetrievedChunk" packages/docintel-core/src/docintel_core/types.py | grep -c 'frozen=True'` returns 1).
    - Source assertion: Exactly seven fields are declared on the class (chunk_id, text, score, ticker, fiscal_year, item_code, char_span_in_section). NO bm25_rank, dense_rank, rrf_score, or rerank_score fields.
    - Source assertion: `"RetrievedChunk"` is added to the `__all__` list at the top of types.py (alphabetical position after `NormalizedFilingManifest`).
    - Behavior: `RetrievedChunk` instances are constructable with all seven fields, reject extra fields (ValidationError), reject post-construction mutation (ValidationError on frozen), and `char_span_in_section` round-trips as a `tuple[int, int]` (not converted to list).
    - Test command: `uv run pytest tests/test_retrieved_chunk_schema.py -ra -q` reports 4 passed, 0 failed, 0 xfailed. The four xfail markers from Plan 05-01 have been deleted.
    - Behavior: `uv run python -c "from docintel_retrieve import RetrievedChunk; print(RetrievedChunk)"` prints the class object (cross-package import works through the re-export added in Task 1).
    - CLI: `uv run mypy --strict packages/docintel-core/src/docintel_core/types.py` exits 0 (no new type errors introduced).
  </acceptance_criteria>
  <done>`RetrievedChunk` Pydantic v2 model exists in `docintel_core.types` with seven D-03 fields, `extra="forbid"` + `frozen=True`, and is in `__all__`; the schema test file flips from 4 xfailed to 4 passed; mypy --strict remains green; cross-package import via `docintel_retrieve` works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for Plan 05-02) | This plan ships a Pydantic model and a workspace package skeleton. No new SDK call, no I/O, no network, no env-var read. Threat surface for the plan itself is empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-V5-01-shape | Tampering | `RetrievedChunk` schema drift (per-stage debug fields leak onto public shape) | mitigate | `ConfigDict(extra="forbid")` rejects any future PR that tries to add `bm25_rank` / `dense_rank` / `rrf_score` / `rerank_score` to the public model — `tests/test_retrieved_chunk_schema.py::test_retrieved_chunk_forbids_extra` is the bite point. D-03 anti-pattern in RESEARCH.md line 622 forbids this. |
| T-5-V5-mutability | Tampering | `RetrievedChunk` post-construction mutation | mitigate | `ConfigDict(frozen=True)` blocks `rc.score = X` after construction; `tests/test_retrieved_chunk_schema.py::test_retrieved_chunk_is_frozen` enforces. |
</threat_model>

<verification>
1. `uv lock --check` exits 0 (lockfile in sync; new 7th workspace member resolved cleanly).
2. `uv run mypy --strict` clean across all 43 source files (this plan adds one Pydantic class; no new type errors should appear).
3. `uv run pytest tests/test_retrieved_chunk_schema.py -ra -q` reports 4 passed.
4. `uv run pytest -ra -q` — full suite green; xfail count drops by 4 from Plan 05-01 baseline (the four RetrievedChunk schema tests flip to passed); all other Plan 05-01 xfails remain xfailed.
5. `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — three grep gates still exit 0.
6. `uv run python -c "from docintel_retrieve import RetrievedChunk; print(RetrievedChunk)"` succeeds.
</verification>

<success_criteria>
- New 7th workspace package `docintel-retrieve` exists with pyproject + `__init__.py` re-exporting `RetrievedChunk` + empty `py.typed`.
- `uv lock --check` exits 0; workspace root pyproject lists `docintel-retrieve` in `[tool.uv.sources]` alphabetically.
- `RetrievedChunk` Pydantic v2 model lives in `docintel_core.types` with the exact seven D-03 fields, `extra="forbid" + frozen=True`, and is in `__all__`.
- Plan 05-01's `tests/test_retrieved_chunk_schema.py` flips from 4 xfailed to 4 passed; xfail markers deleted.
- No new third-party deps; no new env vars; no Settings changes; `mypy --strict` green; all three CI grep gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-hybrid-rerank/05-02-SUMMARY.md` with: files created, files modified, exact numpy pin used in docintel-retrieve/pyproject.toml, the verbatim RetrievedChunk class source (so downstream plans can grep against the field list), uv.lock diff summary (line count change), and the four xfail markers deleted from tests/test_retrieved_chunk_schema.py.
</output>
