---
phase: 05-retrieval-hybrid-rerank
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - packages/docintel-retrieve/src/docintel_retrieve/retriever.py
  - packages/docintel-retrieve/src/docintel_retrieve/__init__.py
  - packages/docintel-core/src/docintel_core/adapters/factory.py
  - packages/docintel-core/src/docintel_core/adapters/__init__.py
  - tests/test_retriever_search.py
  - tests/test_make_retriever.py
autonomous: true
requirements: [RET-02, RET-04]
must_haves:
  truths:
    - "Retriever.search(query, k) executes the BM25 → dense → RRF → rerank → top-K pipeline in one call (D-02 single seam)"
    - "Pipeline parameters pinned as module-level Final[int] constants — TOP_N_PER_RETRIEVER=100 (D-05), TOP_M_RERANK=20 + TOP_K_FINAL=5 (D-06); ablation seams use null adapters (D-08) rather than env-var sweeps"
    - "Reranker receives raw chunk.text verbatim — no item_title prefix, no pre-truncation (D-09); chain of custody depends on Phase 3 chunker's 500-token hard cap (re-verified by D-10 assertion)"
    - "make_retriever(cfg) in docintel_core.adapters.factory composes make_adapters(cfg) + make_index_stores(cfg) and constructs Retriever; the lazy from docintel_retrieve.retriever import Retriever lives inside the function body (D-04 + D-12)"
    - "Retriever.__init__ eager-loads chunk_id → Chunk map from data/corpus/chunks/**/*.jsonl AND verifies cardinality against data/indices/MANIFEST.json chunk_count (CD-01 + Pitfall 7)"
    - "Pre-rerank assertion `chunk.n_tokens <= 500` fires with verbatim CLAUDE.md quote in the AssertionError message on every candidate in the rerank loop (D-10 + Pitfall 6)"
    - "Query is truncated at 64 BGE tokens BEFORE embedder.embed; truncation emits retriever_query_truncated structlog warning (D-11)"
    - "retriever_search_completed structlog line emits with the twelve D-12 fields (query_tokens, query_truncated, bm25_candidates, dense_candidates, rrf_unique, rerank_input, results_returned, bm25_ms, dense_ms, fuse_ms, rerank_ms, total_ms)"
    - "Zero-candidate path returns [] with retriever_search_zero_candidates structlog warning (CD-07)"
    - "Real-mode soft warning chunk_reranker_token_overflow fires only when reranker name == 'bge-reranker-base' and a chunk's XLM-RoBERTa-tokenized count > 500 (RESEARCH §3 defense-in-depth)"
    - "RetrievedChunk results carry exactly the seven D-03 fields with reranker score (or RRF score in no-rerank ablation)"
    - "Retriever composes already-wrapped adapter calls and adds NO new tenacity wraps (CD-04); the three CI grep gates (check_adapter_wraps, check_index_wraps, check_ingest_wraps) remain unchanged"
    - "tests/test_retriever_search.py + tests/test_make_retriever.py xfails from Plan 05-01 flip to all-pass with xfail markers removed"
  artifacts:
    - path: "packages/docintel-retrieve/src/docintel_retrieve/retriever.py"
      provides: "Retriever orchestrator class with .search seam; TOP_N_PER_RETRIEVER=100, TOP_M_RERANK=20, TOP_K_FINAL=5, QUERY_TOKEN_HARD_CAP=64 module constants; _CLAUDE_MD_HARD_GATE module constant for the D-10 assertion text; private _truncate_query, _load_chunk_map, _check_reranker_token_overflow helpers"
    - path: "packages/docintel-core/src/docintel_core/adapters/factory.py"
      provides: "make_retriever(cfg) function — third sibling factory; lazy import of Retriever inside the body; string forward reference annotation"
    - path: "packages/docintel-core/src/docintel_core/adapters/__init__.py"
      provides: "__all__ extended with 'make_retriever' re-export (alphabetical between make_adapters and end)"
    - path: "packages/docintel-retrieve/src/docintel_retrieve/__init__.py"
      provides: "__all__ extended with 'Retriever' re-export"
  key_links:
    - from: "packages/docintel-core/src/docintel_core/adapters/factory.py::make_retriever"
      to: "packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever"
      via: "Lazy import inside function body (D-12 + Pattern S5)"
      pattern: "from docintel_retrieve.retriever import Retriever"
    - from: "Retriever._load_chunk_map"
      to: "data/corpus/chunks/**/*.jsonl + data/indices/MANIFEST.json"
      via: "rglob+Chunk.model_validate_json then cardinality check against MANIFEST.chunk_count (Pitfall 7)"
      pattern: "rglob.*\\*\\.jsonl|MANIFEST\\.json"
    - from: "Retriever rerank loop"
      to: "CLAUDE.md hard-gate paragraph"
      via: "AssertionError message embeds verbatim _CLAUDE_MD_HARD_GATE constant (Pitfall 6)"
      pattern: "BGE 512-token truncation FIRST"
---

<objective>
Land the `Retriever` orchestrator class (the single-seam `.search()` callable that closes RET-02 + RET-04) AND the `make_retriever(cfg)` factory in `docintel_core.adapters.factory` (D-04). This is the load-bearing implementation plan for Phase 5 — every public API consumer in Phase 6/10/13 calls `Retriever.search()` (or the factory). The plan composes the existing adapter and store protocols, the RRF helper from Plan 05-03, the null adapters from Plan 05-04, and the `RetrievedChunk` model from Plan 05-02 into the production pipeline. NO new tenacity wraps, NO new Settings fields, NO new SDK call sites — Phase 5 is a composition layer.

Purpose: D-02's "one callable, end-to-end" seam is the API contract every downstream phase consumes. Splitting `Retriever.search` from `make_retriever(cfg)` into one plan rather than two means the factory's eager-load semantics (CD-01) are testable in the same plan as the search pipeline. The D-10/D-11/D-12 silent-truncation defenses (the canary's structural preconditions) all live in `retriever.py`.

Output: 1 new file (`retriever.py`); 1 modified file (`docintel_core.adapters.factory.py`) adds `make_retriever`; both package `__init__.py` files extended; 5 xfails in `tests/test_retriever_search.py` and 2 xfails in `tests/test_make_retriever.py` flip to passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md
@.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md
@.planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md
@.planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md

<interfaces>
Pipeline contract (D-02 + D-05/06/07/09/10/11/12 — the full Step A..G in RESEARCH.md Pattern 2 architecture diagram lines 174-264):
- Step A query normalization: tokenize+truncate at 64 BGE tokens (D-11), emit `retriever_query_truncated` warn if truncated, then `q_vec = embedder.embed([truncated_query])[0]`.
- Step B parallel candidate retrieval: `bm25.query(truncated_query, 100)` + `dense.query(q_vec, 100)` (D-05 top-N=100 each); time each with `time.perf_counter()`.
- Step C RRF fusion: `_rrf_fuse(bm25_results, dense_results)` from Plan 05-03; pure function with RRF_K=60.
- Step C zero-candidate guard (CD-07): if fused is `[]`, emit `retriever_search_zero_candidates` warn and return `[]`.
- Step D top-M lookup + n_tokens assertion: take top-20 chunk_ids (D-06 TOP_M_RERANK=20), look up Chunk in `self._chunk_map`, assert `chunk.n_tokens <= 500` per chunk with the verbatim CLAUDE.md quote in the AssertionError message (D-10 + Pitfall 6).
- Step D defense-in-depth (RESEARCH §3 + Pitfall 1): if `bundle.reranker.name == "bge-reranker-base"`, lazily access `bundle.reranker._model.tokenizer` and for each chunk compute `len(tokenizer.encode(chunk.text, truncation=False))`; if any > 500, emit `chunk_reranker_token_overflow` warning structlog line. Stub mode skips this.
- Step E rerank: `reranker.rerank(truncated_query, [chunk.text for chunk in top_m_chunks])` (D-09 raw chunk.text verbatim; no prefix).
- Step F top-K post-processing: take `reranked[:k]`; for each `RerankedDoc`, look up the chunk via the doc_id index into top_m_chunks; build a `RetrievedChunk` with the seven D-03 fields (score = `reranked_doc.score`).
- Step G telemetry: emit `retriever_search_completed` structlog info line with the twelve D-12 fields verbatim.

Eager load contract (CD-01 + Pitfall 7):
- `__init__` reads `data/corpus/chunks/**/*.jsonl` via `sorted(Path(cfg.data_dir, "corpus/chunks").rglob("*.jsonl"))` and parses every non-empty line with `Chunk.model_validate_json`.
- Builds `self._chunk_map: dict[str, Chunk]` keyed by `chunk.chunk_id`.
- Reads `data/indices/MANIFEST.json` → `chunk_count` field (Phase 4 IndexManifest.chunk_count exists per Phase 4 D-13 schema) and asserts `len(self._chunk_map) == chunk_count`; mismatch raises `ValueError` with the explicit fix path "Phase 3 corpus drifted vs Phase 4 indices. Run `uv run docintel-index build` to rebuild indices, OR `uv run docintel-ingest all` to rebuild chunks." (RESEARCH.md lines 837-849 verbatim).
- Triggers warm-up calls `self._stores.dense.query(np.zeros(384, dtype=np.float32), k=1)` AND `self._stores.bm25.query("warmup", k=1)` to fire each store's `_lazy_load_from_disk()` path during construction (RESEARCH §5 recommendation; first-query latency property).

Constants (D-05/06/11 — module-level Final[int]):
- `TOP_N_PER_RETRIEVER: Final[int] = 100`
- `TOP_M_RERANK: Final[int] = 20`
- `TOP_K_FINAL: Final[int] = 5`
- `QUERY_TOKEN_HARD_CAP: Final[int] = 64`

`_CLAUDE_MD_HARD_GATE: Final[str]` module constant (Pitfall 6) — verbatim text that MUST contain the substrings "BGE 512-token truncation FIRST", "before suspecting hybrid retrieval, RRF, or chunk size", AND "the canary exists specifically to catch it". This constant is reused in both the D-10 assertion message AND Plan 05-06's canary failure message.

Token-counting helper (Open Question 1 from RESEARCH):
- Use the EMBEDDER tokenizer for the 64-token cap (D-11 specifies "BGE tokens"). The BGE-small embedder's `_model` is a `SentenceTransformer` with `.tokenizer` attribute.
- For stub embedder, approximate via `len(text.lower().split())` (within 1-2x of BERT WordPiece — fine for a defensive cap; the cap is loose enough that approximation doesn't break it).
- The cleanest seam is `Retriever._truncate_query(query: str) -> tuple[str, int]` returning `(maybe_truncated_text, original_token_count)`. Branching is based on `bundle.embedder.name` — `"stub-embedder"` uses the approximation path; `"bge-small-en-v1.5"` uses the real tokenizer attribute.
- Truncation strategy: count tokens; if `> 64`, truncate by token then decode back to text via the tokenizer's decode method (real mode) or by whitespace split slice (stub mode).

Existing types and adapters (verified at session start in `packages/docintel-core/src/docintel_core/`):
- `AdapterBundle(embedder, reranker, llm, judge)` Pydantic model at `adapters/types.py`.
- `IndexStoreBundle(dense, bm25)` Pydantic model at `adapters/types.py`.
- `RerankedDoc(doc_id: str, text: str, score: float, original_rank: int)` at `adapters/types.py`.
- `Chunk` model at `docintel_core.types` — `chunk_id`, `ticker`, `fiscal_year`, `accession`, `item_code`, `item_title`, `text`, `char_span_in_section: tuple[int, int]`, `n_tokens`, `prev_chunk_id`, `next_chunk_id`, `sha256_of_text`.
- `RetrievedChunk` model at `docintel_core.types` (added by Plan 05-02) — `chunk_id`, `text`, `score`, `ticker`, `fiscal_year`, `item_code`, `char_span_in_section`.
- `make_adapters(cfg) -> AdapterBundle` at `adapters/factory.py` (Phase 2 D-13).
- `make_index_stores(cfg) -> IndexStoreBundle` at `adapters/factory.py` (Phase 4 D-03).
- `Settings.data_dir` and `Settings.index_dir` are pre-existing Settings fields (Phase 1/4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Retriever class with eager chunk-map load, D-10/D-11 defenses, D-12 telemetry, and zero-candidate path</name>
  <files>packages/docintel-retrieve/src/docintel_retrieve/retriever.py, packages/docintel-retrieve/src/docintel_retrieve/__init__.py, tests/test_retriever_search.py</files>
  <read_first>
    - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md (Pattern 2 lines 394-536 — verbatim skeleton; §3 tokenizer drift + soft warning at lines 1126-1163; §5 lazy-load order at lines 1209-1234; §6 chunk_id → Chunk map memory + load time at lines 1236-1271; Pitfall 1 tokenizer-drift soft warning at lines 648-665; Pitfall 6 verbatim CLAUDE.md quote at lines 740-751; Pitfall 7 chunk-map cardinality check at lines 753-766; "Loading the chunk_id → Chunk map" code example at lines 814-857)
    - .planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md (section "### `packages/docintel-retrieve/src/docintel_retrieve/retriever.py`" lines 128-291; section "### `tests/test_retriever_search.py`" lines 1019-1099; Pattern S3 structlog logger at lines 1223-1232; Pattern S5 lazy-import discipline at lines 1249-1267)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-02 single seam; D-05/06 pipeline parameters; D-09/10/11/12 reranker defenses + query truncation + telemetry; CD-01 eager-load; CD-03 hold bundles; CD-04 no tenacity wraps; CD-05 BM25-rank tie-break in fuse; CD-06 reranker batch size default; CD-07 zero-candidate path; CD-08 no factory cache; CD-09 stub-mode runtime <2s)
    - CLAUDE.md (the verbatim Phase 5 hard-gate paragraph — must appear verbatim in `_CLAUDE_MD_HARD_GATE` module constant)
    - packages/docintel-core/src/docintel_core/adapters/protocols.py (Reranker, Embedder, DenseStore, BM25Store contracts)
    - packages/docintel-core/src/docintel_core/adapters/types.py (AdapterBundle, IndexStoreBundle, RerankedDoc shapes)
    - packages/docintel-core/src/docintel_core/types.py (Chunk fields, RetrievedChunk fields)
    - packages/docintel-core/src/docintel_core/config.py (Settings.data_dir and Settings.index_dir field existence + types)
    - packages/docintel-index/src/docintel_index/build.py (the analog orchestrator that already loads chunks JSONL — copy the `_read_all_chunks` pattern at lines 91-117 verbatim into `Retriever._load_chunk_map`; verify against the file's `_retry_log` + `log` two-logger pattern at lines 130-150)
    - packages/docintel-core/src/docintel_core/adapters/real/numpy_dense.py (lines 179-220 — `_lazy_load_from_disk` trigger point; lines 175-200 — `query` signature shape)
    - packages/docintel-core/src/docintel_core/adapters/real/bm25s_store.py (lines 64-280 — full method surface; lines 200-280 — `query` tokenizer state)
    - packages/docintel-core/src/docintel_core/adapters/real/reranker_bge.py (verify the `_model` attribute exposes `tokenizer` for the RESEARCH §3 soft warning)
    - tests/test_retriever_search.py (xfail scaffold from Plan 05-01 — the five contracts this task makes pass)
    - data/indices/MANIFEST.json (existing artifact — chunk_count field is what _load_chunk_map asserts against; verified to contain 6053)
  </read_first>
  <behavior>
    - Test 1 (`test_retriever_returns_top_k`): `make_retriever(Settings(llm_provider="stub")).search("any plausible question", k=5)` returns exactly 5 `RetrievedChunk` instances; each has all seven D-03 fields populated; scores are reranker-output scores (descending across the list).
    - Test 2 (`test_assertion_quotes_claude_md`): Constructing a Retriever, monkey-patching `self._chunk_map["X-FY2024-Item-1-001"].n_tokens = 501` (or equivalent injection), and triggering a search that pulls this chunk into top-M raises `AssertionError` whose message contains the EXACT substrings "BGE 512-token truncation FIRST" AND "before suspecting hybrid retrieval, RRF, or chunk size".
    - Test 3 (`test_query_truncation_logs`): `search(" ".join(["token"] * 200), k=5)` emits a structlog event with name `retriever_query_truncated` (captured via `structlog.testing.capture_logs()`), `original_tokens > 64`, `truncated_to == 64`.
    - Test 4 (`test_telemetry_fields`): `search("test query", k=5)` emits a `retriever_search_completed` event containing keys: `query_tokens`, `query_truncated`, `bm25_candidates`, `dense_candidates`, `rrf_unique`, `rerank_input`, `results_returned`, `bm25_ms`, `dense_ms`, `fuse_ms`, `rerank_ms`, `total_ms`. All ms fields are non-negative floats; all count fields are non-negative ints.
    - Test 5 (`test_zero_candidates`): Concrete induction — construct Retriever DIRECTLY (NOT via factory) with `bundle=AdapterBundle(...)` (any stub bundle) and `stores=IndexStoreBundle(bm25=NullBM25Store(), dense=NumpyDenseStore(tmp_path-rooted Settings where data/indices/MANIFEST.json is absent))`. Because `_load_chunk_map` skips the cardinality check when `MANIFEST.json` is missing (tmp_path corpus is empty), the chunk_map is `{}` and warm-up succeeds via the try/except guard. NullBM25Store.query returns `[]` for any query (Plan 05-04 contract); NumpyDenseStore.query on an empty index returns `[]`. Both stores produce zero candidates from `_rrf_fuse` (Plan 05-03 returns `[]` when both inputs are empty). The CD-07 zero-candidate guard triggers: `r.search('any query', k=5)` returns `[]` AND emits `retriever_search_zero_candidates` warning via `structlog.testing.capture_logs()`. The test asserts (a) `result == []`, (b) the warning event was captured with `event == 'retriever_search_zero_candidates'`. This induction is fully concrete — the executor does not need to discover how to produce zero fusion.
  </behavior>
  <action>
    Create `packages/docintel-retrieve/src/docintel_retrieve/retriever.py` and modify two files.

    **File 1 — `packages/docintel-retrieve/src/docintel_retrieve/retriever.py`** (new file):

    Module docstring describes the seam (D-02): "Query-time orchestrator. Composes the AdapterBundle + IndexStoreBundle into one `.search()` seam." Mention the full pipeline (Step A–G) and the design intent that ablation seams are adapter swaps not behavior toggles (D-08).

    Imports (ordered):
    - `from __future__ import annotations`
    - `import json`
    - `import time`
    - `from pathlib import Path`
    - `from typing import Final`
    - `import numpy as np`
    - `import structlog`
    - `from docintel_core.adapters.types import AdapterBundle, IndexStoreBundle, RerankedDoc`
    - `from docintel_core.config import Settings`
    - `from docintel_core.types import Chunk, RetrievedChunk` (RetrievedChunk per CD-02; added by Plan 05-02)
    - `from docintel_retrieve.fuse import _rrf_fuse` (added by Plan 05-03)

    NO `tenacity` import (CD-04). NO new SDK imports.

    Two-logger pattern (Pattern SP-3 from build.py — `_retry_log = logging.getLogger(__name__)` + `log = structlog.stdlib.get_logger(__name__)`). Actually Retriever does NOT need `_retry_log` (no tenacity); keep only `log = structlog.stdlib.get_logger(__name__)`.

    Module-level constants (D-05/06/11):
    - `TOP_N_PER_RETRIEVER: Final[int] = 100`
    - `TOP_M_RERANK: Final[int] = 20`
    - `TOP_K_FINAL: Final[int] = 5`
    - `QUERY_TOKEN_HARD_CAP: Final[int] = 64`

    Module-level `_CLAUDE_MD_HARD_GATE: Final[str] = ...` constant — multi-line triple-quoted string containing the verbatim CLAUDE.md hard-gate paragraph from CLAUDE.md "Hard sequencing constraints" section. MUST contain the substrings:
    - "BGE 512-token truncation FIRST"
    - "before suspecting hybrid retrieval, RRF, or chunk size"
    - "the canary exists specifically to catch it"
    Pitfall 6 mitigation: this constant is used in BOTH the D-10 chunk-loop assertion message AND (later) Plan 05-06's canary failure message via shared import.

    **Class `Retriever`** — public constructor `__init__(self, bundle: AdapterBundle, stores: IndexStoreBundle, cfg: Settings) -> None`. Implementation:
    1. Assign `self._bundle = bundle`, `self._stores = stores`, `self._cfg = cfg` (CD-03 hold bundles).
    2. Call `self._chunk_map = self._load_chunk_map()` (eager load per CD-01; populates `dict[str, Chunk]`).
    3. Trigger store lazy-loads with warm-up calls: `self._stores.dense.query(np.zeros(384, dtype=np.float32), k=1)` and `self._stores.bm25.query("warmup", k=1)` (RESEARCH §5 recommendation). Wrap each in `try/except Exception` to handle empty-store edge cases (no chunks indexed yet) — the warm-up failure must NOT prevent construction.

    **Method `search`** with signature `def search(self, query: str, k: int = TOP_K_FINAL) -> list[RetrievedChunk]:`. Implementation (the order is non-negotiable — Step A–G from RESEARCH.md Pattern 2 lines 433-516):

    1. `t0 = time.perf_counter()` — total timer.
    2. **Step A:** `truncated_query, q_tokens = self._truncate_query(query)`; emit `retriever_query_truncated` warn (`original_tokens=q_tokens, truncated_to=QUERY_TOKEN_HARD_CAP`) only when `q_tokens > QUERY_TOKEN_HARD_CAP`. Then `q_vec = self._bundle.embedder.embed([truncated_query])[0]`.
    3. **Step B:** `bm25_t0 = time.perf_counter(); bm25_results = self._stores.bm25.query(truncated_query, TOP_N_PER_RETRIEVER); bm25_ms = (time.perf_counter() - bm25_t0) * 1000`. Same for `dense_results` using `q_vec` and `TOP_N_PER_RETRIEVER`.
    4. **Step C:** `fuse_t0 = time.perf_counter(); fused = _rrf_fuse(bm25_results, dense_results); fuse_ms = ...`. If `not fused`, emit `retriever_search_zero_candidates` with `query=truncated_query[:100]` and return `[]` (CD-07 path; total_ms is not emitted in zero-candidate case — keep the telemetry contract clean by emitting `retriever_search_zero_candidates` instead).
    5. **Step D:** `top_m_ids = [cid for cid, _ in fused[:TOP_M_RERANK]]`. For each cid, get `self._chunk_map[cid]` (Pitfall 7 — KeyError is acceptable here because `__init__` already verified cardinality against MANIFEST; if a cid is somehow missing now, that's a genuine bug we want to surface). Append to `top_m_chunks: list[Chunk]`. Then assert per chunk: `assert chunk.n_tokens <= 500, f"Chunk {chunk.chunk_id!r} has n_tokens={chunk.n_tokens} > 500. " + _CLAUDE_MD_HARD_GATE` (the D-10 assertion message embeds the verbatim CLAUDE.md text).
    6. **Step D defense-in-depth (RESEARCH §3 + Pitfall 1):** Call `self._check_reranker_token_overflow(top_m_chunks)`. The helper is a no-op in stub mode (when `self._bundle.reranker.name != "bge-reranker-base"`). In real mode it lazily accesses `self._bundle.reranker._model.tokenizer` and for each chunk computes `len(tokenizer.encode(chunk.text, truncation=False))`; if any count > 500, emits `chunk_reranker_token_overflow` warning structlog with `chunk_id`, `embedder_n_tokens=chunk.n_tokens`, `reranker_n_tokens=count`, `note="reranker tokenizer disagrees with embedder; investigate canary"`. Guard the `_model.tokenizer` access with `getattr(...) is None: return` so a future reranker adapter that doesn't expose `_model` doesn't crash the search.
    7. **Step E:** `rerank_t0 = time.perf_counter(); reranked = self._bundle.reranker.rerank(truncated_query, [chunk.text for chunk in top_m_chunks]); rerank_ms = ...` (D-09 raw chunk.text verbatim; no prefix).
    8. **Step F:** Build `results: list[RetrievedChunk] = []`. For each `RerankedDoc` in `reranked[:k]`: `idx = int(reranked_doc.doc_id); chunk = top_m_chunks[idx]; results.append(RetrievedChunk(chunk_id=chunk.chunk_id, text=chunk.text, score=reranked_doc.score, ticker=chunk.ticker, fiscal_year=chunk.fiscal_year, item_code=chunk.item_code, char_span_in_section=chunk.char_span_in_section))`. Exactly the seven D-03 fields.
    9. **Step G:** `total_ms = (time.perf_counter() - t0) * 1000`. Emit `retriever_search_completed` INFO structlog with the twelve fields verbatim per D-12: `query_tokens=q_tokens, query_truncated=q_tokens > QUERY_TOKEN_HARD_CAP, bm25_candidates=len(bm25_results), dense_candidates=len(dense_results), rrf_unique=len(fused), rerank_input=len(top_m_chunks), results_returned=len(results), bm25_ms=round(bm25_ms, 2), dense_ms=round(dense_ms, 2), fuse_ms=round(fuse_ms, 2), rerank_ms=round(rerank_ms, 2), total_ms=round(total_ms, 2)`. Return `results`.

    **Helper `_truncate_query(self, query: str) -> tuple[str, int]`** (D-11 + Open Question 1 resolution — use embedder-side tokenizer):
    - If `self._bundle.embedder.name == "stub-embedder"`: `tokens = query.lower().split(); n = len(tokens); truncated = " ".join(tokens[:QUERY_TOKEN_HARD_CAP]) if n > QUERY_TOKEN_HARD_CAP else query; return (truncated, n)`. The approximation is within 1-2x of BERT WordPiece — fine for the defensive cap.
    - Else (real-mode bge-small): `tokenizer = self._bundle.embedder._model.tokenizer` (verified attribute on SentenceTransformer). Encode → count → if > 64, decode `tokenizer.decode(tokens[:QUERY_TOKEN_HARD_CAP], skip_special_tokens=True)` to get the truncated text; return `(truncated, original_count)`. Guard with `getattr(...) is None: fall back to whitespace approximation`.

    **Helper `_load_chunk_map(self) -> dict[str, Chunk]`** (CD-01 + Pitfall 7):
    - Copy the `_read_all_chunks` pattern from `packages/docintel-index/src/docintel_index/build.py` lines 91-117 — `sorted(Path(self._cfg.data_dir, "corpus/chunks").rglob("*.jsonl"))` traversal, `text.splitlines()` then `Chunk.model_validate_json(line)` per non-empty line, append to `chunk_map: dict[str, Chunk]` keyed by `chunk.chunk_id`.
    - Read `Path(self._cfg.index_dir, "MANIFEST.json")`; if file exists, parse JSON, read `chunk_count` field, and if `chunk_count != len(chunk_map)` raise `ValueError` with the verbatim message from RESEARCH §6 ("Phase 3 corpus has drifted vs Phase 4 indices. Run `uv run docintel-index build` ... `uv run docintel-ingest all` ..."). If MANIFEST does not exist (e.g. tests with `tmp_path`), skip the check.
    - Emit `retriever_chunk_map_loaded` INFO structlog with `n=len(chunk_map), corpus_root=str(chunks_root)`.
    - Return `chunk_map`.

    **Helper `_check_reranker_token_overflow(self, chunks: list[Chunk]) -> None`** (RESEARCH §3 defense-in-depth):
    - Guard 1: `if self._bundle.reranker.name != "bge-reranker-base": return`.
    - Guard 2: lazily access `self._bundle.reranker._model` then `.tokenizer`; if either is None, return (future-proofs against reranker adapter shape changes).
    - For each `chunk` in `chunks`: `n_rerank = len(tokenizer.encode(chunk.text, truncation=False)); if n_rerank > 500: log.warning("chunk_reranker_token_overflow", chunk_id=chunk.chunk_id, embedder_n_tokens=chunk.n_tokens, reranker_n_tokens=n_rerank, note="reranker tokenizer disagrees with embedder; investigate canary")`.

    Do NOT add `@retry` decorators anywhere in this file. Do NOT add SDK imports (`bm25s`, `qdrant_client`, `sentence_transformers`). Do NOT add new env-var reads — `Settings.data_dir` and `Settings.index_dir` are pre-existing.

    **File 2 — `packages/docintel-retrieve/src/docintel_retrieve/__init__.py`** (modify):

    Append `from docintel_retrieve.retriever import Retriever` (after the null_adapters and fuse imports). Extend `__all__` to `["NullBM25Store", "NullReranker", "RRF_K", "RetrievedChunk", "Retriever", "_rrf_fuse"]` (alphabetical).

    **File 3 — `tests/test_retriever_search.py`** (modify):

    Delete the five `@pytest.mark.xfail(strict=True, ...)` decorators added in Plan 05-01. Implement the five test function bodies per the `<behavior>` block above. Use `structlog.testing.capture_logs()` as the canonical pattern for structlog assertions (the analog gap from PATTERNS.md note line 1099). Pattern:
    ```python
    from structlog.testing import capture_logs

    def test_telemetry_fields() -> None:
        r = make_retriever(Settings(llm_provider="stub"))
        with capture_logs() as cap:
            r.search("test query", k=5)
        events = [rec for rec in cap if rec.get("event") == "retriever_search_completed"]
        assert len(events) == 1
        ev = events[0]
        for field in ("query_tokens", "query_truncated", "bm25_candidates", "dense_candidates",
                      "rrf_unique", "rerank_input", "results_returned", "bm25_ms", "dense_ms",
                      "fuse_ms", "rerank_ms", "total_ms"):
            assert field in ev, f"D-12: missing telemetry field {field!r}"
    ```

    For `test_assertion_quotes_claude_md`: construct `make_retriever(Settings(llm_provider="stub"))`, then mutate one chunk in `r._chunk_map` to have `n_tokens=501` (via `r._chunk_map[some_id].__dict__["n_tokens"] = 501` — Chunk is not frozen). Pick a chunk_id that will reliably be returned by both BM25 and dense for some test query (use a high-frequency chunk like the first AAPL chunk). Search with a query that pulls that chunk into top-M; assert the AssertionError raised contains both required substrings.

    For `test_zero_candidates` (concrete induction — no executor discovery work):
    ```python
    def test_zero_candidates(tmp_path: Path) -> None:
        """RET-02 + CD-07 — zero-fusion path returns [] with structlog warning."""
        from docintel_core.adapters import make_adapters
        from docintel_core.adapters.types import IndexStoreBundle
        from docintel_core.adapters.real.numpy_dense import NumpyDenseStore
        from docintel_core.config import Settings
        from docintel_retrieve.null_adapters import NullBM25Store
        from docintel_retrieve.retriever import Retriever
        from structlog.testing import capture_logs

        # tmp_path-rooted Settings: corpus is empty, data/indices/MANIFEST.json is absent.
        # _load_chunk_map skips the cardinality check when MANIFEST.json is missing
        # (RESEARCH §6 + the helper's optional-manifest branch), so chunk_map = {}.
        cfg = Settings(llm_provider="stub", data_dir=tmp_path, index_dir=tmp_path / "data" / "indices")
        bundle = make_adapters(cfg)
        # NullBM25Store.query returns [] for any input (Plan 05-04 contract).
        # NumpyDenseStore.query on an empty index returns [] (Phase 4 contract).
        stores = IndexStoreBundle(bm25=NullBM25Store(), dense=NumpyDenseStore(cfg))
        # Construct directly — NOT via make_retriever — so we control both stores explicitly.
        r = Retriever(bundle=bundle, stores=stores, cfg=cfg)

        with capture_logs() as cap:
            result = r.search("any query", k=5)

        # CD-07 — empty list returned, zero-candidate warning emitted.
        assert result == [], f"CD-07 expected empty list, got {result\!r}"
        zero_events = [rec for rec in cap if rec.get("event") == "retriever_search_zero_candidates"]
        assert len(zero_events) == 1, f"CD-07 expected exactly one retriever_search_zero_candidates event, got {len(zero_events)}"
    ```

    Why this induction is reliable:
    - `NullBM25Store.query` returns `[]` unconditionally (Plan 05-04 D-08 contract — the test_null_bm25_empty test enforces this).
    - `NumpyDenseStore.query` against an empty index returns `[]` (Phase 4 contract — verified via tests/test_index_stores.py analogs).
    - `_rrf_fuse([], [])` returns `[]` (Plan 05-03 RRF helper — the test_rrf_fuse contract handles empty-input).
    - `Retriever._load_chunk_map` skips the MANIFEST cardinality check when `data/indices/MANIFEST.json` does NOT exist (per the helper's optional-manifest branch — RESEARCH §6 + the action block's `_load_chunk_map` spec above). Empty `tmp_path` corpus → chunk_map is `{}` → cardinality check is skipped → no ValueError.
    - Both warm-up calls in `Retriever.__init__` are wrapped in `try/except Exception` so empty-store warm-up does NOT prevent construction.
    - The CD-07 zero-candidate guard in `Retriever.search` Step C runs `if not fused: log.warning("retriever_search_zero_candidates", ...); return []`. This is the load-bearing path the test asserts.
  </action>
  <verify>
    <automated>uv run pytest tests/test_retriever_search.py -ra -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file `packages/docintel-retrieve/src/docintel_retrieve/retriever.py` exists; declares `class Retriever:` exactly once.
    - Source assertion: four module constants present with `Final[int]` typing — `TOP_N_PER_RETRIEVER = 100`, `TOP_M_RERANK = 20`, `TOP_K_FINAL = 5`, `QUERY_TOKEN_HARD_CAP = 64` (verified by `grep -cE '^(TOP_N_PER_RETRIEVER|TOP_M_RERANK|TOP_K_FINAL|QUERY_TOKEN_HARD_CAP)\s*:\s*Final\[int\]' packages/docintel-retrieve/src/docintel_retrieve/retriever.py` returns 4).
    - Source assertion: `_CLAUDE_MD_HARD_GATE` constant contains all three required substrings (verified by `grep -c "BGE 512-token truncation FIRST" packages/docintel-retrieve/src/docintel_retrieve/retriever.py` returns ≥1; same for "before suspecting hybrid retrieval, RRF, or chunk size" and "the canary exists specifically to catch it").
    - Source assertion: file has zero tenacity imports and zero `@retry` decorators (verified by `grep -cE '(^from tenacity|@retry)' packages/docintel-retrieve/src/docintel_retrieve/retriever.py` returns 0).
    - Source assertion: file has zero new SDK imports (verified by `grep -cE '^(from|import)\s+(bm25s|qdrant_client|sentence_transformers|torch)' packages/docintel-retrieve/src/docintel_retrieve/retriever.py` returns 0).
    - Source assertion: file has the D-09 raw-text rerank call pattern (verified by `grep -F '[chunk.text for chunk in top_m_chunks]' packages/docintel-retrieve/src/docintel_retrieve/retriever.py` returns ≥1 match).
    - Source assertion: Retriever.search emits the twelve D-12 telemetry fields (verified by `grep -F 'retriever_search_completed' packages/docintel-retrieve/src/docintel_retrieve/retriever.py` returns ≥1 AND all twelve field names appear as bareword key=value patterns).
    - Behavior: `uv run python -c "from docintel_retrieve import Retriever, RetrievedChunk; print(Retriever)"` succeeds.
    - Test command: `uv run pytest tests/test_retriever_search.py -ra -q` reports 5 passed, 0 xfailed, 0 failed.
    - CLI: `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — all three exit 0 (CD-04 confirmed — no new tenacity-required SDK call sites).
    - CLI: `uv run mypy --strict packages/docintel-retrieve/src/docintel_retrieve/retriever.py` exits 0.
  </acceptance_criteria>
  <done>Retriever class lives in retriever.py with all D-05..D-12 + CD-01/CD-04/CD-07 + RESEARCH §3 defenses; module re-exports updated; test file flips from 5 xfailed to 5 passed; mypy strict + grep gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add make_retriever(cfg) factory to docintel_core.adapters.factory and flip make_retriever xfails</name>
  <files>packages/docintel-core/src/docintel_core/adapters/factory.py, packages/docintel-core/src/docintel_core/adapters/__init__.py, tests/test_make_retriever.py</files>
  <read_first>
    - .planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md (section "### `packages/docintel-core/src/docintel_core/adapters/factory.py` (MODIFY — add `make_retriever(cfg)`)" lines 535-647; Pattern S5 lazy-import discipline lines 1249-1267)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-04 factory placement; CD-01 eager-load; CD-08 no factory cache)
    - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md (§Architecture Patterns System Diagram lines 174-264; §Pattern 2 lines 394-410 — Retriever __init__ signature)
    - packages/docintel-core/src/docintel_core/adapters/factory.py (current state — read the full file; the existing `make_adapters` and `make_index_stores` are the structural analogs at lines 37-129)
    - packages/docintel-core/src/docintel_core/adapters/__init__.py (current `__all__` and re-export shape)
    - packages/docintel-retrieve/src/docintel_retrieve/retriever.py (from Task 1 — verify the Retriever signature is `__init__(self, bundle: AdapterBundle, stores: IndexStoreBundle, cfg: Settings)`)
    - tests/test_make_retriever.py (xfail scaffold from Plan 05-01)
  </read_first>
  <behavior>
    - Test 1 (`test_make_retriever_stub`): `make_retriever(Settings(llm_provider="stub"))` returns a `Retriever` instance (verified by `isinstance(result, Retriever)`).
    - Test 2 (`test_chunk_map_eager_load`): The constructed Retriever's `_chunk_map` (private attribute read with `# noqa: SLF001`) has length equal to the count of non-empty lines across `data/corpus/chunks/**/*.jsonl` (currently 6053 on this corpus per Phase 3 SUMMARY).
    - Side effect: Importing `docintel_core.adapters.factory` (the module load) does NOT trigger an import of `docintel_retrieve` — verified by checking that `docintel_retrieve` is not in `sys.modules` immediately after `from docintel_core.adapters import factory`. The Retriever import happens only when `make_retriever(...)` is called.
  </behavior>
  <action>
    Add a new function `make_retriever(cfg)` to `packages/docintel-core/src/docintel_core/adapters/factory.py` as the third sibling factory alongside `make_adapters(cfg)` and `make_index_stores(cfg)`. Re-export it from `packages/docintel-core/src/docintel_core/adapters/__init__.py`. Remove the xfail markers from `tests/test_make_retriever.py`.

    **File 1 — `packages/docintel-core/src/docintel_core/adapters/factory.py`** (modify):

    Extend the existing factory module. The current file ends at line 129 with `make_index_stores`. Append a new function `def make_retriever(cfg: Settings) -> "Retriever":` (string forward reference — CRITICAL for Pattern S5 lazy-import discipline; do NOT import `Retriever` at module top).

    Multi-line docstring explaining:
    - D-04: third sibling factory; lives in `docintel_core.adapters.factory` (NOT `docintel-retrieve`) so the import direction matches every prior phase (`docintel-retrieve` imports from `docintel-core`, never the reverse).
    - Lazy-import discipline (D-12): `from docintel_retrieve.retriever import Retriever` lives INSIDE the function body so `import docintel_core.adapters.factory` stays cheap.
    - CD-01: eager-load — constructing the Retriever loads the chunk_id → Chunk map (~600 ms one-time) AND warms the index-store lazy-load paths (~100 ms). The caller pays this cost once per process; Phase 13's FastAPI lru-caches the result so subsequent requests reuse the warm instance.
    - CD-08: no factory-level cache — Phase 13 lru-caches; Phase 10 constructs once per run.

    Function body:
    ```
    # Lazy import — keeps `import docintel_core.adapters.factory` cheap (D-12 + Pattern S5).
    from docintel_retrieve.retriever import Retriever
    
    bundle = make_adapters(cfg)
    stores = make_index_stores(cfg)
    return Retriever(bundle=bundle, stores=stores, cfg=cfg)
    ```

    (The exact return statement uses keyword arguments to match the Retriever signature defined in Task 1.)

    Do NOT add `Retriever` to the `TYPE_CHECKING` block at the top of factory.py — the return-type annotation is a string forward reference so no top-level import (even type-checking-only) is needed. mypy will resolve `"Retriever"` at type-check time by reading the function body's import.

    **File 2 — `packages/docintel-core/src/docintel_core/adapters/__init__.py`** (modify):

    Extend the existing `from docintel_core.adapters.factory import make_adapters` line to `from docintel_core.adapters.factory import make_adapters, make_retriever`. Extend `__all__` to include `"make_retriever"` (alphabetical — between `make_adapters` and end of list, or right after `make_adapters`). Final `__all__` includes `["AdapterBundle", "BM25Store", "CompletionResponse", "DenseStore", "Embedder", "IndexStoreBundle", "JudgeVerdict", "LLMClient", "LLMJudge", "RerankedDoc", "Reranker", "TokenUsage", "make_adapters", "make_retriever"]`.

    **File 3 — `tests/test_make_retriever.py`** (modify):

    Delete the two `@pytest.mark.xfail(strict=True, ...)` decorators added in Plan 05-01. Verify the test bodies match the `<behavior>` block. The `test_chunk_map_eager_load` test counts non-empty lines across `data/corpus/chunks/**/*.jsonl` (use the verified pattern from PATTERNS.md lines 1002-1014). Add a third test `test_factory_lazy_imports_retriever_module` that:
    ```python
    def test_factory_lazy_imports_retriever_module() -> None:
        """D-12 + Pattern S5: importing factory does NOT eagerly load docintel_retrieve."""
        import sys
        # Drop any cached modules so the test is hermetic.
        for mod in list(sys.modules):
            if mod.startswith("docintel_retrieve"):
                del sys.modules[mod]
        # Importing the factory must NOT pull in docintel_retrieve.
        from docintel_core.adapters import factory  # noqa: F401
        assert "docintel_retrieve" not in sys.modules, (
            "D-12: factory module top-level pulled in docintel_retrieve eagerly; "
            "the import must live inside make_retriever()."
        )
    ```
    Note: this test is brittle if pytest collection has already imported docintel_retrieve elsewhere — guard with the `del sys.modules` cleanup above; this hermetic-reset pattern works under pytest because module imports are cached at the interpreter level.
  </action>
  <verify>
    <automated>uv run pytest tests/test_make_retriever.py -ra -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/docintel-core/src/docintel_core/adapters/factory.py` declares `def make_retriever(cfg: Settings) -> "Retriever":` (string forward reference; verified by `grep -cE 'def make_retriever\(cfg:\s*Settings\)\s*->\s*"Retriever":' packages/docintel-core/src/docintel_core/adapters/factory.py` returns 1).
    - Source assertion: `make_retriever` body has the lazy import `from docintel_retrieve.retriever import Retriever` INSIDE the function (verified by `grep -B5 'from docintel_retrieve.retriever import Retriever' packages/docintel-core/src/docintel_core/adapters/factory.py | grep -c 'def make_retriever'` returns 1).
    - Source assertion: NO top-level `from docintel_retrieve` import in factory.py (verified by `grep -E '^(from|import)\s+docintel_retrieve' packages/docintel-core/src/docintel_core/adapters/factory.py` returns no matches).
    - Source assertion: `__all__` in `packages/docintel-core/src/docintel_core/adapters/__init__.py` contains `"make_retriever"` (verified by `grep -F '"make_retriever"' packages/docintel-core/src/docintel_core/adapters/__init__.py` succeeds).
    - Source assertion: NO `@pytest.mark.xfail` remains in `tests/test_make_retriever.py` (verified by `grep -c 'pytest.mark.xfail' tests/test_make_retriever.py` returns 0).
    - Behavior: `uv run python -c "from docintel_core.adapters import make_retriever; from docintel_core.config import Settings; from docintel_retrieve import Retriever; r = make_retriever(Settings(llm_provider='stub')); assert isinstance(r, Retriever); print('OK')"` exits 0.
    - Behavior: `uv run python -c "import sys; from docintel_core.adapters import factory; assert 'docintel_retrieve' not in sys.modules, 'D-12 lazy import broken'; print('OK')"` exits 0 — this is the Pattern S5 cheap-import gate.
    - Test command: `uv run pytest tests/test_make_retriever.py -ra -q` reports 2 or 3 passed, 0 xfailed, 0 failed.
    - CLI: `uv run mypy --strict packages/docintel-core/src/docintel_core/adapters/factory.py` exits 0.
    - CLI: `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — all three exit 0.
  </acceptance_criteria>
  <done>make_retriever(cfg) factory landed in docintel_core.adapters.factory with string forward reference + lazy body import; re-exported from adapters/__init__; test file flips from 2 xfailed to 2 (or 3) passed; mypy strict + three CI grep gates remain green; D-04 + CD-01 + Pattern S5 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → Retriever.search | Untrusted query string crosses into the retrieval library. D-11 caps the query at 64 BGE tokens BEFORE embedder.embed runs — bounds embedding cost and prevents reranker-side token-budget exhaustion. |
| chunks JSONL → Retriever._chunk_map | Phase 3 corpus artifact is committed and read-only at retrieval time. Pitfall 7 mitigation: cardinality check against MANIFEST.chunk_count at __init__. |
| Phase 4 indices → IndexStoreBundle.query results | Index artifacts (gitignored, rebuilt by CI) flow chunk_ids back into Retriever.search; the chunk_id → Chunk lookup is a `dict[chunk_id]` that raises KeyError on drift, which is intentional (cardinality check at __init__ should have caught the drift first). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-V5-01 | Tampering / DoS | Malicious oversized query | mitigate | D-11 hard cap at 64 BGE tokens BEFORE embedder.embed; `tests/test_retriever_search.py::test_query_truncation_logs` asserts the warning fires; truncation is loud (structlog) not silent. |
| T-5-V5-02 | Tampering | Index drift between Phase 4 build and Phase 5 query | mitigate | Retriever.__init__ raises ValueError with explicit fix command if `len(self._chunk_map) != MANIFEST.chunk_count` (Pitfall 7); test `test_chunk_map_eager_load` asserts cardinality match in stub mode. |
| T-5-V5-03 | Tampering / undetected regression | Canary regression masking BGE tokenizer drift | mitigate | D-10 assertion with verbatim CLAUDE.md quote AND RESEARCH §3 soft warning `chunk_reranker_token_overflow` (real-mode only) — when bge-reranker-base XLM-R tokenizes a chunk to > 500 tokens, structlog emits the warning; D-16 debug order points the debugger here first. |
| T-5-V5-05 | Information disclosure | Sensitive chunk text in logs | mitigate | D-12 telemetry log line emits counts + timings only, no chunk text; query string is logged in the truncation warning as `query[:100]` slice only. Acceptable for v1 (corpus is public). |
| T-5-V5-06 | DoS | Zero-candidate query produces unexpected exception | mitigate | CD-07 path returns `[]` with `retriever_search_zero_candidates` structlog warning; `tests/test_retriever_search.py::test_zero_candidates` asserts. Phase 6 GEN-04 refusal path handles the empty list. |
</threat_model>

<verification>
1. `uv run pytest tests/test_retriever_search.py tests/test_make_retriever.py -ra -q` — 7 or 8 passed (5 from retriever_search + 2 or 3 from make_retriever), 0 xfailed, 0 failed.
2. `uv run pytest -ra -q` — full suite green; xfail count drops by 7 from Plan 05-04 baseline.
3. `uv run mypy --strict` — clean across all source files.
4. `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — all three exit 0.
5. `uv run python -c "from docintel_core.adapters import make_retriever; from docintel_core.config import Settings; r = make_retriever(Settings(llm_provider='stub')); results = r.search('apple revenue', k=5); assert len(results) == 5; print('OK')"` exits 0 — end-to-end stub-mode smoke test.
6. `uv run docintel-index verify` exits 0 (Phase 4 invariant; this plan does not modify Phase 4 artifacts).
</verification>

<success_criteria>
- Retriever class with the .search seam ships per D-02.
- All four pipeline parameters pinned as module constants (D-05/06/11).
- D-10 assertion fires with verbatim CLAUDE.md quote text in failure message (Pitfall 6).
- D-11 query truncation at 64 BGE tokens emits structlog warning.
- D-12 retriever_search_completed line has all twelve required fields.
- CD-07 zero-candidate path returns [] with structlog warning.
- CD-01 eager-load: chunk_map + warm-up calls fire at __init__.
- Pitfall 7 cardinality check raises ValueError on drift.
- RESEARCH §3 soft warning `chunk_reranker_token_overflow` fires in real mode (stub mode skips).
- CD-04 verified: no new tenacity wraps; three CI grep gates remain green.
- D-04 verified: `make_retriever` lives in docintel_core.adapters.factory with string forward reference + lazy body import.
- Pattern S5 verified: importing factory module does not eagerly load docintel_retrieve.
- 7 xfails removed (5 in retriever_search + 2 in make_retriever); tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-hybrid-rerank/05-05-SUMMARY.md` documenting: the verbatim Retriever.__init__ signature; the four module constants and their D-* citations; the verbatim _CLAUDE_MD_HARD_GATE string; the twelve D-12 telemetry field names; the verbatim make_retriever signature (`-> "Retriever":` string forward reference); confirmation that the three CI grep gates remain green; confirmation that `docintel_retrieve` is NOT loaded by importing docintel_core.adapters.factory (Pattern S5 gate); benchmark numbers if measured (chunk_map load time, search latency for one stub-mode query).
</output>
