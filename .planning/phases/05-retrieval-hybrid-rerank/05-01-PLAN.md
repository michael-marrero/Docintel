---
phase: 05-retrieval-hybrid-rerank
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_rrf_fuse.py
  - tests/test_null_adapters.py
  - tests/test_retrieved_chunk_schema.py
  - tests/test_make_retriever.py
  - tests/test_retriever_search.py
  - tests/test_reranker_canary.py
  - data/eval/canary/cases.jsonl
autonomous: true
requirements: [RET-01, RET-02, RET-03, RET-04]
must_haves:
  truths:
    - "Six new test files exist under tests/ with xfail markers covering every RET-01..04 behavior listed in 05-VALIDATION.md Per-Task Verification Map"
    - "data/eval/canary/cases.jsonl exists and contains at least 1 placeholder case so the canary loader test has a target in Wave 0"
    - "uv run pytest -ra -q exits 0 with the six new xfail-marked tests reported as xfailed (not failed) — the failing slot is reserved for Wave 1+ implementation"
    - "Every test function named in 05-VALIDATION.md Per-Task Verification Map column 'Automated Command' is collectable by pytest (i.e. discovered, not skipped or errored)"
  artifacts:
    - path: "tests/test_rrf_fuse.py"
      provides: "RET-01 xfail scaffolds — test_rrf_fuse_known_score, test_rrf_skip_missing, test_rrf_one_based_ranks, test_rrf_k_constant"
    - path: "tests/test_null_adapters.py"
      provides: "D-08 xfail scaffolds — test_null_reranker_preserves_order, test_null_reranker_satisfies_protocol, test_null_bm25_empty, test_null_bm25_satisfies_protocol"
    - path: "tests/test_retrieved_chunk_schema.py"
      provides: "RET-04 xfail scaffolds — test_retrieved_chunk_required_fields, test_char_span_tuple, test_retrieved_chunk_forbids_extra, test_retrieved_chunk_is_frozen"
    - path: "tests/test_make_retriever.py"
      provides: "D-04 + CD-01 xfail scaffolds — test_make_retriever_stub, test_chunk_map_eager_load"
    - path: "tests/test_retriever_search.py"
      provides: "RET-02 xfail scaffolds — test_retriever_returns_top_k, test_assertion_quotes_claude_md, test_query_truncation_logs, test_telemetry_fields, test_zero_candidates"
    - path: "tests/test_reranker_canary.py"
      provides: "RET-03 xfail scaffolds — test_cases_loaded, test_reranker_canary_stub_mode, test_reranker_canary_real_mode (@pytest.mark.real), test_failure_message_quotes_claude_md"
    - path: "data/eval/canary/cases.jsonl"
      provides: "Wave 0 placeholder case so loader test has a target (≥1 record); real cases curated in Plan 05-06"
  key_links:
    - from: "tests/test_reranker_canary.py"
      to: "data/eval/canary/cases.jsonl"
      via: "_REPO_ROOT = Path(__file__).resolve().parent.parent / 'data/eval/canary/cases.jsonl'"
      pattern: "_REPO_ROOT.*data.*eval.*canary.*cases\\.jsonl"
---

<objective>
Land all six test-file scaffolds + the canary `cases.jsonl` placeholder so every RET-01..04 behavior named in `05-VALIDATION.md`'s Per-Task Verification Map has a collectable pytest target. Every new test function is xfail-marked (`@pytest.mark.xfail(strict=True, reason="...")`) so Wave 0 ships green, and Wave 1+ plans flip the xfails to xpass as implementations land. Per CLAUDE.md operating rule "Tests before code where possible, especially for the eval harness" — this plan is the literal tests-first scaffold for the Phase 5 acceptance gate.

Purpose: Tests-first discipline (Phase 5 RESEARCH §"Project Constraints from CLAUDE.md" row 2) gives Wave 1+ implementers a target to flip from xfail to passing. Without this scaffold, the canary test driver and the RRF/null-adapter/schema tests would be invented during Wave 1 alongside the code — interleaved, harder to review, and harder to verify the test asserts the contract rather than the implementation.

Output: 6 test files under `tests/` (every test function name listed verbatim in 05-VALIDATION.md Per-Task Verification Map) + the `data/eval/canary/` directory + `cases.jsonl` with 1 placeholder case. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md
@.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md
@.planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md
@.planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md

<interfaces>
<!-- Existing pytest configuration (verified in repo) — every test file authored here MUST be collectable by the existing pytest config without modification. -->

From `/Users/michaelmarrero/GitHub/Docintel/pyproject.toml` (workspace root, `[tool.pytest.ini_options]`):
```toml
asyncio_mode = "auto"
testpaths = ["tests", "packages/*/tests"]
addopts = "-ra --strict-markers"
markers = ["real: tests that require real (non-stub) backends — gated behind workflow_dispatch"]
```

From `tests/test_index_build_real.py` (Phase 4 analog for `@pytest.mark.real`):
```python
pytestmark = pytest.mark.real  # module-level; every test in file marked
```

From `tests/test_chunk_idempotency.py` (Phase 3 analog for `_REPO_ROOT` + JSONL-line reading):
```python
_REPO_ROOT = Path(__file__).resolve().parent.parent
```

From `data/corpus/chunks/AAPL/FY2024.jsonl` (JSONL-per-record committed-data analog — one JSON object per line, no comments):
```
{"chunk_id":"AAPL-FY2024-Item-1-000","ticker":"AAPL","fiscal_year":2024,...}
```

From `pytest.mark.xfail` standard pattern (used across this codebase in every Phase 3/4 Wave 0 scaffold):
```python
@pytest.mark.xfail(strict=True, reason="Wave 1 — implementation lands in Plan 05-XX")
def test_some_behavior() -> None: ...
```

`strict=True` means an xpass (test starts passing without xfail removal) is treated as a FAIL — this forces Wave 1+ plans to explicitly remove the marker when they flip the behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four pure-unit test scaffolds (RRF + null adapters + RetrievedChunk schema + factory)</name>
  <files>tests/test_rrf_fuse.py, tests/test_null_adapters.py, tests/test_retrieved_chunk_schema.py, tests/test_make_retriever.py</files>
  <read_first>
    - .planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md (Per-Task Verification Map — every test function name verbatim)
    - .planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md (Pattern Map — analog code snippets for tests/test_rrf_fuse.py at section "### `tests/test_rrf_fuse.py`"; tests/test_null_adapters.py at "### `tests/test_null_adapters.py`"; tests/test_retrieved_chunk_schema.py at "### `tests/test_retrieved_chunk_schema.py`"; tests/test_make_retriever.py at "### `tests/test_make_retriever.py`")
    - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md (§1 RRF formula table for hand-computed assertions; §Validation Architecture lines 1496-1517 for the canonical test function names)
    - tests/test_index_stores.py (analog for `_make_chunk` Pydantic-construction helper at lines 38-53, and end-to-end test shape lines 71-122)
    - tests/test_adapters.py (analog for protocol-satisfaction tests via `isinstance(...)` at line 152-187)
    - tests/test_chunk_idempotency.py (analog for `_REPO_ROOT = Path(__file__).resolve().parent.parent` pattern at line 27)
    - pyproject.toml (verify `[tool.pytest.ini_options]` config — testpaths, --strict-markers — already configured; do NOT modify)
  </read_first>
  <action>
    Create four test files under `tests/`, each containing xfail-marked stub functions whose names are exact matches for the entries in 05-VALIDATION.md Per-Task Verification Map. Every function MUST be decorated with `@pytest.mark.xfail(strict=True, reason="Wave N — implementation lands in Plan 05-XX")` referencing the plan that will flip it.

    **`tests/test_rrf_fuse.py`** — four functions (RET-01 + D-07): `test_rrf_fuse_known_score`, `test_rrf_skip_missing`, `test_rrf_one_based_ranks`, `test_rrf_k_constant`. Body of each is `pytest.skip(...)` is FORBIDDEN — use a real assertion that will pass when `docintel_retrieve.fuse._rrf_fuse` exists and behaves per RESEARCH §1. For the scaffold, every test body should attempt the import `from docintel_retrieve.fuse import RRF_K, _rrf_fuse` — this will fail at import time in Wave 0 (the package does not exist yet), which is exactly what xfail wants. xfail reason references Plan 05-03.

    **`tests/test_null_adapters.py`** — four functions (D-08): `test_null_reranker_preserves_order`, `test_null_reranker_satisfies_protocol`, `test_null_bm25_empty`, `test_null_bm25_satisfies_protocol`. Each body imports `from docintel_retrieve.null_adapters import NullReranker, NullBM25Store` and asserts the contract from 05-PATTERNS.md (NullReranker score = `-float(rank)`; NullBM25Store.query returns `[]`; both `isinstance(...)` against the Protocol). xfail reason references Plan 05-04.

    **`tests/test_retrieved_chunk_schema.py`** — four functions (RET-04 + D-03 + CD-02): `test_retrieved_chunk_required_fields`, `test_char_span_tuple`, `test_retrieved_chunk_forbids_extra`, `test_retrieved_chunk_is_frozen`. Each body imports `from docintel_core.types import RetrievedChunk` and exercises the seven fields listed in CONTEXT.md D-03 (`chunk_id`, `text`, `score`, `ticker`, `fiscal_year`, `item_code`, `char_span_in_section`). Use a `_ok_payload()` helper that returns a dict with all seven keys — chunk_id = "AAPL-FY2024-Item-1A-007", ticker = "AAPL", fiscal_year = 2024, item_code = "Item 1A", char_span_in_section = (100, 250), score = 0.812, text = "Some chunk text.". `test_retrieved_chunk_forbids_extra` adds an unknown `bm25_rank: 3` field and expects `pydantic.ValidationError`. `test_retrieved_chunk_is_frozen` constructs the model and attempts `rc.score = 0.99` expecting `pydantic.ValidationError`. xfail reason references Plan 05-02.

    **`tests/test_make_retriever.py`** — two functions (D-04 + CD-01): `test_make_retriever_stub`, `test_chunk_map_eager_load`. Each imports `from docintel_core.adapters.factory import make_retriever` and `from docintel_core.config import Settings`. The first asserts `isinstance(make_retriever(Settings(llm_provider="stub")), Retriever)` via a deferred `from docintel_retrieve.retriever import Retriever` import. The second uses `_REPO_ROOT = Path(__file__).resolve().parent.parent`, counts non-empty lines across `_REPO_ROOT / "data/corpus/chunks/**/*.jsonl"`, constructs `make_retriever(Settings(llm_provider="stub"))`, and asserts `len(r._chunk_map) == expected_count` (private-attribute read — annotate `# noqa: SLF001`). xfail reason references Plan 05-05.

    NONE of these test files may import `docintel_retrieve.*` at module top — every import lives inside the function body or inside an xfail-marked deferred import. Reason: at Wave 0 the package does not exist; module-top import would make pytest collection itself fail rather than xfail the individual tests. (For pytest, an ImportError inside a function body decorated with xfail is treated as an expected failure; an ImportError at module top is a collection error that breaks the entire file.)

    Every test must include in its docstring the requirement-id it covers (e.g. `"""RET-01 — RRF skip-missing behavior (Plan 05-03)."""`) so the validation map traceability is preserved at code-read time.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py --collect-only -q 2>&1 | grep -E "test_(rrf_fuse_known_score|rrf_skip_missing|rrf_one_based_ranks|rrf_k_constant|null_reranker_preserves_order|null_reranker_satisfies_protocol|null_bm25_empty|null_bm25_satisfies_protocol|retrieved_chunk_required_fields|char_span_tuple|retrieved_chunk_forbids_extra|retrieved_chunk_is_frozen|make_retriever_stub|chunk_map_eager_load)" | wc -l | grep -q "^14$" && uv run pytest tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py -ra -q 2>&1 | grep -qE "14 xfailed"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: each of the four files contains the exact test function names listed above (verified by `grep -c "^def test_" tests/test_rrf_fuse.py` returns 4, `tests/test_null_adapters.py` returns 4, `tests/test_retrieved_chunk_schema.py` returns 4, `tests/test_make_retriever.py` returns 2 — total 14).
    - Source assertion: every test function in these four files is decorated with `@pytest.mark.xfail(strict=True, ...)` (verified by `grep -B1 "^def test_" tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py | grep -c "xfail(strict=True"` returns 14).
    - Source assertion: no `docintel_retrieve` import lives at module top in any of the four files (verified by `grep -E "^(from|import) docintel_retrieve" tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py | wc -l` returns 0).
    - Test command: `uv run pytest tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py -ra -q` reports exactly 14 xfailed, 0 passed, 0 failed.
    - Behavior: pytest collection of all four files emits zero ImportError at collection time (verified by `--collect-only -q` exit code 0 and the test name grep above returns 14).
  </acceptance_criteria>
  <done>Four test files committed; 14 test functions total, every one xfail-marked with strict=True and a Plan 05-XX reason; pytest collection succeeds with no ImportError; full file slice runs in under 5 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two integration test scaffolds (Retriever.search + canary) + the cases.jsonl placeholder</name>
  <files>tests/test_retriever_search.py, tests/test_reranker_canary.py, data/eval/canary/cases.jsonl</files>
  <read_first>
    - .planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md (Per-Task Verification Map test function names for tests/test_retriever_search.py rows 05-02-01..05-02-05 and tests/test_reranker_canary.py rows 05-03-01..05-03-04)
    - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md (§9 Pattern A — single file with two modes; §Validation Architecture lines 1503-1510 for canonical test function names; lines 862-1003 for the canary skeleton; the verbatim `_CLAUDE_MD_QUOTE` constant at lines 934-939)
    - .planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md (sections "### `tests/test_retriever_search.py`" lines 1019-1099 and "### `tests/test_reranker_canary.py`" lines 1103-1141; Pattern S4 `_REPO_ROOT` at lines 1235-1245; Pattern S7 NOT applied)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-13 cases.jsonl schema with 6 required fields per record; D-16 verbatim CLAUDE.md quote text; D-15 stub-mandatory / real-via-dispatch)
    - tests/test_index_build_real.py (analog for `@pytest.mark.real`; verify it uses function-level marker now — `pytestmark` was removed in Plan 04-07 but a module-level `pytestmark` would be wrong for Phase 5 which is dual-mode)
    - tests/test_chunk_idempotency.py (analog for `_REPO_ROOT` at line 27)
    - data/corpus/chunks/AAPL/FY2024.jsonl (JSONL-per-record analog — one JSON object per line, no comments, no trailing whitespace)
    - CLAUDE.md (the verbatim Phase 5 hard-gate paragraph — D-16 + Pitfall 6 require this exact substring in failure messages; `_CLAUDE_MD_QUOTE` constant in test_reranker_canary.py must contain the substrings "BGE 512-token truncation FIRST" AND "before suspecting hybrid retrieval, RRF, or chunk size" AND "the canary exists specifically to catch it")
  </read_first>
  <action>
    Create two test files plus one JSONL data file.

    **`tests/test_retriever_search.py`** — five xfail-marked functions (RET-02 + CD-07): `test_retriever_returns_top_k`, `test_assertion_quotes_claude_md`, `test_query_truncation_logs`, `test_telemetry_fields`, `test_zero_candidates`. Imports `make_retriever` from `docintel_core.adapters.factory` inside each function body (NOT module-top). For `test_assertion_quotes_claude_md`: the test must assert the AssertionError message contains the EXACT substring `"BGE 512-token truncation FIRST"` AND `"before suspecting hybrid retrieval, RRF, or chunk size"` — these are the Pitfall 6 grep-asserts. For `test_query_truncation_logs` + `test_telemetry_fields`: use `structlog.testing.capture_logs()` as a context manager and assert against the captured records (RESEARCH PATTERNS line 1099 — new ground; this is the canonical pattern for structlog 25.x). xfail reason references Plan 05-05.

    **`tests/test_reranker_canary.py`** — four functions (RET-03): `test_cases_loaded`, `test_reranker_canary_stub_mode`, `test_reranker_canary_real_mode`, `test_failure_message_quotes_claude_md`. CRITICAL: the marker pattern is FUNCTION-LEVEL not MODULE-LEVEL (RESEARCH §9 Pattern A, additional_planning_notes constraint 7). `test_reranker_canary_real_mode` MUST carry `@pytest.mark.real` AS WELL AS `@pytest.mark.xfail(strict=True, reason="Wave 2 — Plan 05-06 implements canary cases + driver")`. Apply markers in the order: `@pytest.mark.real` outer, `@pytest.mark.xfail` inner (so pytest collection respects the `not real` deselection at the strict-marker layer). The other three tests are xfail-only.

    Define module-level `_REPO_ROOT = Path(__file__).resolve().parent.parent` and `_CASES_PATH = _REPO_ROOT / "data" / "eval" / "canary" / "cases.jsonl"`. Define module-level constant `_CLAUDE_MD_QUOTE` containing the verbatim text from CLAUDE.md "If that gate fails, look at BGE 512-token truncation FIRST before suspecting hybrid retrieval, RRF, or chunk size. This is the most common subtle failure mode and the canary exists specifically to catch it." prefixed with `'Per CLAUDE.md: "'` and suffixed with `'"'` (matches RESEARCH lines 934-939 verbatim).

    `test_failure_message_quotes_claude_md` is a pure-string unit test (not an integration test): it asserts `"BGE 512-token truncation FIRST" in _CLAUDE_MD_QUOTE` AND `"before suspecting hybrid retrieval, RRF, or chunk size" in _CLAUDE_MD_QUOTE` AND `"the canary exists specifically to catch it" in _CLAUDE_MD_QUOTE`. Pitfall 6 defense — turns a "make error messages clearer" cleanup PR into a red CI signal. xfail because the constant doesn't exist until the file is written; the test will xpass on the very same commit that introduces the file unless the assert is wrapped in a guard. Wrap each assert in `assert _CLAUDE_MD_QUOTE.find("BGE 512-token truncation FIRST") != -1, "Pitfall 6: verbatim quote drifted"` to make the failure mode obvious.

    Note: `test_failure_message_quotes_claude_md` is the ONE test in this file that will NOT xfail in Wave 0 — once the `_CLAUDE_MD_QUOTE` constant exists at the top of the file, the test passes. To keep the Wave 0 acceptance contract uniform (all-xfail), still mark it `@pytest.mark.xfail(strict=True, reason="Wave 2 — Plan 05-06 commits the final canary driver with the verbatim quote constant")` AND have the test additionally assert something that requires the canary infrastructure (e.g., assert the constant is exported from the module's top-level — leave a hook for the Wave 2 implementation).

    xfail reason for the three non-real tests references Plan 05-06.

    **`data/eval/canary/cases.jsonl`** — Wave 0 placeholder. Single line:
    ```
    {"case_id": "C-00-placeholder", "question": "PLACEHOLDER — replaced by curated cases in Plan 05-06", "gold_chunk_ids": ["AAPL-FY2024-Item-1-000"], "ticker": "AAPL", "fiscal_year": 2024, "rationale": "Wave 0 placeholder so the JSONL loader test has a target file; real curated cases land in Plan 05-06 Wave 2 per RESEARCH §7 playbook + §8 stub-vs-real verification protocol."}
    ```
    This is a single line of JSON ending in `\n`. The line MUST be valid JSON-per-line (no trailing comma; closing `}` on the same line). One record. Plan 05-06 replaces this file with 7+ curated cases. Note: D-14 requires ≥5 cases — the loader test `test_cases_loaded` will xfail at Wave 0 because there's only 1 record; it flips to xpass at Plan 05-06 when 7+ records land.

    Create the `data/eval/canary/` directory in the same commit. The path must be EXACTLY `data/eval/canary/cases.jsonl` (D-13). `data/eval/` is a NEW directory for Phase 5 (sibling of future `data/eval/ground_truth/` Phase 8 and `data/eval/reports/` Phase 10). Per CLAUDE.md and CONTEXT.md, `data/eval/canary/cases.jsonl` IS COMMITTED (it's part of the artifact — recruiters cloning the repo see the structural acceptance criterion in plain text).
  </action>
  <verify>
    <automated>uv run pytest tests/test_retriever_search.py tests/test_reranker_canary.py --collect-only -q 2>&1 | grep -E "test_(retriever_returns_top_k|assertion_quotes_claude_md|query_truncation_logs|telemetry_fields|zero_candidates|cases_loaded|reranker_canary_stub_mode|reranker_canary_real_mode|failure_message_quotes_claude_md)" | wc -l | grep -q "^9$" && test -f data/eval/canary/cases.jsonl && python -c "import json; lines=[l for l in open('data/eval/canary/cases.jsonl') if l.strip()]; assert len(lines) >= 1; [json.loads(l) for l in lines]; print('OK')" && uv run pytest tests/test_retriever_search.py tests/test_reranker_canary.py -m "not real" -ra -q 2>&1 | grep -qE "(7 xfailed|8 xfailed)" && uv run pytest tests/test_reranker_canary.py -m real --collect-only -q 2>&1 | grep -q "test_reranker_canary_real_mode"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/test_retriever_search.py` contains exactly 5 test functions (`grep -c "^def test_" tests/test_retriever_search.py` returns 5); all are xfail-marked (`grep -B1 "^def test_" tests/test_retriever_search.py | grep -c "xfail(strict=True"` returns 5).
    - Source assertion: `tests/test_reranker_canary.py` contains exactly 4 test functions (`grep -c "^def test_" tests/test_reranker_canary.py` returns 4); all are xfail-marked; `test_reranker_canary_real_mode` ALSO carries `@pytest.mark.real` (verified by `grep -B2 "def test_reranker_canary_real_mode" tests/test_reranker_canary.py | grep -c "pytest.mark.real"` returns 1).
    - Source assertion: `tests/test_reranker_canary.py` contains a module-level `_CLAUDE_MD_QUOTE` constant whose value contains all three substrings `"BGE 512-token truncation FIRST"`, `"before suspecting hybrid retrieval, RRF, or chunk size"`, `"the canary exists specifically to catch it"` (verified by `grep -c "BGE 512-token truncation FIRST" tests/test_reranker_canary.py` returns ≥1).
    - Source assertion: `tests/test_reranker_canary.py` contains `_REPO_ROOT = Path(__file__).resolve().parent.parent` (verified by grep).
    - Behavior: `data/eval/canary/cases.jsonl` exists, every non-empty line parses as JSON, contains ≥1 record with the six required D-13 fields (`case_id`, `question`, `gold_chunk_ids`, `ticker`, `fiscal_year`, `rationale`).
    - Test command: `uv run pytest tests/test_retriever_search.py tests/test_reranker_canary.py -m "not real" -ra -q` shows 7 or 8 xfailed (5 from retriever_search + 3 from canary excluding the real-mode one which is deselected by `not real`), 0 passed, 0 failed (the count is 7 if real-mode is deselected by `not real` AND not counted; pytest behavior: marker-deselected tests are reported in `deselected` not `xfailed` — so likely 7 xfailed + 1 deselected).
    - Test command: `uv run pytest tests/test_reranker_canary.py -m real --collect-only -q` finds `test_reranker_canary_real_mode`.
    - CLI: full quick suite `uv run pytest tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py tests/test_retriever_search.py tests/test_reranker_canary.py -m "not real" -ra -q` exit code 0; reports 21 or 22 xfailed total.
  </acceptance_criteria>
  <done>Two test files committed (5 + 4 = 9 test function scaffolds, all xfail-marked, real-mode marker function-level on the canary real test); `data/eval/canary/cases.jsonl` exists with the placeholder record; pytest collection succeeds with zero ImportError; quick suite runs in under 10 seconds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for Plan 05-01) | This plan creates test scaffolds + a placeholder JSONL — no production behavior change, no new SDK call site, no trust-boundary crossing. The threat surface for Plan 05-01 itself is empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-V5-01-W0 | Tampering | `tests/test_reranker_canary.py::_CLAUDE_MD_QUOTE` | mitigate | The verbatim CLAUDE.md quote is encoded as a module-level constant in this plan; `test_failure_message_quotes_claude_md` asserts on the three required substrings, turning any future "make error messages clearer" cleanup into a red CI signal (Pitfall 6 + D-16). |
| T-5-V5-02-W0 | Tampering | `data/eval/canary/cases.jsonl` placeholder loader | accept | The Wave 0 placeholder is a single committed record so `tests/test_reranker_canary.py::test_cases_loaded` has a loadable target. The xfail strict=True marker means the test FAILS if it accidentally starts passing at Wave 0 (placeholder count < 5 floor), forcing Wave 2's curation work to land before the canary's xfail can be removed. Risk accepted: the placeholder is grep-visible (`PLACEHOLDER — replaced by curated cases in Plan 05-06`), so a casual commit will not mistake it for a real case. |
</threat_model>

<verification>
After both tasks complete:

1. `uv run pytest tests/test_rrf_fuse.py tests/test_null_adapters.py tests/test_retrieved_chunk_schema.py tests/test_make_retriever.py tests/test_retriever_search.py tests/test_reranker_canary.py -m "not real" -ra -q` — reports ≥ 22 xfailed (14 + 5 + 3, plus optionally `test_failure_message_quotes_claude_md` if xfail-wrapped), 0 passed, 0 failed.
2. `uv run pytest -ra -q` — full suite passes; Phase 4's existing 96-pass baseline is unchanged; only new contribution is the additional xfails.
3. `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — all three grep gates still exit 0 (Phase 5 added no new SDK call sites; these gates do NOT need extension).
4. `git status -- data/eval/` — the new `data/eval/canary/` directory and `cases.jsonl` are tracked (not gitignored — committed per D-13).
5. `python -c "import json; [json.loads(l) for l in open('data/eval/canary/cases.jsonl') if l.strip()]"` — exits 0 (every non-empty line is valid JSON).
</verification>

<success_criteria>
- 6 test files exist under `tests/` with a combined 23 xfail-marked test functions (14 + 5 + 4) — every function name verbatim from 05-VALIDATION.md Per-Task Verification Map columns.
- `data/eval/canary/cases.jsonl` exists with ≥1 placeholder record matching the D-13 6-field schema.
- pytest collection emits zero ImportError; Phase 4's 96-pass baseline is preserved.
- The `_CLAUDE_MD_QUOTE` constant in `tests/test_reranker_canary.py` contains the verbatim three substrings required by D-16 + Pitfall 6.
- No new dependencies added; no Settings field added; no production code modified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-hybrid-rerank/05-01-SUMMARY.md` with: files created, total xfail count, the verbatim `_CLAUDE_MD_QUOTE` value (so Wave 2 can grep against it), and the list of test-name-to-plan handoffs (which xfail strict=True marker each Wave 1+ plan must remove).
</output>
