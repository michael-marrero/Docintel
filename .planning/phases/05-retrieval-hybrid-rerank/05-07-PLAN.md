---
phase: 05-retrieval-hybrid-rerank
plan: 07
type: execute
wave: 4
depends_on: [05-06]
files_modified:
  - tests/test_reranker_canary.py
  - scripts/measure_tokenizer_drift.py
  - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md
  - .planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md
autonomous: false
requirements: [RET-01, RET-02, RET-03, RET-04]
must_haves:
  truths:
    - "Every xfail marker introduced in Plans 05-01..05-06 has been removed; full test suite (`uv run pytest -m 'not real' -ra -q`) reports 0 xfailed and 0 xpassed"
    - "Real-mode canary test_reranker_canary_real_mode is empirically verified under workflow_dispatch (gh workflow run + green outcome) and its xfail marker is removed"
    - "Decision-coverage audit at .planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md confirms every D-01..D-17 + CD-01..CD-09 has at least one source-of-truth citation in code or tests"
    - "scripts/measure_tokenizer_drift.py committed; ran against the 6,053-chunk corpus; output committed into 05-RESEARCH.md assumption A1 row (was 'assumed ~5%', now empirical)"
    - "Phase 5 STRUCTURAL ACCEPTANCE GATE per ROADMAP.md is closed — all four RET-* requirements have a green test command, every CONTEXT.md decision is mapped to code"
    - "The three CI grep gates (check_adapter_wraps, check_index_wraps, check_ingest_wraps) remain green; mypy --strict remains green; `uv lock --check` remains green"
  artifacts:
    - path: "scripts/measure_tokenizer_drift.py"
      provides: "One-shot diagnostic — loads BGE-embedder + bge-reranker tokenizers, iterates all 6,053 chunks, reports max/p99/mean ratio of XLM-RoBERTa-token-count to BERT-WordPiece-token-count; informs RESEARCH §3 + Pitfall 1 + A1 assumption"
    - path: ".planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md"
      provides: "Coverage matrix mapping every D-01..D-17 + CD-01..CD-09 to the source file + line range where the decision is realized; serves as gsd-verifier evidence and Phase 13 DECISIONS.md ADR seed"
  key_links:
    - from: "scripts/measure_tokenizer_drift.py output"
      to: "05-RESEARCH.md A1 assumption row"
      via: "diagnostic numbers committed into the Assumptions Log"
      pattern: "WordPiece.*SentencePiece"
    - from: "05-DECISIONS-AUDIT.md"
      to: "every D-01..D-17 + CD-01..CD-09 source-of-truth"
      via: "table mapping each decision ID to the code location (file:line) where it lands"
      pattern: "D-[0-9]+|CD-[0-9]+"
---

<objective>
Close Phase 5 by: (1) removing every remaining xfail marker introduced in Plans 05-01..05-06; (2) running the real-mode canary under workflow_dispatch and verifying it green; (3) producing a Decision-Coverage Audit document mapping every D-01..D-17 + CD-01..CD-09 to its source-of-truth code location (gsd-verifier evidence + Phase 13 DECISIONS.md ADR seed); (4) running the tokenizer-drift diagnostic script against the 6,053-chunk corpus and committing the empirical numbers into RESEARCH.md's A1 assumption row (replacing the "assumed ~5%" placeholder with measured data).

Purpose: The final phase gate. Plans 05-01..05-06 ship the implementation and the canary cases; this plan turns the implementation into a verifiable phase outcome (gsd-verifier 15/15 dimensions, RET-01..04 closed, ROADMAP.md Phase 5 STRUCTURAL ACCEPTANCE GATE closed) and produces the audit-trail document that downstream phases consume.

Output: Real-mode canary verified under workflow_dispatch; one new diagnostic script; one new audit document; RESEARCH.md updated with empirical A1 row; all xfail markers removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md
@.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md
@.planning/phases/05-retrieval-hybrid-rerank/05-PATTERNS.md
@.planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md

<interfaces>
Decision-coverage audit format (mirrors Phase 4 decision-coverage gate per STATE.md "Decision-coverage gate passes 21/21" at the Plan 04-07 line):

A markdown table with one row per locked decision (D-01..D-17) and per Claude's Discretion (CD-01..CD-09), columns:
- Decision ID
- One-line summary
- Source-of-truth (file:line or test:function)
- Status (✓ realized / ⚠ partial / ❌ unrealized)
- Notes

Example row:
- D-07 — RRF_K = 60 pinned module constant — `packages/docintel-retrieve/src/docintel_retrieve/fuse.py:<line>` — ✓ realized — `tests/test_rrf_fuse.py::test_rrf_k_constant`

Total expected rows: 17 D + 9 CD = 26 rows. The gate passes when every row is ✓ or has a documented ⚠ rationale.

Tokenizer-drift diagnostic (RESEARCH §3 + Pitfall 1 + Assumption A1 + VALIDATION.md "Manual-Only Verifications" row 3):

Script structure (per VALIDATION.md row 3):
- Load `AutoTokenizer.from_pretrained("BAAI/bge-small-en-v1.5")` (BERT WordPiece, the EMBEDDER side).
- Load `AutoTokenizer.from_pretrained("BAAI/bge-reranker-base")` (XLM-RoBERTa SentencePiece, the RERANKER side).
- Read every chunk from `data/corpus/chunks/**/*.jsonl` (6,053 total).
- For each chunk's `.text` field, count tokens under both tokenizers.
- Report: mean ratio (XLM / BERT), p99 ratio, max ratio, count of chunks where XLM > 500 (the reranker truncation risk).
- Exit 0 always (diagnostic, not a gate); print summary to stdout.

RESEARCH A1 row update format (in 05-RESEARCH.md "Assumptions Log" table):
- Current row: `| A1 | "BERT WordPiece vs XLM-RoBERTa SentencePiece typical English-prose disagreement ~5%" | §3 Tokenizer drift | Could be 10-20%. Recommend planner spawn a one-shot diagnostic ... |`
- After this plan: same row, but the "Risk if Wrong" column appended with "MEASURED on 2026-MM-DD: mean ratio X.XX, p99 ratio Y.YY, max ratio Z.ZZ, N chunks over 500 reranker-tokens. See `scripts/measure_tokenizer_drift.py` for the diagnostic; full output committed to .planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md."

The plan also writes the full diagnostic output to a new `.planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md` file (planning artifact — gitignored under `.planning/`).

Existing infrastructure to NOT touch:
- `Settings` (no new env vars).
- `Makefile` (Phase 5 has no CLI).
- `docker-compose.yml` (no new services).
- `scripts/check_*_wraps.sh` (no extension needed).
- Phase 4 MANIFEST schema (read-only).

xfail audit:
After Plans 05-01..05-06 ran, the remaining xfail markers should be:
- ZERO on `test_rrf_fuse.py` (removed in Plan 05-03).
- ZERO on `test_null_adapters.py` (removed in Plan 05-04).
- ZERO on `test_retrieved_chunk_schema.py` (removed in Plan 05-02).
- ZERO on `test_make_retriever.py` (removed in Plan 05-05).
- ZERO on `test_retriever_search.py` (removed in Plan 05-05).
- ZERO on `test_reranker_canary.py` for the 3 stub-mode-eligible tests (removed in Plan 05-06).
- ONE remaining: `test_reranker_canary_real_mode` — to be removed in this plan after workflow_dispatch verification.

If any other xfail remains, this plan removes it (or documents why it must stay).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run scripts/measure_tokenizer_drift.py against the 6,053-chunk corpus + commit empirical numbers to RESEARCH.md A1 row</name>
  <files>scripts/measure_tokenizer_drift.py, .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md, .planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md</files>
  <read_first>
    - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md (§3 Tokenizer drift at lines 1089-1169; Pitfall 1 at lines 648-665; Assumptions Log row A1 at line 1568)
    - .planning/phases/05-retrieval-hybrid-rerank/05-VALIDATION.md (Manual-Only Verifications row 3 — the diagnostic-script playbook)
    - packages/docintel-core/src/docintel_core/adapters/real/embedder_bge.py (verify the bge-small tokenizer load path — `AutoTokenizer.from_pretrained("BAAI/bge-small-en-v1.5")`)
    - packages/docintel-core/src/docintel_core/adapters/real/reranker_bge.py (verify the bge-reranker-base tokenizer load path)
    - data/corpus/chunks/AAPL/FY2024.jsonl (one chunk file — verify the JSONL line shape with `chunk.text`)
  </read_first>
  <action>
    Create `scripts/measure_tokenizer_drift.py` per VALIDATION.md Manual-Only row 3 + RESEARCH §3 + Assumption A1.

    Script content: standalone Python module that runs as `uv run python scripts/measure_tokenizer_drift.py` and produces stdout summary.

    Imports:
    - `from pathlib import Path`
    - `import statistics`
    - `from docintel_core.types import Chunk`
    - `from transformers import AutoTokenizer` (or — to avoid heavy import — `from docintel_core.adapters.real.embedder_bge import BGEEmbedder; from docintel_core.adapters.real.reranker_bge import BGEReranker` and reach into `_model.tokenizer` on instantiated adapters; the planner picks the cleanest path).

    Recommended approach (avoids torch import): use `from transformers import AutoTokenizer` directly:
    - `embedder_tok = AutoTokenizer.from_pretrained("BAAI/bge-small-en-v1.5")`
    - `reranker_tok = AutoTokenizer.from_pretrained("BAAI/bge-reranker-base")`

    Walk `data/corpus/chunks/**/*.jsonl`:
    - Use the pattern from PATTERNS.md (sorted rglob, splitlines, Chunk.model_validate_json).
    - For each chunk, compute `n_bert = len(embedder_tok.encode(chunk.text, truncation=False))` and `n_xlmr = len(reranker_tok.encode(chunk.text, truncation=False))`.
    - Track: list of ratios `n_xlmr / n_bert`; list of (chunk_id, n_xlmr) where `n_xlmr > 500`.

    Print summary:
    ```
    TOKENIZER DRIFT DIAGNOSTIC (Phase 5 A1 assumption verification)
    ===============================================================
    Corpus: data/corpus/chunks/**/*.jsonl (N chunks)
    BERT WordPiece tokenizer:  BAAI/bge-small-en-v1.5
    XLM-RoBERTa SentencePiece: BAAI/bge-reranker-base

    Token-count ratio (XLM-RoBERTa / BERT):
      mean: X.XX
      median: X.XX
      p99: X.XX
      max: X.XX

    Chunks where XLM-RoBERTa-token-count > 500 (reranker truncation risk):
      count: N
      list:
        - chunk_id (n_bert -> n_xlmr)
        - ...

    A1 assumption (research §3): ~5% disagreement on English prose.
    A1 measurement: mean ratio = X.XX (so disagreement = (X.XX - 1.0) * 100 = Y.YY%).
    A1 status: <CONFIRMED|REFUTED|EXTENDED — planner decides based on numbers>
    ```

    Run the script (Bash, one-shot): `uv run python scripts/measure_tokenizer_drift.py > .planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md`. Commit both the script AND the output file. The output file is a planning artifact (gitignored under `.planning/`); the script is a permanent diagnostic tool (committed under `scripts/`).

    Update `.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md` Assumption A1 row: append to the "Risk if Wrong" column the text "MEASURED on 2026-MM-DD: mean ratio X.XX, p99 ratio Y.YY, max ratio Z.ZZ, N chunks over 500 reranker-tokens. See `scripts/measure_tokenizer_drift.py` for the diagnostic; full output at `.planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md`." (Substitute the actual measured numbers; "2026-MM-DD" is the run date.)

    If the measured mean ratio is > 1.10 (10%+ disagreement): the soft warning `chunk_reranker_token_overflow` from Plan 05-05 becomes load-bearing, not belt-and-suspenders. Note this in the SUMMARY for Phase 13 ADR seed.

    If the measured count of chunks with `n_xlmr > 500` is > 0: the canary risk profile is elevated. Note this in SUMMARY. The soft warning's INFO-level emit would fire on those specific chunk_ids during a real-mode search if they enter top-M.

    The script must not crash on a missing chunks JSONL file; if the chunks directory is empty (running on a fresh clone before `make fetch-corpus`), print a warning and exit 1.

    Real-mode dependency: this script runs in REAL mode (needs torch + transformers + huggingface model download). Document this in the script docstring — the user runs this once locally OR via a workflow_dispatch trigger (planner's pick). If the user opts for workflow_dispatch, the script call can be added to the `real-index-build` job; otherwise it's a developer-machine command.
  </action>
  <verify>
    <automated>test -f scripts/measure_tokenizer_drift.py && grep -q "BAAI/bge-small-en-v1.5" scripts/measure_tokenizer_drift.py && grep -q "BAAI/bge-reranker-base" scripts/measure_tokenizer_drift.py && grep -q "MEASURED on" .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md && test -f .planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `scripts/measure_tokenizer_drift.py` exists and references both `BAAI/bge-small-en-v1.5` and `BAAI/bge-reranker-base` (verified by grep).
    - Source assertion: `.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md` Assumption A1 row's "Risk if Wrong" column contains the verbatim substring "MEASURED on" followed by a date (verified by `grep "MEASURED on" .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md` returns ≥1 match).
    - Behavior: `uv run python scripts/measure_tokenizer_drift.py` exits 0 (assuming the corpus is present) AND prints all six summary fields (mean, median, p99, max ratios; chunks-over-500 count; A1 status).
    - Behavior: `.planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md` exists and contains the full script output.
    - CLI: script load time is < 30 seconds (model download dominates first run; subsequent runs hit HF cache).
  </acceptance_criteria>
  <done>Tokenizer-drift diagnostic landed; A1 assumption replaced with empirical numbers; planning artifact captured at 05-TOKENIZER-DRIFT.md; the soft warning's status (load-bearing vs belt-and-suspenders) is documented based on measurement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the real-mode canary under workflow_dispatch and verify green; remove the last xfail marker on test_reranker_canary_real_mode</name>
  <files>tests/test_reranker_canary.py</files>
  <read_first>
    - tests/test_reranker_canary.py (current state — test_reranker_canary_real_mode has both @pytest.mark.real AND @pytest.mark.xfail per Plan 05-06)
    - .github/workflows/ci.yml (real-index-build job at lines 240-290 — already runs `uv run pytest -m real -ra -q` which collects this test)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-15 stub-mandatory/real-via-dispatch + D-14 aggregate criterion)
    - .planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md (§10 workflow_dispatch wiring)
  </read_first>
  <what-built>
    Plan 05-06 left `test_reranker_canary_real_mode` with BOTH a `@pytest.mark.real` marker AND a `@pytest.mark.xfail(strict=True, reason="Plan 05-07 — real-mode verification under workflow_dispatch")` marker because the real-mode bge-reranker-base path requires HuggingFace model download (~250 MB, ~30-60s first-run cold) and the canary cases.jsonl needs empirical verification under the real reranker. This task closes that loop.

    Workflow:
    1. Developer opens the PR for Phase 5 (or pushes branch).
    2. Default CI's `lint-and-test` job runs the stub-mode canary green (already verified by Plan 05-06).
    3. Developer triggers `gh workflow run` against the `real-index-build` job (workflow_dispatch).
    4. The job spins up Qdrant via compose `--profile real`, runs `docintel-index all`, then `uv run pytest -m real -ra -q`. The `pytest -m real` step collects BOTH the Phase 4 `test_index_build_real.py` AND Phase 5's `test_reranker_canary_real_mode`.
    5. If `test_reranker_canary_real_mode` xpasses (i.e., the real-mode aggregate criterion holds AND xfail strict=True is still in place — pytest reports XPASS as a failure under strict mode), the developer removes the xfail marker and re-runs the workflow to confirm the test now genuinely passes.
    6. If `test_reranker_canary_real_mode` xfails (legitimate failure, real-mode aggregate criterion does NOT hold) — investigation per D-16 debug order: first verify-chunks (n_tokens < 500), then bge-reranker-base SDK pin, then RRF/chunk-size. The developer iterates on cases.jsonl (Plan 05-06's curation work resumes) and re-runs the workflow until D-14 holds.

    The Claude executor cannot fire workflow_dispatch on its own — this is a human action. Claude prepares the test file (removes the xfail), the developer triggers the workflow, the developer reports back.
  </what-built>
  <how-to-verify>
    1. Developer opens the Phase 5 PR or pushes the branch to GitHub.
    2. Developer triggers the workflow: `gh workflow run ci.yml --ref phase/5-retrieval-hybrid-rerank` (or via the GitHub Actions UI: select `ci.yml`, click "Run workflow", select branch).
    3. Wait for the `real-index-build` job to complete (timeout-minutes: 15 per existing ci.yml).
    4. Verify in the GitHub Actions UI:
       - The `real-index-build` job's `Real-only pytest` step (line 285-286 of ci.yml) passes.
       - The job output contains the line `test_reranker_canary_real_mode PASSED` OR `XFAIL` (depending on whether the xfail marker is removed at this point).
    5. Two outcomes are acceptable:
       - **A) XPASS (red — strict=True)**: with the xfail marker still in place, pytest reports XPASS as failure. Developer removes the xfail marker (delete the `@pytest.mark.xfail(strict=True, ...)` decorator above `test_reranker_canary_real_mode`), commits, re-pushes, re-triggers workflow_dispatch. The second run should show `PASSED`.
       - **B) Direct PASSED**: the xfail marker was already removed by Claude in Task 2 (preemptively). The first workflow_dispatch run shows `PASSED` immediately.
    6. Developer confirms `test_reranker_canary_real_mode` is PASSED (not XPASS, not XFAIL) on the real-index-build job's output.

    Diagnostic if the canary fails in real mode but passes in stub mode (D-16 debug order):
    1. Check the structlog output for `chunk_reranker_token_overflow` warnings — if present, the tokenizer-drift soft warning (Plan 05-05) is firing, meaning a chunk's XLM-RoBERTa-tokenized count exceeds 500 even though `chunk.n_tokens` (BGE-small) is ≤ 500. This is the Pitfall 1 / RESEARCH §3 failure mode.
    2. Run `make verify-chunks` (or `uv run docintel-ingest verify`) to confirm every chunk has `n_tokens < 500`.
    3. Confirm the bge-reranker-base pin in pyproject.toml has not changed.
    4. ONLY THEN look at RRF / chunk-size / hybrid retrieval logic.

    If the canary trips in real mode AND the diagnostic identifies a curation issue (rather than a code regression), the developer iterates on Plan 05-06's cases.jsonl until D-14 holds in BOTH stub mode AND real mode.
  </how-to-verify>
  <action>Pause for human action: this is `checkpoint:human-verify` because triggering a GitHub Actions workflow_dispatch run requires `gh workflow run` from a logged-in developer machine (or the GitHub UI). Claude prepares the test file state — confirms `test_reranker_canary_real_mode` carries both `@pytest.mark.real` and the `@pytest.mark.xfail(strict=True, reason="Plan 05-07 — real-mode verification under workflow_dispatch")` decorator — and instructs the developer to (1) push the Phase 5 branch, (2) `gh workflow run ci.yml --ref phase/5-retrieval-hybrid-rerank`, (3) wait for `real-index-build` to complete, (4) verify `test_reranker_canary_real_mode` outcome in the job's `Real-only pytest` step output. Two outcomes are acceptable per &lt;how-to-verify&gt;: XPASS (developer removes xfail marker and re-triggers; second run should PASS) OR PASSED on first run (xfail marker was removed preemptively by Claude). Once the developer confirms PASSED, Claude removes the xfail marker (if not already removed) and proceeds to Task 3 (audit + xfail sweep).</action>
  <verify>
    <automated>grep -B3 "def test_reranker_canary_real_mode" tests/test_reranker_canary.py | grep -c "pytest.mark.xfail" | grep -q "^0$" && grep -B3 "def test_reranker_canary_real_mode" tests/test_reranker_canary.py | grep -c "pytest.mark.real" | grep -q "^1$"</automated>
  </verify>
  <done>Workflow_dispatch real-index-build job's `Real-only pytest` step shows `test_reranker_canary_real_mode PASSED`; xfail marker on `test_reranker_canary_real_mode` is removed from `tests/test_reranker_canary.py`; `@pytest.mark.real` marker is preserved (deselects from default CI); developer has confirmed via the resume-signal that real-mode D-14 holds.</done>
  <resume-signal>Reply "real-mode canary green" once the `real-index-build` job's `Real-only pytest` step shows `test_reranker_canary_real_mode PASSED` (NOT XPASS, NOT XFAIL). Claude then proceeds to remove any remaining xfail markers and run the decision-coverage audit. If the canary fails in real mode but passes in stub mode, reply "real-mode canary failed" and Claude initiates the D-16 debug protocol (likely re-opening Plan 05-06 for curation iteration).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove all remaining xfail markers + produce Decision-Coverage Audit (05-DECISIONS-AUDIT.md)</name>
  <files>tests/test_reranker_canary.py, .planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md</files>
  <read_first>
    - tests/test_reranker_canary.py (current state after Task 2 — verify test_reranker_canary_real_mode xfail status)
    - All six test files from Plans 05-01..05-06: tests/test_rrf_fuse.py, tests/test_null_adapters.py, tests/test_retrieved_chunk_schema.py, tests/test_make_retriever.py, tests/test_retriever_search.py, tests/test_reranker_canary.py (xfail audit — every marker introduced by Plan 05-01 should now be removed)
    - .planning/phases/05-retrieval-hybrid-rerank/05-CONTEXT.md (D-01..D-17 + CD-01..CD-09 — the 26 items to audit)
    - All six PLAN.md SUMMARYs in .planning/phases/05-retrieval-hybrid-rerank/: 05-01-SUMMARY.md through 05-06-SUMMARY.md (every plan's "Output" section names the file:line locations of the decisions it realized)
    - packages/docintel-retrieve/src/docintel_retrieve/retriever.py (the densest decision-realization site — D-02/05/06/09/10/11/12 + CD-01/03/04/07 all land here)
    - packages/docintel-core/src/docintel_core/adapters/factory.py (D-04 + CD-08 land here in make_retriever)
    - data/eval/canary/cases.jsonl (D-13 lives here; D-14/15/16/17 are realized by the test driver)
  </read_first>
  <action>
    Two outputs.

    **Output 1 — final xfail sweep:**

    After Task 2 closes the real-mode canary, delete the `@pytest.mark.xfail(strict=True, reason="Plan 05-07 — real-mode verification under workflow_dispatch")` decorator on `test_reranker_canary_real_mode` in `tests/test_reranker_canary.py`. Keep the `@pytest.mark.real` decorator (it gates collection to workflow_dispatch).

    Run a project-wide xfail audit to confirm no other xfail markers remain from Plans 05-01..05-06:
    ```
    grep -rE 'pytest\.mark\.xfail.*Wave\s+[0-9]+\s*[—-]\s*Plan\s+05' tests/
    ```
    The expected output is empty (no matches). If any markers remain, remove them (they are obsolete — the implementation has shipped).

    Verify the full suite:
    ```
    uv run pytest -m "not real" -ra -q
    ```
    Expected: 0 xfailed, 0 xpassed, 0 failed. The Phase 4 96-pass baseline plus Phase 5's new tests (10 RRF/null/schema/factory unit + 5 retriever_search + 3 stub-mode canary = ~18 new passes) all green.

    **Output 2 — Decision-Coverage Audit document:**

    Create `.planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md`. The file has:

    Header:
    ```
    # Phase 5 — Decision-Coverage Audit

    **Audited:** 2026-MM-DD
    **Audit scope:** Every D-01..D-17 (locked decisions) + CD-01..CD-09 (Claude's Discretion) from `05-CONTEXT.md`.
    **Audit gate:** Every row must be ✓ realized; any ⚠ partial or ❌ unrealized requires a documented rationale.
    **Total rows:** 26 (17 D + 9 CD).
    ```

    Table for D-01..D-17:
    ```
    | Decision ID | One-line summary | Source-of-truth | Status | Notes |
    |-------------|-------------------|-----------------|--------|-------|
    | D-01 | New 7th workspace package docintel-retrieve | packages/docintel-retrieve/pyproject.toml + Plan 05-02 | ✓ | Library-only (no [project.scripts]) per D-01 |
    | D-02 | Single seam Retriever.search(query, k) -> list[RetrievedChunk] | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.search | ✓ | Internal _bm25_candidates/_dense_candidates/_fuse/_rerank are private helpers (testable but not public) |
    | D-03 | RetrievedChunk seven minimal fields | packages/docintel-core/src/docintel_core/types.py::RetrievedChunk | ✓ | ConfigDict(extra="forbid", frozen=True) — tests/test_retrieved_chunk_schema.py asserts |
    | D-04 | make_retriever in docintel_core.adapters.factory | packages/docintel-core/src/docintel_core/adapters/factory.py::make_retriever | ✓ | Lazy import of Retriever inside function body; string forward reference; tests/test_make_retriever.py::test_factory_lazy_imports_retriever_module verifies |
    | D-05 | top-N=100 per retriever | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::TOP_N_PER_RETRIEVER | ✓ | Final[int] = 100 |
    | D-06 | top-M=20, top-K=5 | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::TOP_M_RERANK + TOP_K_FINAL | ✓ | Module constants; not Settings fields |
    | D-07 | RRF_K = 60 (Cormack 2009) | packages/docintel-retrieve/src/docintel_retrieve/fuse.py::RRF_K | ✓ | Pinned Final[int]; tests/test_rrf_fuse.py::test_rrf_k_constant asserts |
    | D-08 | Null adapter ablation seams | packages/docintel-retrieve/src/docintel_retrieve/null_adapters.py | ✓ | NullReranker + NullBM25Store satisfy protocols structurally; tests/test_null_adapters.py verify |
    | D-09 | Reranker input = raw chunk.text verbatim | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.search Step E | ✓ | [chunk.text for chunk in top_m_chunks] — no prefix |
    | D-10 | Pre-rerank assertion chunk.n_tokens <= 500 + CLAUDE.md quote | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.search Step D + _CLAUDE_MD_HARD_GATE | ✓ | tests/test_retriever_search.py::test_assertion_quotes_claude_md asserts the verbatim substrings |
    | D-11 | Query truncation at 64 BGE tokens + structlog warn | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.search Step A + ._truncate_query | ✓ | tests/test_retriever_search.py::test_query_truncation_logs asserts |
    | D-12 | Per-stage structlog telemetry retriever_search_completed | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.search Step G | ✓ | All twelve fields present; tests/test_retriever_search.py::test_telemetry_fields asserts |
    | D-13 | Canary cases.jsonl at data/eval/canary/ | data/eval/canary/cases.jsonl | ✓ | N curated cases (replace N with actual count); D-13 6-field schema |
    | D-14 | Aggregate pass criterion (rerank > dense AND >= 5) | tests/test_reranker_canary.py::test_reranker_canary_stub_mode + ::test_reranker_canary_real_mode | ✓ | Stub-mode green; real-mode workflow_dispatch verified |
    | D-15 | Stub-mode mandatory CI gate, real-mode workflow_dispatch | .github/workflows/ci.yml lint-and-test step + real-index-build job + @pytest.mark.real | ✓ | Per RESEARCH §10 |
    | D-16 | Failure protocol with verbatim CLAUDE.md quote + debug order | tests/test_reranker_canary.py::_CLAUDE_MD_QUOTE + ::test_failure_message_quotes_claude_md | ✓ | Pitfall 6 doubled (_CLAUDE_MD_QUOTE in test + _CLAUDE_MD_HARD_GATE in retriever.py) |
    | D-17 | Hand-curated cases from real 10-K text | data/eval/canary/cases.jsonl + .planning/phases/05-retrieval-hybrid-rerank/05-06-SUMMARY.md | ✓ | No templated generation; no third-party eval sets; curated against AAPL/MSFT/NVDA/JPM/V/PG etc. (the 9 fully-covered tickers per STATE.md) |
    ```

    Table for CD-01..CD-09:
    ```
    | Decision ID | One-line summary | Source-of-truth | Status | Notes |
    |-------------|-------------------|-----------------|--------|-------|
    | CD-01 | Eager chunk_map load + warm-up calls in Retriever.__init__ | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.__init__ + ._load_chunk_map | ✓ | tests/test_make_retriever.py::test_chunk_map_eager_load asserts cardinality vs MANIFEST.chunk_count (Pitfall 7) |
    | CD-02 | RetrievedChunk lives in docintel_core.types (not docintel-retrieve.types) | packages/docintel-core/src/docintel_core/types.py | ✓ | Shared contract for Phase 6/7/13 |
    | CD-03 | Retriever holds AdapterBundle + IndexStoreBundle as instance attributes | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.__init__ | ✓ | self._bundle + self._stores |
    | CD-04 | No tenacity wraps in Retriever | packages/docintel-retrieve/src/docintel_retrieve/retriever.py + scripts/check_*_wraps.sh | ✓ | Three CI grep gates green; no @retry decorators in retriever.py |
    | CD-05 | BM25-rank tie-break in RRF | packages/docintel-retrieve/src/docintel_retrieve/fuse.py::_sort_key | ✓ | tests/test_rrf_fuse.py::test_rrf_skip_missing asserts the BM25-first ordering |
    | CD-06 | Reranker batch_size default 32 (passes 20 pairs in one call) | (no explicit code — relies on CrossEncoder default) | ✓ | The existing BGEReranker adapter from Phase 2 passes the default; no Phase 5 override; CONTEXT.md CD-06 explicitly leaves to planner discretion |
    | CD-07 | Zero-candidate path returns [] + structlog warn | packages/docintel-retrieve/src/docintel_retrieve/retriever.py::Retriever.search Step C zero-candidate guard | ✓ | tests/test_retriever_search.py::test_zero_candidates asserts |
    | CD-08 | No factory-level cache (callers cache) | packages/docintel-core/src/docintel_core/adapters/factory.py::make_retriever | ✓ | No @lru_cache decorator; Phase 13 caches at FastAPI dependency level |
    | CD-09 | Stub-mode canary < 2s; real-mode < 30s under workflow_dispatch | tests/test_reranker_canary.py + .github/workflows/ci.yml timeout-minutes: 15 | ✓ | Stub-mode runtime: TBD (measure during execution); real-mode runtime: TBD (workflow_dispatch verification in Task 2) |
    ```

    Footer:
    ```
    ## Audit outcome

    - **17/17 D-* items ✓ realized.**
    - **9/9 CD-* items ✓ realized.**
    - **Total: 26/26 ✓ — Phase 5 STRUCTURAL ACCEPTANCE GATE per ROADMAP.md is closed.**
    - **Pitfalls addressed (from 05-RESEARCH.md):** 1 (tokenizer drift — soft warning + diagnostic), 5 (RRF 0→1-based — test_rrf_one_based_ranks), 6 (verbatim CLAUDE.md quote — doubled with _CLAUDE_MD_QUOTE + _CLAUDE_MD_HARD_GATE), 7 (chunk_map cardinality check), 9 (testpaths exclusion — unchanged), 10 (stub determinism — verified via hash-based stub).
    - **Threat model addressed:** T-5-V5-01 (D-11 hard cap), T-5-V5-02 (chunk_map cardinality), T-5-V5-03 (D-10 + D-16 + soft warning), T-5-V5-05 (telemetry counts/timings only), T-5-V5-06 (CD-07 empty list).
    - **A1 assumption (tokenizer drift):** measured on 2026-MM-DD — see Task 1.
    ```

    Document gsd-verifier expectations: every RET-* requirement has at least one passing test command. List them:
    - RET-01: `uv run pytest tests/test_rrf_fuse.py -ra -q` green (5 tests)
    - RET-02: `uv run pytest tests/test_retriever_search.py -ra -q` green (5 tests)
    - RET-03: `uv run pytest tests/test_reranker_canary.py -m "not real" -ra -q` green (3 tests) AND workflow_dispatch real-canary green (1 test)
    - RET-04: `uv run pytest tests/test_retrieved_chunk_schema.py -ra -q` green (4 tests) AND `uv run pytest tests/test_retriever_search.py::test_retriever_returns_top_k -ra -q` green

    Cross-link to Phase 13 DECISIONS.md ADR seed: every ✓ row maps to a potential ADR line in the Phase 13 deliverable. Tag at the bottom: "Phase 13 ADR seed — extract from this audit for the ≥ 8 ADRs UI-05 requires."
  </action>
  <verify>
    <automated>test -f .planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md && grep -c "^| D-" .planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md | python -c "import sys; n = int(sys.stdin.read()); assert n == 17, f'expected 17 D-* rows, got {n}'; print('OK')" && grep -c "^| CD-" .planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md | python -c "import sys; n = int(sys.stdin.read()); assert n == 9, f'expected 9 CD-* rows, got {n}'; print('OK')" && ! grep -rE 'pytest\.mark\.xfail.*Wave\s+[0-9]+\s*[—-]\s*Plan\s+05' tests/ && uv run pytest -m "not real" -ra -q 2>&1 | grep -qE "0 xfailed|^(?!.*xfail)" && uv run mypy --strict && bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh && uv lock --check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `.planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md` exists with 17 D-* rows and 9 CD-* rows (26 total).
    - Source assertion: every row has a source-of-truth column with at least one `file:line` or `file::function` citation.
    - Source assertion: every row has a status column with `✓`, `⚠`, or `❌`; the audit outcome footer counts ✓ totals.
    - Source assertion: NO xfail markers remain in any Phase 5 test file (verified by the grep -rE command — returns no matches).
    - Test command: `uv run pytest -m "not real" -ra -q` reports 0 xfailed, 0 xpassed, 0 failed (full suite green).
    - Test command: `uv run pytest -m real --collect-only -q` collects both `test_index_build_real` (Phase 4) AND `test_reranker_canary_real_mode` (Phase 5).
    - CLI: `uv run mypy --strict` exits 0 across all source files.
    - CLI: `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — three CI grep gates exit 0.
    - CLI: `uv lock --check` exits 0.
    - Behavior: `.planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md` cross-links to RET-01..RET-04 requirements with their respective passing test commands.
  </acceptance_criteria>
  <done>Phase 5 phase gate closed: every Phase 5 xfail marker removed; decision-coverage audit shows 26/26 ✓; the four RET-* requirements each have ≥1 green test command; mypy strict + three CI grep gates + uv lock --check all green; the audit document is the gsd-verifier evidence + Phase 13 DECISIONS.md ADR seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new for Plan 05-07) | This plan adds one diagnostic script and one audit document. The diagnostic reads the corpus but does not modify it. The audit document is metadata. Threat surface for this plan is empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-tokenizer-A1 | Tampering / undetected regression | A1 assumption was wrong (drift >> 5%) | mitigate | `scripts/measure_tokenizer_drift.py` produces empirical numbers committed into RESEARCH.md A1 row; if drift > 10%, the SUMMARY flags `chunk_reranker_token_overflow` (from Plan 05-05) as load-bearing rather than belt-and-suspenders, and Phase 11 ablations get a heads-up. |
| T-5-decision-drift | Tampering | A Phase 5 decision (D-* or CD-*) was implemented incorrectly or omitted | mitigate | The Decision-Coverage Audit (Output 2) maps every D-01..D-17 + CD-01..CD-09 to source-of-truth file:line citations. gsd-verifier reads this document to confirm all 26 items are realized. |
| T-5-xfail-pollution | Tampering | A stale xfail marker remains from an earlier wave and silently masks a regression | mitigate | `grep -rE 'pytest\.mark\.xfail.*Wave.*Plan 05' tests/` returns no matches in this plan's final acceptance criteria; the full-suite test command asserts 0 xfailed. |
</threat_model>

<verification>
1. `uv run pytest -m "not real" -ra -q` — full suite green (Phase 4 baseline + Phase 5 new); 0 xfailed; 0 xpassed; 0 failed.
2. `uv run pytest -m real --collect-only -q` — collects both Phase 4's `test_index_build_real` and Phase 5's `test_reranker_canary_real_mode`.
3. Workflow_dispatch real-index-build job shows `test_reranker_canary_real_mode PASSED` (verified by developer in Task 2).
4. `uv run mypy --strict` — clean.
5. `bash scripts/check_index_wraps.sh && bash scripts/check_adapter_wraps.sh && bash scripts/check_ingest_wraps.sh` — all three exit 0.
6. `uv lock --check` — exits 0.
7. `.planning/phases/05-retrieval-hybrid-rerank/05-DECISIONS-AUDIT.md` shows 26/26 ✓.
8. `.planning/phases/05-retrieval-hybrid-rerank/05-RESEARCH.md` A1 row has empirical measurement appended.
9. `.planning/phases/05-retrieval-hybrid-rerank/05-TOKENIZER-DRIFT.md` exists with the full diagnostic output.
10. ROADMAP.md "Phase 5 STRUCTURAL ACCEPTANCE GATE" is verified — `gsd-verifier` 15/15 dimensions PASS expected.
</verification>

<success_criteria>
- 0 xfail markers remaining anywhere in tests/ from Phase 5 plans.
- Real-mode canary green under workflow_dispatch.
- Decision-coverage audit: 26/26 ✓ for D-01..D-17 + CD-01..CD-09.
- Tokenizer-drift A1 assumption: measured against the 6,053-chunk corpus and the numbers committed.
- Three CI grep gates + mypy strict + uv lock --check all green.
- RET-01..RET-04 all closed with named passing test commands.
- Phase 5 STRUCTURAL ACCEPTANCE GATE per ROADMAP.md: closed.
- Phase 13 DECISIONS.md ADR seed: extracted from this audit.
</success_criteria>

<output>
After completion, create `.planning/phases/05-retrieval-hybrid-rerank/05-07-SUMMARY.md` documenting: the final xfail audit (grep output before vs after); the measured tokenizer-drift numbers (mean, p99, max ratio, chunks-over-500 count); the workflow_dispatch run ID and outcome for the real-mode canary; the 26 audit-row source-of-truth file:line citations; the four RET-* test commands and their pass counts; the verification of all three CI grep gates + mypy --strict + uv lock --check; the ADR-seed extraction note for Phase 13 UI-05.
</output>
