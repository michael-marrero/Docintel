---
context: phase
phase: 14-empirical-closure
task: 1
total_tasks: 7
status: in_progress
last_updated: 2026-06-01T12:39:10.062Z
---

# Phase 14 — Resume Instructions

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| cwd-drift into worktree directory | The orchestrator's Bash shell ended up with `cwd` inside the Plan 14-03 worktree path (`.claude/worktrees/agent-ae3477e13118f300d`) after the 14-03 executor returned. The first attempted merge (`git merge` for 14-02) ran inside the worktree branch, not the phase branch — landed a merge commit on the wrong branch before recovery. | advisory | Always run `cd /Users/mm/GitHub/Docintel-main && git rev-parse --abbrev-ref HEAD` before each merge command, and assert the result is the expected phase branch (not `worktree-agent-*`). The recovery path was a clean `git merge --ff-only worktree-agent-*` from the correct directory; future runs should pre-emptively cd. |
| `pytest` without `-m "not real"` | Direct `pytest tests/` invocation runs `@pytest.mark.real` tests which require real API keys; they fail noisily in stub-mode local runs. The Plan 14-01 executor used `-m "not real"` correctly; the orchestrator's first post-merge gate did not, and got a false-positive failure on `test_real_ablate_includes_chunk_sweep`. | advisory | Always run post-merge tests as `pytest -m "not real"` to match the CI gate. `make test` does NOT include this filter — it's wrong as a CI-mirror command. Project's CI runs use the marker filter implicitly through `make test`'s pytest invocation. |
| Assume CI runs == workflow runs locally | First real-eval dispatch crashed on missing API keys because the v1.0 ci.yml had no `secrets.*_API_KEY` references in the real-mode job env blocks. Phase 14 plans assumed the workflow worked end-to-end. | advisory | When resuming, verify both: (1) repo secrets exist via `gh secret list`, (2) ci.yml env blocks reference them. Phase 14 commit 06d1c29 fixed #2; user must do #1 before resuming Wave 3. |

<current_state>
Phase 14 paused at Wave 3 (plan 14-06, Task 1 = checkpoint:human-action). 5 of 7 plans complete (14-01..14-05 plus 2 in-Phase-14 bonus fixes for CI). Branch `phase/14-empirical-closure` is at `06d1c29`, pushed to origin, in sync. The .planning/ dirty-tree entries are gitignored noise (`commit_docs: false`).
</current_state>

<completed_work>

Wave 0 (1 plan):
- 14-01: 7 red xfail-strict test scaffolds (test_eval_set_frozen, test_baseline_schema, test_baseline_cost_matches, test_before_sleep_redacts_api_keys, test_check_before_sleep_safe, test_readme_no_stub_banner, test_hero_gif_present) + 2 planted fixture dirs (before_sleep_violations/_unsafe_example.py, before_sleep_violations_with_safe/safe_example.py). Merge: `79e574b`.

Wave 1 (2 plans, parallel worktrees):
- 14-02: `git mv data/eval/ground_truth/questions.jsonl → eval_set.jsonl` (D-01); 9-site grep-replace across packages/docintel-eval/{runner,ablate,validate}.py + 3 test files; backfill `EVAL_SET_SHA256 = "5d9f879207c6b8a0c363804eebb9add4babefaa63beadcea7dc80dbd8db88d82"` in tests/test_eval_set_frozen.py (D-02); ADR-013 4-step mutation policy in DECISIONS.md (D-03). xfail-strict marker on test_eval_set_frozen.py flipped green.
- 14-03: `packages/docintel-core/src/docintel_core/adapters/real/_logging.py` with `before_sleep_safe(logger, log_level)` factory; mirrors tenacity 9.1.4 semantics + None-guards on outcome/next_action (Pitfall 3); D-05 redaction regex with `sk-ant-` before `sk-`; 4 parametrized redaction tests flipped green.
- Combined merge: `be6a31c`.

Wave 2 (2 plans, parallel worktrees):
- 14-04: 12-site sweep `before_sleep_log → before_sleep_safe` across 6 adapter files (qdrant_dense ×6 at lines 374/385/399/416/431/448, judge ×2 at 129/244 — Pitfall 1 correction vs CONTEXT.md D-04's 6/5 undercount, embedder_bge / llm_anthropic / llm_openai / reranker_bge ×1 each). `scripts/check_before_sleep_safe.sh` with D-06 two-sided gate (negative grep + positive @retry-import check). Wired as 6th step in ci.yml `lint-and-test` job at line 169. 3 gate tests flipped green. Merge: `7464f41`.
- 14-05: `make baseline-lock TS=<ts>` (D-08 validation gates); `make readme-paste` + `scripts/readme_paste.py` 274-line helper (D-10); `docs/REAL-RUN-CHECKLIST.md` +192 lines documenting the 4-step user-owned sequence. Smoke-tested all 3 D-08 gates with bad inputs (missing TS, nonexistent TS, stub-sample TS=representative:false) — all FAIL correctly. Merge: `15d2a25`.

Bonus fixes pushed during Wave 3 attempt (in scope for Phase 14):
- `0408d80`: Unbreak CI lint — UP037 in _logging.py (ours) + auto-fix pre-existing UP035 in trace.py + RUF022 in types.py + RUF002/RUF003 ambiguous `×` in types.py + UP022 in eval_view.py + black-reformat 9 files. main's CI was already failing on these.
- `06d1c29`: Wire `secrets.ANTHROPIC_API_KEY` + `secrets.OPENAI_API_KEY` env passthrough to all 3 real-mode CI jobs (real-mode index build, real-mode eval run, real-mode ablation run). Was a structural EMP-01 gap.

Test gate after all merges + bonus fixes: **257 passed, 4 xfailed (all pinned to 14-06 / 14-07), 0 failed** under `pytest -m "not real"`.
</completed_work>

<remaining_work>

Wave 3 — Plan 14-06 (autonomous: false; Task 1 is checkpoint:human-action; Task 2 is the autonomous verification block that runs AFTER user actions):
- User triggers `gh workflow run ci.yml --ref phase/14-empirical-closure`, watches the run, pulls the committed report
- User eyeballs `data/eval/reports/<ts>/report.md` (representative:true, Hit@3 differential, cost_usd sanity)
- User runs `make baseline-lock TS=<ts>` (writes + commits `data/eval/baseline.json` with D-07 schema)
- User runs `make readme-paste`, reviews diff, commits `docs(README): paste real numbers per EMP-02 / D-10`
- User verifies UI auto-flip via the Python one-liner (Pitfall 5 — code-free)
- User pushes
- Then signals "approved" so the executor removes the 3 xfail decorators on tests/test_baseline_schema.py, tests/test_baseline_cost_matches.py, tests/test_readme_no_stub_banner.py and runs the post-commit test gate

Wave 4 — Plan 14-07 (autonomous: false; depends on 14-06):
- User records `docs/hero.gif` per `docs/HERO-STORYBOARD.md` storyboard (multi-hop comparative → hoverable citations → out-of-corpus refusal → Traces Gantt → Eval-Results tab with representative:true)
- User commits + pushes
- Executor removes the final xfail decorator on tests/test_hero_gif_present.py, writes `.planning/phases/14-empirical-closure/DECISIONS-AUDIT.md` mirroring 06-07/11-06/12-05/13-07 v1.0 audit pattern (every EMP-XX requirement + every D-XX decision with implementation pointers)

After Wave 4: gsd-verifier runs the goal-backward check, advisory code-review, schema-drift gate, marks phase Complete via `gsd-sdk query phase.complete 14`.
</remaining_work>

<decisions_made>

- Branched `phase/14-empirical-closure` off `main` (CLAUDE.md project rule: one phase = one branch) before dispatching execute-phase
- Used `isolation=worktree` parallel agents for Waves 1 and 2; sequential one-`Agent()`-per-message dispatch to avoid `.git/config.lock` race per #2924
- Auto-fixed pre-existing lint debt (UP035/RUF022/RUF002/RUF003 + 9 black reformats) in Phase 14 scope because main's CI was already red on these and EMP-01 couldn't reach the eval step otherwise (commit 0408d80)
- Wired API-key passthrough in ci.yml as a Phase-14 EMP-01 fix (commit 06d1c29) — the v1.0 workflow YAML never referenced the secrets even though Phase 10's D-10 plan assumed they would work
- Deferred Wave 3 + Wave 4 user-action work per user choice in this session
</decisions_made>

<blockers>
- **API keys not configured** on the repo. `gh secret list` returned empty. The workflow YAML now references `secrets.ANTHROPIC_API_KEY` and `secrets.OPENAI_API_KEY` (commit 06d1c29) — the user needs to set both via `gh secret set ANTHROPIC_API_KEY --body 'sk-ant-...'` and `gh secret set OPENAI_API_KEY --body 'sk-...'` (or via GitHub Settings UI) before the next `gh workflow run ci.yml --ref phase/14-empirical-closure` invocation.
- **No representative real-eval report exists** under `data/eval/reports/` yet (only `stub-sample`). EMP-01 requires the user to trigger the workflow_dispatch and wait for the commit.
- **`docs/hero.gif` not recorded.** EMP-03 / D-11 user-owned per ROADMAP; recording rig + screen capture not automatable.
</blockers>

## Required Reading (in order)

1. `.planning/phases/14-empirical-closure/14-CONTEXT.md` — locked decisions D-01..D-11; scope fence ("no v2.0 quality changes")
2. `.planning/phases/14-empirical-closure/14-RESEARCH.md` — Pitfall 1 (12 sites/6 files), Pitfall 2 (9 rename targets), Pitfall 3 (None-guards), Pitfall 4 (macOS toolchain), Pitfall 5 (UI auto-flip is code-free), Pitfall 7 (jq prereq)
3. `docs/REAL-RUN-CHECKLIST.md` — Phase 14 section has the exact 4-step user-owned sequence (lines 263–449)
4. `.planning/phases/14-empirical-closure/14-06-PLAN.md` — the user-action plan with full must_haves and key-links
5. `.planning/phases/14-empirical-closure/14-07-PLAN.md` — the phase-gate plan with DECISIONS-AUDIT.md template pointer
6. `.planning/HANDOFF.json` — machine-readable mirror of this file

## Infrastructure State

- **Branch:** `phase/14-empirical-closure` @ `06d1c29` — pushed to origin, in sync
- **No active worktrees** (all `.claude/worktrees/agent-*` cleaned up)
- **STATE.md:** Phase 14 = In Progress, 5/7 plans complete
- **ROADMAP.md:** Phase 14 entry annotated with wave headers + plan progress
- **Auto-chain flag:** cleared (`workflow._auto_chain_active = false`) — next `/gsd-execute-phase 14` invocation will NOT auto-advance through transition
- **Failed CI run:** Run ID 26727961315 (https://github.com/michael-marrero/Docintel/actions/runs/26727961315) — failed at ruff check (now fixed in 0408d80) and real-eval missing API keys (now wired in 06d1c29)
- **Repo secrets configured:** **none** (`gh secret list` returned empty)
- **Tests:** `pytest -m "not real"` clean — 257 passed, 4 xfailed (pinned to Waves 3+4), 0 failed
- **jq installed locally:** yes (`/usr/bin/jq`) — required by `make baseline-lock` per Pitfall 4
- **gh CLI auth:** logged in as michael-marrero with `repo` + `workflow` scopes

<context>
Phase 14 closes the v1.0 EMPIRICAL-PENDING debt (EMP-01..EMP-05). The autonomous infrastructure (Waves 0-2) is all in place: the eval-set is frozen via SHA256, the API-key-leak hole is plugged via _logging.before_sleep_safe + a 6th CI grep gate, the baseline-lock + readme-paste targets exist and smoke-test. The remaining work is intentionally user-owned per CONTEXT.md D-08 and D-11: the user eyeballs the real-eval report before locking the baseline, and the hero GIF is screen capture work.

When you resume, the natural sequence is:
1. Read .planning/HANDOFF.json for machine-readable state
2. `git pull` to sync (no-op if you're returning to the same machine, but safe)
3. Check `gh secret list` — if empty, run `gh secret set ANTHROPIC_API_KEY --body 'sk-ant-...'` and `gh secret set OPENAI_API_KEY --body 'sk-...'`
4. Then either:
   - Follow `docs/REAL-RUN-CHECKLIST.md` Phase 14 section manually (4 user steps), OR
   - Re-invoke `/gsd-execute-phase 14` which will skip the 5 done plans and resume from 14-06's checkpoint
5. After the 4 user steps land (data/eval/baseline.json committed + README PASTE-REAL-NUMBERS swapped + docs/hero.gif committed), the executor finishes 14-06 + 14-07 autonomously and gsd-verifier closes the phase

There's no time pressure — the phase branch is pushed and the work is preserved. The plan-checker already validated all 7 plans pass quality gates; only execution remains.
</context>

<next_action>
Start with: `cd /Users/mm/GitHub/Docintel-main && git status && gh secret list`. If `gh secret list` is empty, configure ANTHROPIC_API_KEY + OPENAI_API_KEY first. Then re-invoke `/gsd-execute-phase 14` (it will skip the 5 done plans and present the 14-06 human-action checkpoint), OR manually follow `docs/REAL-RUN-CHECKLIST.md` Phase 14 section.
</next_action>
